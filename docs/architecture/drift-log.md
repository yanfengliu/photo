# Architecture Drift Log

| Date | What Changed | Why | Updated By |
| --- | --- | --- | --- |
| 2026-03-29 | Initial architecture doc created from template | Project reached stable multi-module state with Library/Detail tabs | agent |
| 2026-03-30 | Added jpeg-decoder direct dependency | DCT-level downscaling for fast JPEG thumbnails; it was already a transitive dependency | agent |
| 2026-03-30 | Added image editing system (`edit.rs`, `lens.rs`, extended shader, blur pre-pass) | 12 GPU shader-based adjustments, Lensfun lens corrections, undo/redo, save-as-copy | agent |
| 2026-03-30 | Added `kamadak-exif` and `quick-xml` dependencies | EXIF reading for lens auto-detection and Lensfun XML database parsing | agent |
| 2026-04-03 | Added collection module (`collection.rs`) and sidebar UI integration | Named photo collections with JSON persistence, collection sidebar in Library view, context menu/drag types, 19 new message variants (stubbed) | agent |
| 2026-04-03 | Completed collections system (`collection.rs`, sidebar UI, context menus, drag-drop, grid view, detail nav) | Named photo collections with JSON persistence, context menu overlay, drag-and-drop, collection-scoped navigation | agent |
| 2026-04-03 | Added serde and serde_json dependencies | JSON serialization for collection persistence | agent |
| 2026-04-19 | Added RAW image support in `decode.rs`/`nav.rs` and file-dialog filters | The app now treats common camera RAW formats as first-class images by extracting embedded previews or developing raw pixels when needed | codex |
| 2026-04-19 | Split canonical docs into `docs/architecture/`, `docs/devlog/`, and support subdirectories with legacy shims at the old flat paths | Align the repo with AGENTS.md while preserving older links during the transition | codex |
| 2026-04-20 | Switched RAW Detail loading to a staged preview-first flow with async EXIF in `main.rs`/`decode.rs` | Reduce time-to-first-image in Detail while keeping the full-quality RAW decode path | codex |
| 2026-04-21 | Added local per-image edit persistence in `main.rs`/`edit.rs` | Non-destructive Detail edits, including rotation and crop, now survive reopen and restart through a local edit-state store | codex |
| 2026-04-21 | Moved local edit persistence from profile-scoped storage to repo-local `local-edits.json` | Make saved edit state inspectable inside the repo, matching the repo-local decoded-cache direction | codex |
| 2026-04-21 | Replaced persisted edit-operation history with repo-local baked local-edit files in `local-edits/` | The product contract is "reopen the edited image" rather than "restore undo history", so cross-session persistence now stores edited pixels while undo/redo remains session-only | codex |
| 2026-04-22 | Hardened repo-local baked local-edit persistence with shared full/thumbnail generations and source-metadata validation | Keep Library thumbnails and persisted local copies fail-closed after partial writes, EXIF-delayed auto-lens updates, or source rewrites | codex |
| 2026-04-23 | Added persisted decoded-image cache in `decode.rs` (repo-local `decoded-cache/`, normalized path keys, schema + cache-contract versions, source-fingerprint validation, collision-safe temp writes, LRU-style byte/entry caps) plus an in-memory same-session full-image cache in `main.rs` with a small recent-history floor and same-image Library-return short-circuit | Reduce repeated RAW/SVG Detail-load latency without serving stale or partially written entries; keep the two most recent full Detail images hot even when large images would otherwise overflow the byte budget | claude |
| 2026-04-23 | Persistence task now owns the post-commit edited-render and the resulting Library thumbnail refresh instead of the UI thread | Slider release / double-click reset / rotation / auto-lens commits no longer freeze the UI on multi-megapixel images | codex |
| 2026-04-24 | Documentation drift audit: bumped `ARCHITECTURE.md` last-verified date, recorded the persisted decoded-cache + in-memory full-image cache responsibilities, refreshed README project structure | Keep canonical architecture doc aligned with the cache and persistence work landed since the previous verification | claude |
| 2026-06-10 | Split the 9,006-line `main.rs` monolith into focused modules: `app/` (mod/update/view/tests), `theme.rs`, `widgets.rs`, `detail_load.rs`, `session_cache.rs`, `local_edits.rs`, `loading.rs`, `library.rs`, `repo.rs`; `main.rs` is now a 31-line entry point | Pure move refactor (no behavior change, 298 tests green throughout) to restore the no-file->500-LOC review rule's viability and give each subsystem an owned boundary; follow-ups tracked: redistribute `app/tests.rs`, dedup repo-root with `decode.rs`, further split `app/update.rs` and `local_edits.rs` | claude |
| 2026-06-11 | Local-edit store made offline-capable: candidate path-key resolution + fail-open-when-source-absent validation in `local_edits.rs`; thumbnail bases carry provenance (`LoadedThumbnailBase`) so session state never re-renders onto baked pixels; `load_library` keeps offline paths; owed-bake registry persists commits made during staged loads after navigation | Library is the durable home of edited work: with sources on removable camera media, baked edits must stay reachable, library membership must survive unplugged drives, and preview-window commits must not be silently dropped | claude |
| 2026-06-11 | Save-as-copy exports redirected from beside-the-source to repo-local gitignored `edited/` (created on demand; no-repo-root fallback keeps the legacy location); `repo.rs` gained a test override for the absent-repo-root case | The save path wrote exports onto the camera card when photos were opened from removable media; the repo is the durable home for app output | claude |
| 2026-06-11 | Bake-absorption tracking: persists return their written generation, the app records `last_baked_generations` per path, and full-image reloads whose bake generation matches reset the absorbed session edit state instead of re-applying it; mainline persist requests reuse the owed-bake nothing-new predicate | A bake-as-base reload after session-cache eviction re-applied and re-baked the session's edits (compounding corruption, post-hoc review finding 3-H1); unedited baked images also stopped triggering full-resolution identity re-bakes per reopen | claude |
| 2026-06-12 | Default RAW develop tone: `edit::apply_raw_develop_tone` (exposure lift + power-law contrast through the existing primitives, exact 256-entry LUT) applied in `decode.rs`'s full-develop path only; `DECODE_CACHE_CONTRACT_VERSION` 5→6 | rawler develops carry no tone curve and read flat/dark next to the camera's embedded JPEG (user report); tone in the base keeps CPU/GPU parity structural and embedded previews keep the camera's own curve | claude |
| 2026-06-12 | CPU render perf pass in `edit.rs`: blur atlas + per-pixel blur sampling gated to clarity/dehaze ≠ 0, exact identity early-returns in tone-zones/saturation/vibrance, and a row-parallel `std::thread::scope` main render loop (byte-identical output); RAW tone constants recalibrated to (0.85 EV, 65) over a 35-image set | The full-resolution render fires on every committed edit; ~35× faster for typical commits with the entire pixel-exact regression suite as the correctness gate. The v0.2.5 single-photo tone constants were overfit | claude |
| 2026-06-13 | Unified the per-user app storage dir behind `library::local_app_storage_dir()` with a `#[cfg(test)]` thread-local override defaulting to `None`; `collection::collections_file_path()` now derives `collections.json` from it instead of reading `LOCALAPPDATA` directly; added `with_test_app_storage_dir` for opt-in persistence tests | The test suite resolved `%LOCALAPPDATA%/photo` with no isolation, so importing fixtures and creating collections overwrote the real `library.txt` and `collections.json` with temp-dir paths — the Library startup grid then rendered dead `...` thumbnails captioned with fixture filenames (user report). Test builds can no longer read or write real user storage | claude |
| 2026-07-07 | Added the agent-harness subsystem: `launch.rs` (CLI options), `harness/` (protocol contract, run artifacts, TCP JSON-lines server as an iced subscription, pixel statistics), `app/harness_{exec,actions,observe,artifacts}.rs` (dispatcher, user-equivalent actions, observation, artifact pipelines), `bin/harnessctl.rs` (std-only client), runtime storage-sandbox overrides in `library.rs`/`repo.rs`, `save_in_flight` tracking | An AI agent must be able to drive the app like a user and inspect edited renders itself (screenshots + CPU dumps + statistics) to close the edit-quality feedback loop that previously required human relay; modeled on civ-engine's visual-playtest/improvement-loop contracts | claude |
| 2026-07-08 | Deduplicated repo-root discovery: `decode.rs` now delegates cache-root resolution to `crate::repo::photo_repo_root()` (single owner of discovery, test override, and the harness runtime sandbox override); its private duplicate resolver and statics are deleted | Adversarial review (agent-harness iteration 1, HIGH): the duplicate resolver ignored the harness sandbox override, so sandboxed sessions read/wrote/pruned the REAL repo's `decoded-cache/`; this also closes the dedup follow-up tracked since the 2026-06-10 split | claude |
