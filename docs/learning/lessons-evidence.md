# Lessons — evidence

The war story and the evidence-anchor table behind each linked line in [lessons.md](lessons.md). Not session-start reading: open an entry when its rule is in doubt, or when the work is in that area.

One `##` heading per rule, matching the anchor the rule links to. Empty while the queue is empty.

For lessons that have already graduated, the anchor lives with the gate: `../learning/gate-proofs.md` records the mutation that proves each one, and the claim itself is in the gate's own header.
