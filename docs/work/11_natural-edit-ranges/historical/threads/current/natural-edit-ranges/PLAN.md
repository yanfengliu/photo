# Natural Edit Ranges — Plan

1. Capture the current JPEG endpoint-recovery failure with GPU screenshots, CPU renders, statistics, comparison output, and a schema-v1 finding; rerun the same scenario in fresh sandboxed storage to verify the finding.
2. TDD the verified RAW no-upscale prerequisite: add a conversion-helper regression, implement downscale-only behavior, prove the original RAW buffer dimensions through the harness, review, document/version, run the four gates, commit, and push the coherent fix.
3. Mechanically verify the suspected sRGB GPU transfer defect with a non-neutral edit; add source/packing tests that pin the correct hardware-transfer contract, fix the shader if confirmed, re-drive GPU-versus-CPU behavior, review, document/version, run the gates, commit, and push.
4. TDD the tone-zone response defect: add dense monotonicity/spacing coverage for the supplied recipe and all endpoint-sign combinations, retune the shared `tone_zone_amount` mapping to the strongest globally monotone safe envelope, update existing endpoint/parity tests, and run affected tests.
5. Rebuild release and re-drive the exact endpoint-recovery scenario on a fresh JPEG session. Compare before/after screenshots, full-render statistics, and pixel diffs at the bug-class level; promote the finding to verified and then fixed-proven only when the fresh render has continuous tones and the regression passes.
6. Broaden to the calibration corpus: zero RAW anchor, moderate outdoor XMP recipe, dark RAW endpoint recipe, Milky Way blacks/dehaze stress, and carousel highlights/skin stress. Record local findings and iterate through any substantive candidate.
7. Generalize on held-out RAW and JPEG scenes. Stop only after two consecutive materially different observe-act-measure scopes produce no new substantive finding.
8. Run parallel adversarial reviewers with the mandatory live-code verification prompt, fix and re-review substantive findings, synthesize each iteration under this thread, update changelog/devlogs/version and applicable canonical docs, run doc drift checks plus all four Rust gates, commit, push, clean task-owned processes, move the thread to `docs/threads/done/`, and complete the goal.
