# Review — natural-edit-ranges iteration 2

Date: 2026-07-14. Scope: GPU/CPU sRGB-transfer prerequisite for supplied-photo calibration, with lens correction off. Reviewers: in-process independent finder/refuter `gpu_transfer_design_refute` and independent final reviewer `gpu_transfer_final_reviewer`. Both were directed to verify every symbol, texture format, surface fallback, uniform offset, shader exit, test claim, documentation statement, and harness artifact against the live codebase rather than approving from the prompt.

## Findings and dispositions

| ID | Severity | Finding | Disposition |
| --- | --- | --- | --- |
| GPU-1 | HIGH | `Rgba8UnormSrgb` photo/blur textures and the normal sRGB presentation target already perform transfer conversion, while `image.wgsl` repeated both source decoding and output encoding. Identity concealed the defect; Exposure +1 made the GPU preview materially darker than the CPU export path. | Fixed: all five photo/blur texture sites share the sRGB format constant; shader adjustment math consumes hardware-decoded linear samples directly; the sRGB target encodes once. |
| GPU-2 | MEDIUM | The first patch assumed every iced presentation target was sRGB, but iced can fall back to the adapter's first non-sRGB format. | Fixed: Rust derives `output_needs_srgb_encode` from the runtime target format at byte 212; WGSL agrees on the 240-byte layout and conditionally encodes at every fragment exit only for the non-sRGB fallback. |
| GPU-3 | MEDIUM | A CPU byte-128/+1 EV anchor and shader-string assertions alone could not prove the runtime wgpu path. | Fixed within the available contract: the test pins source format, target selection, uniform layout, and absence of a second source decode; a fresh release-harness run supplies the required real-GPU proof. No claim is made that the unit test renders a non-sRGB surface. |
| GPU-4 | MEDIUM | Converting `.05/.18/.25` gray literals as if they were sRGB values would introduce a separate crop/checker appearance change. | Fixed: the longstanding numeric viewer/crop/checker constants are unchanged and receive one consistent linear interpretation under the corrected output contract. |
| GPU-5 | MEDIUM | Lens-enabled CPU/GPU sampling still differs: GPU sRGB texture filtering occurs in linear light, while the CPU lens sampler interpolates encoded bytes before decoding. | Verified as pre-existing and explicitly out of this slider-calibration prerequisite. The design, summary, changelog evidence, and harness claim are lens-off; lens interpolation remains a separate follow-up. |
| GPU-6 | LOW | Canonical architecture text still described unconditional shader gamma encoding; the objective design still described closed prerequisites as pending. | Fixed in `ARCHITECTURE.md` and `DESIGN.md`; architecture verification metadata now reflects the current pass. |

## Verification and verdict

Two fresh pre-fix sessions reproduced the defect exactly. Final sandboxed release-harness run `20260714-163056Z-35340` finalized through `quit` with Exposure +1, every other adjustment neutral, and lens correction off. The measured 1380×920 GPU photo region versus the matched CPU render has luma means 199.54/199.12, p50 205/205, channel-mean gaps no larger than 0.72, and a mean-saturation gap of 0.00023. The remaining 0.00759 highlight-clipping gap is disclosed and consistent with GPU bilinear versus CPU Triangle minification; it does not resemble the former double-digit tone bias. The CPU render is byte-identical before/after. Real GPU screenshots differ across 58.05% of pixels, and a local absolute pixel-diff PNG records the correction.

All 35 viewer tests pass, including the new transfer/target/layout regression. The full suite passes with 426 photo tests plus 7 harnessctl tests and one ignored manual perf probe; clippy with `-D warnings`, rustfmt check, and debug build are green in the isolated target directory. The final independent reviewer reported no substantive finding. Verdict: clean for this coherent, bounded lens-off prerequisite; later dehaze calibration must exercise the changed blur-sampling branch, and the parent natural-range objective remains open.
