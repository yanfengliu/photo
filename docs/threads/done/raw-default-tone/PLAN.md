# Implementation plan: raw-default-tone

Target version: 0.2.5 (non-breaking: bakes keep rendering identically; new RAW develops adopt the new default look; decoded-cache re-derives via contract bump).

1. `edit.rs` primitive (TDD): `apply_raw_develop_tone(pixels, exposure_ev, contrast_slider)` — sRGB-decode → `apply_exposure` → `apply_contrast` → sRGB-encode, in place, alpha untouched. Reuses the validated linear-RGB math so the adjustment-math boundary stays in edit.rs; the policy constants live at the call site. Unit tests: identity at (0,0), midtone brightening at tuned constants, endpoint stability, alpha preservation, ramp monotonicity.
2. `decode.rs` wiring: apply the primitive in `decode_raw_pixels` after `raw_dynamic_image_to_rgba`, gated by `RAW_DEVELOP_TONE_*` constants; bump `DECODE_CACHE_CONTRACT_VERSION` 5 → 6 so stale flat developments re-derive. Test: develop of the DNG fixture equals untoned develop + primitive (pins the wiring); embedded-preview path stays untouched (no tone — camera JPEGs already carry their curve).
3. Tuning harness (env-gated, like the slider grid): `PHOTO_RAW_TONE_COMPARE=<raw path>` develops once untoned, sweeps an (exposure EV, contrast) grid through the primitive, scores each against the embedded camera preview (luma mean, p5/p25/p50/p75/p95, clip fractions), prints the table and writes downscaled PNGs to `tmp/raw-tone/` for visual inspection. Tune on `C:\Users\38909\Documents\images\DSC01169.ARW`; hard-code the winning constants with the metrics recorded in the devlog (the embedded preview is the reference look — see the 2026-04-24 lesson about not claiming proprietary Lightroom math).
4. Gates, multi-CLI review (Gemini + Claude; Codex quota-blocked), docs (changelog 0.2.5, devlog, summary, ARCHITECTURE decode line + flows, decisions row, drift-log), thread to done.
