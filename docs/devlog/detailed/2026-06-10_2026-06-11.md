# Devlog 2026-06-10 — 2026-06-10

## 2026-06-10 18:05 — Escape cancels cropping instead of leaving Detail (v0.2.1)

- Action: User-reported nit: pressing Escape during a crop drag dumped them on the empty-library screen instead of cancelling the crop. Two defects fixed TDD-style (both tests red first): (1) the Escape handler in `app/update.rs` had no crop-mode case, so Detail fell through to the back-to-library branch — added a cancel-crop branch (mirrors `ToggleCropMode`'s zoom preservation) ahead of the navigation branches; (2) the viewer's `ButtonReleased` committed a crop based only on `state.crop_dragging` without re-checking `crop_mode`, so a release after Escape would have committed the abandoned selection — the commit is now gated on `crop_mode` and a lingering drag is dropped on the next cursor move too.
- Reviewer comments: multi-CLI review ran after the fix commit; all three reviewers found the code correct (Claude verified every Escape-branch combination and the full viewer drag state machine adversarially; Codex and Gemini concurred). Findings were docs-side: a dangling devlog pointer (this line, originally written ahead of the synthesis — Codex 1-M1), the stale summary.md version/test-count (Codex 1-M2), a changelog precedence wording slip, and an untested purge branch (now pinned by `crop_drag_is_purged_on_cursor_move_after_crop_mode_cancel`, with the toolbar-click re-entry assumption documented in a code comment). Synthesis: `docs/threads/done/esc-crop-cancel/2026-06-10/1/REVIEW.md` (lands with the close-out commit).
- Result: Escape in crop mode cancels the tool and stays on the image; the next Escape performs the previous navigation behavior. 319 tests pass; gates green.
- Reasoning: The second defect was invisible in the report but implied by it — fixing only the key handler would have left a release-after-cancel committing crops; the viewer-level regression pins that path explicitly.
- Notes: "No image selected" appeared because the image was opened via CLI (empty library), making the fall-through obvious; with a populated library the bug would have read as "Esc closed my image".

## 2026-06-10 16:50 — lightroom-param-parity review + close-out

- Action: Ran the multi-CLI review on the v0.2.0 diff, synthesized to `docs/threads/done/lightroom-param-parity/2026-06-10/1/REVIEW.md`, fixed all findings, moved the thread to done.
- Reviewer comments, by provider and theme:
  - Codex (gpt-5.5 xhigh, live-tree): 1 HIGH — neutral temperature/tint preview/export mismatch (GPU applies `temperature_tint_matrix(0,0)` ≈ but ≠ identity, CPU skips the stage; pre-existing, surfaced by the parity focus; fixed with an exact-identity short-circuit + red-first regression that reproduced Codex's measured matrix to 7 decimals); 1 MEDIUM — doc claims contradicting the implemented tone-zone constants (fixed in DESIGN.md and two rustdocs). Confirmed the new scalar mappings and live WGSL constants are correct.
  - Claude (fable-5[1m] max, ran the suite itself): no high issues; verified CPU/GPU parity formula-by-formula including summation order and small constants, verified zero missed call sites via crate-wide greps, verified the persistence-compatibility claim against the cache header format; 3 LOW — near-vacuous tint test tolerance (tightened 0.01→1e-6), the stale ±2 EV rustdoc, garbled changelog old-range enumeration (all fixed) — plus a readout observation (`{:.1}` can't show the 0.01 exposure step) fixed via a new tested `slider_value_label` helper.
  - Gemini (3.1-pro plan mode): clean approval on all five focus areas.
- Result: All findings fixed; 317 tests pass; all four gates plus release build green; iteration 1 converged (the only substantive finding was pre-existing, and all three reviewers independently confirmed the new math).
- Reasoning: The verify-before-acting rule mattered here — Codex's HIGH contradicted Claude's "parity verified clean," and a red-first test settled it in Codex's favor (the matrix at neutral measured exactly the 0.99867 blue scale Codex computed).
- Notes: The neutral-identity fix means default-settings renders change by ≤1 u8 LSB versus prior builds (GPU preview only; CPU exports were already correct).

## 2026-06-10 15:40 — Lightroom-convention slider tuning (v0.2.0)

- Action: Aligned all 12 editing sliders with Lightroom's UI conventions per `docs/threads/current/lightroom-param-parity/DESIGN.md`. Ranges: Exposure ±5 EV, everything else ±100. New shared mapping layer in `edit.rs` (`contrast_amount`, `tone_zone_amount`, `vibrance_amount`, `saturation_amount`, `clarity_amount`, `dehaze_amount`, `temperature_kelvin`, `tint_yd_shift`) is the single slider→math conversion used by both `apply_all` (CPU) and a new testable `ScaledAmounts` packing helper in `viewer.rs` (GPU), making preview/export parity structural. Endpoint semantics: Saturation −100 = exact grayscale, +100 = 2× chroma; Temperature ±100 keeps the validated 3200K–9800K span; contrast/clarity/dehaze extremes land on the April-validated strength envelope. Tone zones retuned after visual review: σ tightened √2→1 and per-band reach set to 1.5 EV (total clamp stays ±2 EV) so full Highlights/Shadows moves stay targeted.
- Reviewer comments: deferred to the multi-CLI review running next on this thread.
- Result: 315 tests pass (12 new mapping/parity/extreme tests + 3 new tone-zone isolation/reach/smoothness tests + the render harness); all four gates plus release build green. Rendered 18-case before/after grid from `test.jpg` reviewed visually with metrics (`tmp/param-tuning/`): contrast ±100 strong-but-usable, Saturation −100 measured chroma 0.0000, Highlights −100 now reads as recovery (subject intact, mean luma 0.44 vs the 0.36 global-darkening of the wide-σ version), classic +25/−30/+30/+25 recipe balanced.
- Reasoning: Lightroom's math is proprietary (April lesson), so parity is defined as its observable UI ranges + endpoint semantics + the validated strength envelope spread across those ranges; the σ/reach retune was driven by the render review, where the leak was invisible to every unit test and obvious in one image.
- Notes: TDD throughout (mapping tests red-first, isolation test red at σ=√2 with measured ratio 1.67). Stored EditState values reinterpret on the new scales — acceptable because baked local edits persist pixels, not operations; flagged as breaking in the changelog. The render harness stays env-gated (`PHOTO_RENDER_TUNING_GRID=1`) so CI is hermetic.

## 2026-06-10 14:10 — CI flake fix: same-size rewrite vs mtime granularity (commit H, v0.1.6)

- Action: CI run 27305008375 failed on `decode::tests::cached_full_image_redocodes_when_source_file_changes` (left: 1, right: 2 — the rewrite was never detected) while the same suite passed locally all day. Root cause: the test rewrites `<svg>A</svg>` → `<svg>B</svg>` (same byte length), so invalidation depends entirely on the mtime changing, and the persisted cache's metadata fast path is designed to treat same-size+same-mtime as unchanged; the v0.1.4 optimized-dependency speedup made the CI runner fast enough to land both writes inside one Windows file-time tick. Fix: a `rewrite_with_distinct_mtime` test helper that rewrites and verifies the mtime advanced (bounded retry with 15ms sleeps), used at the rewrite site, plus a regression test for the helper itself. Audited the other same-size rewrite sites: the fingerprint test at decode.rs compares content hashes directly (mtime-independent) and the session cache always content-verifies after its metadata check, so no other test carries the assumption.
- Reviewer comments: n/a — CI-caught regression fixed under the same thread; the iteration-1 REVIEW.md disposition stands.
- Result: All four gates green; 299 tests pass (was 298; the helper regression adds one).
- Reasoning: The production fast path is a documented, intentional tradeoff (lessons 2026-04-20), so the fix belongs in the test, which must assert the contract "redecode when the change is observable through metadata," not "redecode on any write."
- Notes: This is the exact class of latent assumption that only surfaces when something else gets faster — recorded in lessons.md with anchors.

## 2026-06-10 13:30 — Multi-CLI review + close-out (commit G)

- Action: Ran the mandated multi-CLI review on the cumulative overhaul diff (`1480f57..HEAD`, Cargo.lock excluded; src/app/tests.rs additionally excluded for Gemini). Synthesized findings into `docs/threads/done/project-health-overhaul/2026-06-10/1/REVIEW.md`, fixed all four findings, updated `docs/devlog/summary.md` (compacted to the new modular reality), added the cargo-fix multi-target lesson with its evidence-anchor table, and moved the thread to `docs/threads/done/`.
- Reviewer comments, by provider and theme:
  - Codex (gpt-5.5 xhigh, live-tree): no code-level correctness/security/performance issues; 1 MEDIUM — the summary linked the thread at `done/` while it still lived in `current/` (fixed by landing the move and the summary in the same commit).
  - Claude (fable-5[1m] max, Read/Grep/git): verified the split is a true pure move at token level (identical literal/item/const inventories, 130 moved tests, 298 total at both ends); 3 LOW — CI gates missing `--locked` (fixed), `rustup show` relying on removed auto-install behavior (fixed with explicit `rustup toolchain install`), and comment residue from the scripted move (misattached doc comment + orphaned banners; fixed).
  - Gemini (3.1-pro plan mode): clean approval on all five focus areas after repeated 429 capacity retries; diff-text-only signal.
- Result: Iteration 1 converged — zero substantive code defects; all findings fixed and verified against the live tree before applying; gates re-run green after the fixes.
- Reasoning: Per AGENTS.md the consolidated post-hoc review on the same branch satisfies the review mandate for the multi-commit task; convergence is measured by substantive finding count, which was zero.
- Notes: Watch the first CI run with `--locked` + explicit toolchain install on the pushed HEAD; Gemini capacity flakiness is recorded in REVIEW.md as an operational note.

## 2026-06-10 12:20 — cargo audit baseline + jxl-grid advisory fix (commit F, v0.1.5)

- Action: Installed cargo-audit and ran the project's first audit (601 crate dependencies). Findings: 1 vulnerability — transitive `jxl-grid` 0.6.1, RUSTSEC-2026-0151 (out-of-bounds writes via integer overflow on 32-bit platforms), also yanked; fixed with targeted `cargo update -p jxl-grid` → 0.6.2. Remaining 5 informational warnings, all transitive with no compatible fixes from our direct deps: `instant` 0.1.13 unmaintained (RUSTSEC-2024-0384), `paste` 1.0.15 unmaintained (RUSTSEC-2024-0436), `lru` 0.12.5 unsound IterMut (RUSTSEC-2026-0002), `rand` 0.8.5/0.9.2 unsound with custom logger (RUSTSEC-2026-0097).
- Reviewer comments: deferred to the consolidated end-of-task multi-CLI review (commit G).
- Result: cargo audit: 0 vulnerabilities, 5 allowed informational warnings; 298 tests pass after the bump.
- Reasoning: AGENTS.md mandates audit on dependency changes but no baseline had ever been recorded; the warnings are acceptable because they sit behind iced/rawler and carry no actionable upgrade path until those parents move.
- Notes: Re-check the 5 warnings whenever iced or rawler is upgraded (the planned iced upgrade that would also unify the dual image-crate versions).

## 2026-06-10 11:45 — 7x faster test suite via optimized dev dependencies (commit E, v0.1.4)

- Action: Added `[profile.dev.package."*"] opt-level = 2` to Cargo.toml after isolating the slow-suite cause: `decode_raw_image_applies_orientation_metadata` alone took 99.9s solo because rawler's `develop_intermediate` is pathologically slow in unoptimized builds even on a 24x12 synthetic DNG. With dependencies optimized (photo itself stays opt-level 0), that test drops to 11.8s and the full 298-test suite drops from ~105s to ~14.4s.
- Reviewer comments: deferred to the consolidated end-of-task multi-CLI review (commit G).
- Result: All four gates green; suite 7x faster locally and in CI.
- Reasoning: Test fixtures were already tiny, so the cost was per-call fixed overhead inside the dependency — a profile override is the standard zero-behavior-change fix and beats `#[ignore]` (which would silently drop RAW coverage) or fixture surgery (fixtures were not the bottleneck).
- Notes: Trade-off: cold builds compile dependencies with optimization (one-time cost, cached by CI's rust-cache); debugging into dependency internals loses fidelity, which we don't do.

## 2026-06-10 11:10 — Split the main.rs monolith (commit D, v0.1.3)

- Action: Pure-move refactor of the 9,006-line `src/main.rs` into focused modules in two scripted passes with compile checkpoints. Pass 1 extracted leaf subsystems: `theme.rs` (palette + styles), `widgets.rs` (rotation buttons, thumbnail slots, grid math, menu items), `detail_load.rs`, `session_cache.rs` (fingerprint + in-session cache), `local_edits.rs` (baked persistence, 1,043 lines), `loading.rs`, `library.rs`, `repo.rs`. Pass 2 split `App` into `app/mod.rs` (state/Message/lifecycle/accessors), `app/update.rs` (message loop + orchestration), `app/view.rs` (view composition), and moved the whole test module to `app/tests.rs`. `main.rs` is now a 31-line entry point. Moved items use `pub(crate)`; cross-module struct fields made `pub(crate)` compiler-driven; macro ambiguity (`column!`/`row!` through glob imports) resolved with direct imports at use sites.
- Reviewer comments: deferred to the consolidated end-of-task multi-CLI review (commit G).
- Result: All four gates green plus a clean release build; 298/298 tests pass at every checkpoint (after pass 1, after pass 2, and at commit). ARCHITECTURE.md Component Map/Boundaries, README project structure, and the drift log all updated.
- Reasoning: Every prior feature paid a 9k-line review-and-context tax in main.rs, and the project's own no-file->500-LOC rule was unenforceable with an 18x violation at the core. Extraction order (leaves first, then the App split) kept each compile-fix cycle small.
- Notes: Accepted residuals tracked as follow-ups: `app/update.rs` (1,540), `local_edits.rs` (1,043), `app/view.rs` (806) still exceed 500 LOC and need behavior-aware splits (e.g. carving the `update` match by domain); `app/tests.rs` (4,078) should redistribute tests to home modules with a shared `test_support`; `repo.rs` and `decode.rs` still hold duplicate repo-root discovery; pre-existing oversize `decode.rs`/`edit.rs`/`viewer.rs` unchanged. One lesson candidate: `cargo fix` removed imports used only by the test cfg — re-check `--all-targets` after any `cargo fix` run.

## 2026-06-10 09:40 — CI + toolchain pin + lockfile (commit C, v0.1.2)

- Action: Added `.github/workflows/ci.yml` running the four gates on windows-latest for every push/PR, pinned the toolchain to 1.94.0 via `rust-toolchain.toml` (clippy + rustfmt components), removed `Cargo.lock` from `.gitignore` and committed the lockfile.
- Reviewer comments: deferred to the consolidated end-of-task multi-CLI review (commit G).
- Result: Gates are now machine-enforced instead of agent-discipline-enforced; local build verified the pin resolves (compiled v0.1.1→v0.1.2 under the pinned toolchain).
- Reasoning: The fmt/clippy drift fixed in v0.1.1 existed precisely because nothing re-ran the gates as the toolchain moved. The gitignored lockfile also contradicted the AGENTS.md dependency protocol, which assumes `Cargo.lock` is committed — for a binary crate the lockfile belongs in git.
- Notes: CI run on the real runner verified after push (this task pushes after this commit so the workflow can prove itself).

## 2026-06-10 09:25 — Restore quality gates: fmt + clippy on rustc 1.94 (commit B, v0.1.1)

- Action: Applied `cargo fmt` (24 stale diffs across decode.rs/edit.rs/main.rs) and fixed all 26 clippy `-D warnings` errors: 6× `flat_map(|rgb| rgb)` → `flatten()`, 4× `repeat().take()` → `repeat_n()`, 6× field-reassign-with-default → struct literals with `..Default::default()`, 2× `assert_eq!(_, bool)` → `assert!`, 2× type_complexity → shared `TestHookCell` alias, 1× needless return → cfg-block tail expressions in `decode_image_cache_dir`, 1× too-many-arguments → grouped the test DNG builder's dims into `(w, h)` tuples (6 call sites updated), 2× useless `.min(255)` after a u8 cast removed.
- Reviewer comments: deferred to the consolidated end-of-task multi-CLI review (commit G).
- Result: All four gates green again on rustc 1.94.0; 298 tests pass. Every fix is in test code except the cache-dir early-return cleanup, which is behavior-identical.
- Reasoning: The toolchain moved since the last code commit (April) and the gates are only enforced at agent commit time, so fmt/clippy drift accumulated silently — CI in the next commit closes that hole.
- Notes: `std::iter::repeat_n` requires Rust ≥ 1.82; the toolchain pin landing in commit C makes that floor explicit.

## 2026-06-10 09:05 — Project health overhaul: docs scaffolding (commit A)

- Action: Started the project-health-overhaul objective (plan at `docs/threads/current/project-health-overhaul/PLAN.md`). Created `docs/changelog.md` with a retroactive 0.1.0 baseline, added a decisions.md row recording the repo-local runtime-cache tradeoff and its distribution flip condition, rewrote `docs/reviews/README.md` as a redirect to the threads convention, added a grandfather note to `lessons.md` for pre-anchor-mandate entries, and opened this devlog file (previous active file ended 2026-04-24; six-week gap is a natural boundary).
- Reviewer comments: deferred — one consolidated multi-CLI review of the cumulative overhaul diff runs as the final step (commit G in the plan), per the post-hoc-on-same-branch provision in AGENTS.md.
- Result: AGENTS.md-mandated artifacts now exist; process docs match reality.
- Reasoning: The 2026-06-10 assessment found the changelog, version-bump trail, and threads convention had never been instantiated — the methodology referenced artifacts that didn't exist. Versioning interpretation adopted: version bumps + changelog entries attach to shipped code/build changes; pure-docs commits get neither.
- Notes: Today's gate check found `cargo fmt --check` failing (24 diffs) and `cargo clippy -D warnings` failing (26 mechanical lints) on rustc 1.94.0 while all 298 tests pass — fixed next in commit B.

## 2026-06-11 09:15 — Library reflects committed edits without the source media (v0.2.2)

**Action**: Root-caused and fixed the user report "library view should have the thumbnails of the updated and edited images after I exit details view and upon startup". Diagnosis against the live machine (library = 17 Sony ARWs on SD card `E:`, one valid bake in `local-edits/` written same morning) found four defects: (F1) baked local edits unreachable when the source is offline — `normalized_source_path_key` falls back to the raw path when `fs::canonicalize` fails so the cache filename hash changes (`bbfe78d0…` vs `8b04dd73…`), and every loader returned `Ok(None)` when `source_file_state` was unreadable; (F2) commits made during the staged RAW preview window never baked if the user navigated away before the full develop landed (the `ImageLoaded` catch-up was gated on `is_current_request`), silently losing the edit at session end; (F5) `ThumbnailLoaded` re-rendered session edit state on top of whatever base the loader returned — when that base was the bake, edits applied twice; (F6) `load_library()` filtered entries on `Path::exists()`, so an unplugged card emptied the library at startup and the next `save_library` made it permanent.

**Fix**: `candidate_source_path_keys` + `resolve_local_edit_cache_file` (canonical → parent-canonical+name → verbatim `\\?\` guess (Windows) → raw, first existing wins; removal purges all candidates); header validation takes `Option` source state and skips only the metadata equality when the source is truly absent (`probe_source_file_state` tri-state: NotFound fails open to the bake, unreadable-but-present fails closed) while a present-but-mismatched source stays fail-closed; repair-thumbnail writes resolve the key and skip persisting offline; `load_library_thumbnail_base_image` returns `LoadedThumbnailBase` with provenance and the app applies session state only to `Original` bases (`refresh_library_thumbnail_for_path` leaves baked bases to the persist pipeline; `set_library_thumbnail_for_path` stores base+provenance; a removed bake triggers an async original-thumbnail reload); `owed_local_edit_bakes: HashMap<request_id, OwedLocalEditBake{path, lens}>` registered in `start_load` when superseding an in-flight staged load with bake-worthy state, fulfilled by the stale `ImageLoaded` branch (which seeds `base_image_sources` exactly like the current-request arm, reads state at fulfillment, and skips identity re-bakes), with stale `ImagePreviewLoaded` completions chaining the full decode so stage-`Loading` supersedes still fulfill; `parse_library_content` keeps offline paths.

**Code reviewer comments**: Codex unreachable (usage limit until 2026-07-10) — proceeded with two reviewers per AGENTS.md. Gemini 3.1 Pro (plan mode, structural): iteration 1 approved with nits (key-hash case sensitivity, Windows-only `\\?\` guess); iteration 2 approved, no findings. Claude (codebase-grounded, load-bearing): iteration 1 caught one HIGH — owed-bake fulfillment skipped the `base_image_sources` seed, so an A→B→A reopen resolved the fresh bake as base and re-applied the held session state (compounding bake corruption) — plus two MEDIUMs (owed bakes registered during stage `Loading` could never be fulfilled and leaked; unedited baked images queued full-resolution identity re-bakes on rapid navigation); all fixed with regressions. Claude's session limit expired right after that pass (resets 13:10 PT), so iteration 2 converged on Gemini plus per-finding regression tests and a line-level self-audit per the AGENTS.md unreachable-CLI clause. Full findings and dispositions: `docs/threads/done/library-offline-edits/2026-06-11/1/REVIEW.md` and `2/REVIEW.md`.

**Result**: 338 tests green (16 new across two review iterations), clippy/fmt/build/release-build clean. Library shows edited thumbnails offline at startup (verified end-to-end against the real unplugged-card library and bake), Detail opens baked edits offline, offline entries persist, preview-window commits bake after navigation.

**Reasoning**: The bake is the only durable record of the user's work; tethering its readability (and the cache *key derivation*) to source reachability inverted the durability contract exactly when it mattered. Library membership likewise is user intent, not a filesystem cache.

**Notes**: Found mid-session that `%LOCALAPPDATA%/photo/decoded-cache` exists from a pre-repo-cache build — harmless leftover, untouched. The EXIF-pending navigate-away window (full image already loaded, lens metadata still loading) is narrower and now logs a warning instead of fixing structurally; the commit bakes on next open of that image. Diagnostic tests against the real user library were used during investigation and deleted before commit.

## 2026-06-11 17:45 — Save-as-copy exports move into the repo (v0.2.3)

**Action**: User request: "the edited files should not be saved in the original path where the photo is provided. It should be saved in this repo path, and the path should be gitignored." `edit::edited_save_path` built the export name with `Path::with_file_name`, so Ctrl+S wrote `<stem>_edited.<ext>` next to the source — onto the camera SD card for this user's library.

**Fix**: `edited_save_path` resolves `repo::photo_repo_root()` and targets `<repo>/edited/<stem>_edited.<ext>` (RAW still exports as PNG); `save_edited_image` creates the directory on demand; `/edited/` gitignored; legacy beside-the-source placement kept only as the no-repo-root fallback (packaged exe). Overwrite-on-re-export retained (matches prior same-name semantics; cross-source stem collisions accepted for a solo user and documented in the thread DESIGN.md). `repo.rs` gained `with_test_photo_repo_root_absent` for the fallback test, and all pre-existing save tests were wrapped in repo-root overrides — without that, the suite would have started writing exports into the real repo.

**Code reviewer comments**: reviewed as part of the library-offline-edits iteration-2 full-diff review. Gemini 3.1 Pro: exports redirection "works as intended", fallback keeps portable mode functional, no findings. Claude was session-limited for that iteration (documented there); a post-hoc Claude pass over both commits is queued now that the limit has reset.

**Result**: 338 tests green (3 new: repo-rooted path expectations, no-repo-root fallback, directory creation), clippy/fmt/build/release green. Saves now land in `edited/` and the status bar shows the destination.

**Reasoning**: Source folders may be read-only-ish removable media and are never the app's to write; the repo is the durable home for app output, consistent with `local-edits/` and `decoded-cache/`.

**Notes**: Thread: `docs/threads/done/repo-local-exports/`. The RAW-default-tone investigation ("duller than Lightroom") is diagnosed and designed in `docs/threads/current/raw-default-tone/DESIGN.md` — rawler's default develop applies no tone curve while Lightroom applies baseline exposure + contrast curve + camera profile; implementation deferred for user look-approval since it changes every developed RAW's appearance.

## 2026-06-11 18:25 — Post-hoc verification pass closes a pre-existing bake-corruption window (v0.2.4)

**Action**: The belt-and-braces Claude pass over the landed v0.2.2/v0.2.3 commits confirmed all iteration-1 fixes hold (owed map leak-free, rescue chain cannot double-decode, exports clean) and surfaced one pre-existing HIGH the v0.2.2 work did not cover: editing on top of an existing bake corrupts on any in-session disk reload. Chain: commit S over baked base B1 → persist writes B2=render(B1,S) while the session keeps state S → browse 4+ photos (session cache holds 4 entries → eviction) → reopen loads B2 from disk → Detail renders S on top (visible double-apply) and the unconditional post-load persist bakes render(B2,S) — compounding per revisit, exports included. The v0.2.2 fix covered the sibling case (Original-base decodes) only.

**Fix**: persists report the generation they wrote (`CompletedLocalEditBake`); the app tracks `last_baked_generations` per path; reloads whose `bake_generation` matches are recognized as having absorbed the session state — the state resets (look unchanged; undo for that photo drops, consistent with the session-only undo contract) and nothing re-persists. Gemini's verification round then caught the sibling race: a reload landing while a NEWER persist is still in flight sees the older generation on disk, and a naive absorbed-match would wipe the newer unsaved state — fixed via `newest_pending_persist_for_path`: while a persist races, the reload adopts that request's own base image (`LocalEditPersistRequest.base_dimensions` added so the substitute carries the right logical dims), keeping the newer state rendered on the exact base it was authored on and skipping the re-enqueue when the racing request already covers the visible state; owed fulfillment defers entirely to any racing persist (it is strictly newer and carries the correct base). Rider (post-hoc MEDIUM): the mainline persist-request path now reuses `owed_bake_has_nothing_new`, ending full-resolution identity re-bakes (and offline warning spam) when reopening never-edited baked images, while preserving the reset-removes-bake contract.

**Code reviewer comments**: post-hoc Claude pass synthesized in `docs/threads/done/library-offline-edits/2026-06-11/3/REVIEW.md` (HIGH 3-H1 + MEDIUM 3-M2 fixed here; LOW 3-L3 task-opacity residual accepted; LOW 3-L4 fulfillment-guard evidence inaccuracy corrected with a real test). Fix-verification round in `4/REVIEW.md`: Gemini's racing-reload finding was real and fixed via pending-base substitution, but both of its suggested remedies (invalidate the generation record on queue, or gate the predicate on pending persists without substituting the base) would have re-introduced the original corruption, and its claim that the older-generation regression test "simulates an impossible state" missed read-vs-atomic-rename concurrency — reviewer suggestions verified against the codebase before adoption, per the AGENTS.md verify-reviewer-claims rule. Claude's iteration-4 pass then caught a real regression in the 3-M2 rider itself: undo-to-default on a baked base no longer persisted the revert (4-H1, with a redo-stack leg recreating the compounding corruption), commits made during the reload window were silently wiped (4-M2), and the owed racing guard deferred to OLDER persists (4-M3) — all fixed along the reviewer's prescribed directions (`loaded_base_generations` + per-path `CompletedBake` state records + always-replace reset + state-compared owed deferral with base adoption).

**Result**: 349 tests green (11 new), clippy/fmt/build/release green. Lessons.md gains the "audit the invariant, not the diff" entry with anchors. Iteration 5 (Claude + Gemini, `5/REVIEW.md`) verified all three remedies hold with no corruption paths and caught one last false-alarm defect — the lost-commits notice re-fired on every re-absorption of the same bake because the reset never normalized the recorded baked state — fixed red-first by normalizing `CompletedBake.state` to default after the reset, with the clean-absorption test extended to a silent second round. Gemini: "clean for release."

**Reasoning**: The bake advances with every persist while the session's parametric state stays anchored to the base it loaded; any disk reload mid-session crosses those two timelines. Making absorption observable (generation equality) is the smallest mechanism that keeps slider-state continuity in the common case and never re-applies absorbed edits; full parametric persistence (Lightroom sidecar model) remains the structural successor and is noted in decisions.md.

**Notes**: Undo loss on absorbed reload only occurs after eviction (4+ photos away) — the same point where slider continuity was already lost pre-fix, minus the corruption.
