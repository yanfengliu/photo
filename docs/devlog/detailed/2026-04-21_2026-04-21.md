# Detailed Devlog

## [2026-04-21 01:22 PDT] - Move the persisted decoded-image cache into the repo
**Action:** Moved the persisted RAW/SVG decoded-image cache to the visible repo-local `decoded-cache` directory in `src/decode.rs` and updated `.gitignore` to ignore `/decoded-cache`. Tightened repo-root discovery for the cache path, normalized equivalent source paths onto one cache key, cleaned up temp files on failed cache finalization, switched the on-disk cache contract to an explicit versioned header, restored a metadata fast path for unchanged cache hits, and kept same-size rewrite protection by hashing the source only when metadata changes but content identity still matters. Expanded regression coverage for repo-local public-path reuse, RAW reuse through `decode_image()`, same-size rewrite invalidation, periodic prune triggering, SVGZ cache scope, and invalid cache-schema fallback. Updated the summary and lessons, and because the date boundary rolled past midnight PDT, started this new detailed devlog file for 2026-04-21.
**Code reviewer comments:**
- Codex / Design: iterative review first flagged metadata-only persisted fingerprints and brittle source-text-derived contract invalidation. Addressed by normalizing source paths, adding source-content validation for changed-metadata cases, and replacing the implicit source-text contract with an explicit cache-contract version. A later design pass flagged full-file verification on every hit; addressed by restoring a metadata fast path so unchanged hits do not rehash the source file. Final remaining Codex design note was a non-blocking tradeoff: pruning still follows cache-file modified time, so retention tracks decode recency rather than true read recency.
- Gemini / Design: Gemini CLI attempts did not produce a design verdict for this task. One invocation failed because this CLI build does not support `--thinking`, and the follow-up `gemini-3-pro` request failed with `ModelNotFoundError` before review output was returned.
- Codex / Test coverage: iterative review flagged missing RAW public-path coverage, missing same-size rewrite coverage for the content-hash invalidation branch, missing periodic prune-trigger coverage, then later SVGZ scope and invalid-cache-schema reject-path gaps. Addressed with focused regressions in `src/decode.rs` for each case. A final post-fix Codex coverage verification was not rerun after the last SVGZ/schema additions because the review loop had already consumed substantial tool time; local coverage work was scoped directly to the exact reported branches.
- Gemini / Test coverage: no Gemini coverage verdict was returned because the CLI/model issues above blocked theme-specific review runs before they could complete.
- Codex / Correctness: no new dedicated Codex correctness pass was completed after this repo-local-path follow-up. Local validation kept the persisted cache fail-closed on corrupt headers, same-size rewrites with changed metadata, missing repo-root discovery, and temp-file cleanup failures.
- Gemini / Correctness: no Gemini correctness verdict was returned because the CLI/model issues above blocked theme-specific review runs before they could complete.
- Codex / Clean code / typing / efficiency / memory leaks: earlier review in this task flagged leaked temp files on failed finalization and duplicate cache keys for equivalent Windows path spellings. Addressed by cleaning up temp files on error paths and canonicalizing the cache key path before hashing. No later dedicated clean-code re-review was completed after the final coverage additions.
- Gemini / Clean code / typing / efficiency / memory leaks: no Gemini clean-code verdict was returned because the CLI/model issues above blocked theme-specific review runs before they could complete.
- Codex / Documentation: no dedicated final Codex documentation pass was completed after the repo-local-path follow-up. The summary, lessons, and this new dated detailed entry were updated to match the final repo-local cache behavior and the remaining pruning tradeoff.
- Gemini / Documentation: no Gemini documentation verdict was returned because the CLI/model issues above blocked theme-specific review runs before they could complete.
**Result:** Success - the persisted decoded-image cache now lives inside this repo at `decoded-cache`, cache files are ignored by git there, repeated RAW/SVG Detail loads can reuse that repo-local cache, same-size rewrites with changed metadata fail closed, and the public repo-local cache path is covered by regression tests for both SVG and RAW.
**Files changed:** `.gitignore`, `docs/devlog/summary.md`, `docs/learning/lessons.md`, `docs/devlog/detailed/2026-04-21_2026-04-21.md`, `src/decode.rs`
**Reasoning:** The user needed the persisted cache to be inspectable and co-located with the repo, so the path change had to be explicit and predictable rather than hidden under `%LOCALAPPDATA%`. Once that move was in place, the safest follow-up was to harden the repo-local cache contract just enough that it stays useful without silently reusing stale data or leaving behind failed temp artifacts, while leaning on the same-session in-memory cache for the truly immediate repeat-open experience.
**Notes:** Validation passed with `cargo test` (229 tests), `cargo clippy -- -D warnings`, and `cargo build --release`. Codex external review completed for design and coverage and drove the follow-up fixes above; the final remaining Codex design note is that prune ordering still tracks cache-write recency rather than true access recency. Gemini review attempts were blocked by CLI/model availability issues rather than by a known code defect. Existing unrelated local edits in `AGENTS.md`, `src/main.rs`, `src/viewer.rs`, and `docs/devlog/detailed/2026-04-20_2026-04-20.md` were left untouched.

## [2026-04-21 09:49 PDT] - Make same-image Library reopens reuse the current full image immediately
**Action:** Added a regression in `src/main.rs` for the real user flow where a fully loaded image is opened from Library, the user returns to Library, and then reopens the same item. Updated `App::start_load()` to reuse the already displayed full image immediately when the requested path matches the current image, the image is already backed by a validated full-image session-cache entry, and the source metadata still matches the cached fingerprint. The fast path still fails closed for ordinary rewrites but no longer forces the library-return reopen through the expensive session-cache revalidation pass. Updated the summary and lessons to record the new reopen behavior and the metadata-guard rule.
**Code reviewer comments:**
- Codex / Design: not run yet for this follow-up.
- Gemini / Design: not run yet for this follow-up.
- Codex / Test coverage: not run yet for this follow-up.
- Gemini / Test coverage: not run yet for this follow-up.
- Codex / Correctness: not run yet for this follow-up.
- Gemini / Correctness: not run yet for this follow-up.
- Codex / Clean code / typing / efficiency / memory leaks: not run yet for this follow-up.
- Gemini / Clean code / typing / efficiency / memory leaks: not run yet for this follow-up.
- Codex / Documentation: not run yet for this follow-up.
- Gemini / Documentation: not run yet for this follow-up.
**Result:** Success - reopening the same image from Library in the same session now reuses the full image that is already on screen instead of blanking and paying the heavy cache-hit validation cost again, while ordinary source rewrites still fall back to a fresh load.
**Files changed:** `src/main.rs`, `docs/devlog/summary.md`, `docs/learning/lessons.md`, `docs/devlog/detailed/2026-04-21_2026-04-21.md`
**Reasoning:** The expensive part of the old reopen path was not the decode itself but the synchronous cache-hit revalidation, which still re-opened and re-fingerprinted the source file. When the user simply leaves Detail and comes straight back to the same library item, we already have the correct full image in memory, so the better UX is to reuse it immediately and reserve the heavier validation path for other cache hits.
**Notes:** Validation passed with `cargo test` (230 tests), `cargo clippy -- -D warnings`, and `cargo build --release`. This follow-up intentionally leaves the broader session-cache hit path unchanged; only the "currently displayed image reopened from Library" path gets the new immediate reuse shortcut.

## [2026-04-21 09:31 PDT] - Warm persisted decoded caches when files are imported
**Action:** Moved RAW/SVG persisted-cache priming earlier in the user flow by adding import-time background warming. `src/main.rs` now routes folder/file imports through a shared import helper, enqueues supported paths for cache warming, and drains that queue one file at a time through `ImportCacheWarmCompleted` so bulk imports do not fan out many full decodes at once. `src/decode.rs` now exposes `warm_persisted_decoded_cache()` and `path_uses_persisted_decoded_cache()` so the app can reuse the existing repo-local persisted-cache contract instead of inventing a second warming path. Added regressions for queue startup, queue advancement on success, queue advancement on failure, appending later imports while a warm is already in flight, and the new decode helper’s supported/unsupported behavior. Updated the summary and lessons.
**Code reviewer comments:**
- Codex / Design: a read-only design review attempt via `codex exec` timed out before returning a verdict, so there is no completed Codex design result for this follow-up.
- Gemini / Design: Gemini’s design review said the serial `VecDeque` queue and the `decode.rs` facade stayed lean, respected module boundaries, and scaled well by avoiding a fan-out of full decodes during bulk imports. The same run later hit `429 RESOURCE_EXHAUSTED` capacity errors after the initial verdict.
- Codex / Test coverage: first coverage review flagged two missing regressions: queue advancement after a failed warm and a second import batch arriving while a warm was already in flight. Both were added in `src/main.rs`, and a second Codex coverage verification confirmed those queue regressions are now covered. Remaining Codex note: `warm_persisted_decoded_cache()` still lacks a direct unit test for a decode failure path in `src/decode.rs`.
- Gemini / Test coverage: no Gemini coverage verdict was completed for this follow-up.
- Codex / Correctness: not run for this follow-up.
- Gemini / Correctness: not run for this follow-up.
- Codex / Clean code / typing / efficiency / memory leaks: not run for this follow-up.
- Gemini / Clean code / typing / efficiency / memory leaks: not run for this follow-up.
- Codex / Documentation: not run for this follow-up.
- Gemini / Documentation: not run for this follow-up.
**Result:** Success - importing RAW and SVG files now starts warming the repo-local persisted decoded cache immediately in the background, so the first Detail open no longer has to be the moment that creates those cache files.
**Files changed:** `src/main.rs`, `src/decode.rs`, `docs/devlog/summary.md`, `docs/learning/lessons.md`, `docs/devlog/detailed/2026-04-21_2026-04-21.md`
**Reasoning:** The user’s complaint was about *when* the expensive work happened, not whether the cache existed at all. The right fix was to shift that work to import time while keeping the UI responsive, which meant adding a narrow background queue in the app layer and reusing the existing persisted-cache logic in the decode layer instead of duplicating it.
**Notes:** Validation passed with `cargo test` (236 tests), `cargo clippy -- -D warnings`, and `cargo build --release`. External review status for this follow-up is partial rather than complete: Gemini returned a positive design verdict before capacity errors, and Codex completed only the coverage pass plus a verification rerun after the queue-regression follow-up.

## [2026-04-21 09:44 PDT] - Keep a small hot set of recently viewed full Detail images in memory
**Action:** Added regressions in `src/main.rs` for the user flow where two detail-viewed images are loaded in the same session and the earlier image is reopened without falling back to a fresh load, plus a focused cache-level regression for keeping two recent entries hot even when their combined size exceeds the byte budget. Updated `SessionFullImageCache` to retain a two-entry recent-history floor beyond the byte cap while still enforcing the entry cap, preserving the existing stale-file validation path and LRU eviction for older entries. Updated the summary and lessons to record the new hot-history rule.
**Code reviewer comments:**
- Codex / Design: not run in this desktop session.
- Gemini / Design: not run in this desktop session.
- Codex / Test coverage: not run in this desktop session.
- Gemini / Test coverage: not run in this desktop session.
- Codex / Correctness: not run in this desktop session.
- Gemini / Correctness: not run in this desktop session.
- Codex / Clean code / typing / efficiency / memory leaks: not run in this desktop session.
- Gemini / Clean code / typing / efficiency / memory leaks: not run in this desktop session.
- Codex / Documentation: not run in this desktop session.
- Gemini / Documentation: not run in this desktop session.
**Result:** Success - same-session reopening now keeps at least the two most recently viewed full Detail images in the in-memory cache even when large images would otherwise overflow the byte budget, so hopping back to a recently viewed image does not immediately lose the memory-cache benefit.
**Files changed:** `src/main.rs`, `docs/devlog/summary.md`, `docs/learning/lessons.md`, `docs/devlog/detailed/2026-04-21_2026-04-21.md`
**Reasoning:** The existing in-memory cache already avoided re-decoding, but its byte cap could still evict the previous large RAW as soon as the user viewed a second one. A small recent-history floor matches the user-facing expectation better: keep the last couple of detail-viewed images hot, then let the ordinary LRU budget handle everything older than that.
**Notes:** `cargo test` first failed in the new regressions, confirming the old eviction behavior. A subsequent full `cargo test` pass after the cache change completed cleanly with 238 passing tests. One intermediate full-suite rerun also hit an unrelated transient failure in `decode::tests::write_decoded_cache_triggers_periodic_pruning`, but the next full rerun passed without code changes.

## [2026-04-21 09:57 PDT] - Persist non-destructive per-image edits to the local app copy
**Action:** Added local per-image edit persistence so committed `EditState` values now round-trip through `%LOCALAPPDATA%/photo/edits.json` instead of living only in memory for the current run. `src/edit.rs` now serializes `QuarterTurns`, `CropRect`, and `EditState`, restores saved state as a committed `UndoHistory` baseline, and normalizes saved crop/rotation data on load. `src/main.rs` now loads saved edit histories during app startup, persists committed edit changes after slider commits, reset/toggle/rotate actions, crop commits, and undo/redo, and prunes default or missing-file entries when writing the local edits file. Added regressions for rotation/crop round-tripping, pruning default and missing-file entries, and normalizing malformed saved crop bounds. Updated the architecture doc and drift log because the app now has a durable local edit-state store alongside the existing library and collection files, and refreshed the summary and lessons.
**Code reviewer comments:**
- Codex / Design: not run in this desktop session.
- Gemini / Design: not run in this desktop session.
- Codex / Test coverage: not run in this desktop session.
- Gemini / Test coverage: not run in this desktop session.
- Codex / Correctness: not run in this desktop session.
- Gemini / Correctness: not run in this desktop session.
- Codex / Clean code / typing / efficiency / memory leaks: not run in this desktop session.
- Gemini / Clean code / typing / efficiency / memory leaks: not run in this desktop session.
- Codex / Documentation: not run in this desktop session.
- Gemini / Documentation: not run in this desktop session.
**Result:** Success - non-destructive Detail edits now survive reopen and restart through the local app copy, including rotation and crop, while save-as-copy remains a separate export path for baked image files.
**Files changed:** `src/edit.rs`, `src/main.rs`, `docs/architecture/ARCHITECTURE.md`, `docs/architecture/drift-log.md`, `docs/devlog/summary.md`, `docs/learning/lessons.md`, `docs/devlog/detailed/2026-04-21_2026-04-21.md`
**Reasoning:** The user asked for the local copy to remember image edits, not just for the app to export a new baked file on demand. The narrowest architecture-safe change was to persist only committed per-image edit state in the app layer, restore it as the baseline when an image is reopened, and keep `edit.rs` responsible for the edit model plus normalization rather than pushing this logic into decode or viewer code.
**Notes:** Validation passed with `cargo test` (241 tests), `cargo clippy -- -D warnings`, and `cargo build --release`. One intermediate parallel validation run hit the existing intermittent `decode::tests::write_decoded_cache_triggers_periodic_pruning` failure again, but a clean standalone `cargo test` rerun passed without code changes. Existing unrelated local edits in `AGENTS.md`, `docs/devlog/detailed/2026-04-20_2026-04-20.md`, and `src/viewer.rs` were left untouched.

## [2026-04-21 18:51 PDT] - Move persisted local edit state into the repo
**Action:** Moved the per-image local edit-state file from `%LOCALAPPDATA%/photo/edits.json` to the visible repo-local `local-edits.json` file. `src/main.rs` now resolves the edit-state path from the discovered photo repo root instead of profile-scoped app storage, exposes a focused `local_edits_file_path_for_repo_root()` helper for the contract, and keeps the rest of the load/save round-trip behavior unchanged. Added focused regressions for repo-local path selection and updated `.gitignore` to ignore `/local-edits.json`. Refreshed the summary, lessons, architecture doc, drift log, and this detailed devlog entry to match the new storage location.
**Code reviewer comments:**
- Codex / Design: not run in this desktop session.
- Gemini / Design: not run in this desktop session.
- Codex / Test coverage: not run in this desktop session.
- Gemini / Test coverage: not run in this desktop session.
- Codex / Correctness: not run in this desktop session.
- Gemini / Correctness: not run in this desktop session.
- Codex / Clean code / typing / efficiency / memory leaks: not run in this desktop session.
- Gemini / Clean code / typing / efficiency / memory leaks: not run in this desktop session.
- Codex / Documentation: not run in this desktop session.
- Gemini / Documentation: not run in this desktop session.
**Result:** Success - persisted per-image edit state still round-trips and restores rotation/crop as before, but the file now lives in the repo root as `local-edits.json` so it is inspectable alongside `decoded-cache`.
**Files changed:** `.gitignore`, `src/main.rs`, `docs/architecture/ARCHITECTURE.md`, `docs/architecture/drift-log.md`, `docs/devlog/summary.md`, `docs/learning/lessons.md`, `docs/devlog/detailed/2026-04-21_2026-04-21.md`
**Reasoning:** The previous path satisfied persistence but not inspectability. Since the user explicitly wanted that metadata in the repo, the smallest safe fix was to change only the path-resolution contract, keep the storage format and semantics the same, and add a focused regression so future refactors do not quietly drift back to profile-local storage.
**Notes:** Validation passed with `cargo test` (243 tests), `cargo clippy -- -D warnings`, and `cargo build --release`. Existing unrelated local edits in `AGENTS.md`, `docs/devlog/detailed/2026-04-20_2026-04-20.md`, and `src/viewer.rs` were left untouched.
