# Detailed Devlog

## [2026-04-21 01:22 PDT] - Move the persisted decoded-image cache into the repo
**Action:** Moved the persisted RAW/SVG decoded-image cache to the visible repo-local `decoded-cache` directory in `src/decode.rs` and updated `.gitignore` to ignore `/decoded-cache`. Tightened repo-root discovery for the cache path, normalized equivalent source paths onto one cache key, cleaned up temp files on failed cache finalization, switched the on-disk cache contract to an explicit versioned header, restored a metadata fast path for unchanged cache hits, and kept same-size rewrite protection by hashing the source only when metadata changes but content identity still matters. Expanded regression coverage for repo-local public-path reuse, RAW reuse through `decode_image()`, same-size rewrite invalidation, periodic prune triggering, SVGZ cache scope, and invalid cache-schema fallback. Updated the summary and lessons, and because the date boundary rolled past midnight PDT, started this new detailed devlog file for 2026-04-21.
**Code reviewer comments:**
- Codex / Design: iterative review first flagged metadata-only persisted fingerprints and brittle source-text-derived contract invalidation. Addressed by normalizing source paths, adding source-content validation for changed-metadata cases, and replacing the implicit source-text contract with an explicit cache-contract version. A later design pass flagged full-file verification on every hit; addressed by restoring a metadata fast path so unchanged hits do not rehash the source file. Final remaining Codex design note was a non-blocking tradeoff: pruning still follows cache-file modified time, so retention tracks decode recency rather than true read recency.
- Gemini / Design: Gemini CLI attempts did not produce a design verdict for this task. One invocation failed because this CLI build does not support `--thinking`, and the follow-up `gemini-3-pro` request failed with `ModelNotFoundError` before review output was returned.
- Codex / Test coverage: iterative review flagged missing RAW public-path coverage, missing same-size rewrite coverage for the content-hash invalidation branch, missing periodic prune-trigger coverage, then later SVGZ scope and invalid-cache-schema reject-path gaps. Addressed with focused regressions in `src/decode.rs` for each case. A final post-fix Codex coverage verification was not rerun after the last SVGZ/schema additions because the review loop had already consumed substantial tool time; local coverage work was scoped directly to the exact reported branches.
- Gemini / Test coverage: no Gemini coverage verdict was returned because the CLI/model issues above blocked theme-specific review runs before they could complete.
- Codex / Correctness: no new dedicated Codex correctness pass was completed after this repo-local-path follow-up. Local validation kept the persisted cache fail-closed on corrupt headers, same-size rewrites with changed metadata, missing repo-root discovery, and temp-file cleanup failures.
- Gemini / Correctness: no Gemini correctness verdict was returned because the CLI/model issues above blocked theme-specific review runs before they could complete.
- Codex / Clean code / typing / efficiency / memory leaks: earlier review in this task flagged leaked temp files on failed finalization and duplicate cache keys for equivalent Windows path spellings. Addressed by cleaning up temp files on error paths and canonicalizing the cache key path before hashing. No later dedicated clean-code re-review was completed after the final coverage additions.
- Gemini / Clean code / typing / efficiency / memory leaks: no Gemini clean-code verdict was returned because the CLI/model issues above blocked theme-specific review runs before they could complete.
- Codex / Documentation: no dedicated final Codex documentation pass was completed after the repo-local-path follow-up. The summary, lessons, and this new dated detailed entry were updated to match the final repo-local cache behavior and the remaining pruning tradeoff.
- Gemini / Documentation: no Gemini documentation verdict was returned because the CLI/model issues above blocked theme-specific review runs before they could complete.
**Result:** Success - the persisted decoded-image cache now lives inside this repo at `decoded-cache`, cache files are ignored by git there, repeated RAW/SVG Detail loads can reuse that repo-local cache, same-size rewrites with changed metadata fail closed, and the public repo-local cache path is covered by regression tests for both SVG and RAW.
**Files changed:** `.gitignore`, `docs/devlog/summary.md`, `docs/learning/lessons.md`, `docs/devlog/detailed/2026-04-21_2026-04-21.md`, `src/decode.rs`
**Reasoning:** The user needed the persisted cache to be inspectable and co-located with the repo, so the path change had to be explicit and predictable rather than hidden under `%LOCALAPPDATA%`. Once that move was in place, the safest follow-up was to harden the repo-local cache contract just enough that it stays useful without silently reusing stale data or leaving behind failed temp artifacts, while leaning on the same-session in-memory cache for the truly immediate repeat-open experience.
**Notes:** Validation passed with `cargo test` (229 tests), `cargo clippy -- -D warnings`, and `cargo build --release`. Codex external review completed for design and coverage and drove the follow-up fixes above; the final remaining Codex design note is that prune ordering still tracks cache-write recency rather than true access recency. Gemini review attempts were blocked by CLI/model availability issues rather than by a known code defect. Existing unrelated local edits in `AGENTS.md`, `src/main.rs`, `src/viewer.rs`, and `docs/devlog/detailed/2026-04-20_2026-04-20.md` were left untouched.

## [2026-04-21 09:49 PDT] - Make same-image Library reopens reuse the current full image immediately
**Action:** Added a regression in `src/main.rs` for the real user flow where a fully loaded image is opened from Library, the user returns to Library, and then reopens the same item. Updated `App::start_load()` to reuse the already displayed full image immediately when the requested path matches the current image, the image is already backed by a validated full-image session-cache entry, and the source metadata still matches the cached fingerprint. The fast path still fails closed for ordinary rewrites but no longer forces the library-return reopen through the expensive session-cache revalidation pass. Updated the summary and lessons to record the new reopen behavior and the metadata-guard rule.
**Code reviewer comments:**
- Codex / Design: not run yet for this follow-up.
- Gemini / Design: not run yet for this follow-up.
- Codex / Test coverage: not run yet for this follow-up.
- Gemini / Test coverage: not run yet for this follow-up.
- Codex / Correctness: not run yet for this follow-up.
- Gemini / Correctness: not run yet for this follow-up.
- Codex / Clean code / typing / efficiency / memory leaks: not run yet for this follow-up.
- Gemini / Clean code / typing / efficiency / memory leaks: not run yet for this follow-up.
- Codex / Documentation: not run yet for this follow-up.
- Gemini / Documentation: not run yet for this follow-up.
**Result:** Success - reopening the same image from Library in the same session now reuses the full image that is already on screen instead of blanking and paying the heavy cache-hit validation cost again, while ordinary source rewrites still fall back to a fresh load.
**Files changed:** `src/main.rs`, `docs/devlog/summary.md`, `docs/learning/lessons.md`, `docs/devlog/detailed/2026-04-21_2026-04-21.md`
**Reasoning:** The expensive part of the old reopen path was not the decode itself but the synchronous cache-hit revalidation, which still re-opened and re-fingerprinted the source file. When the user simply leaves Detail and comes straight back to the same library item, we already have the correct full image in memory, so the better UX is to reuse it immediately and reserve the heavier validation path for other cache hits.
**Notes:** Validation passed with `cargo test` (230 tests), `cargo clippy -- -D warnings`, and `cargo build --release`. This follow-up intentionally leaves the broader session-cache hit path unchanged; only the "currently displayed image reopened from Library" path gets the new immediate reuse shortcut.

## [2026-04-21 09:31 PDT] - Warm persisted decoded caches when files are imported
**Action:** Moved RAW/SVG persisted-cache priming earlier in the user flow by adding import-time background warming. `src/main.rs` now routes folder/file imports through a shared import helper, enqueues supported paths for cache warming, and drains that queue one file at a time through `ImportCacheWarmCompleted` so bulk imports do not fan out many full decodes at once. `src/decode.rs` now exposes `warm_persisted_decoded_cache()` and `path_uses_persisted_decoded_cache()` so the app can reuse the existing repo-local persisted-cache contract instead of inventing a second warming path. Added regressions for queue startup, queue advancement on success, queue advancement on failure, appending later imports while a warm is already in flight, and the new decode helper’s supported/unsupported behavior. Updated the summary and lessons.
**Code reviewer comments:**
- Codex / Design: a read-only design review attempt via `codex exec` timed out before returning a verdict, so there is no completed Codex design result for this follow-up.
- Gemini / Design: Gemini’s design review said the serial `VecDeque` queue and the `decode.rs` facade stayed lean, respected module boundaries, and scaled well by avoiding a fan-out of full decodes during bulk imports. The same run later hit `429 RESOURCE_EXHAUSTED` capacity errors after the initial verdict.
- Codex / Test coverage: first coverage review flagged two missing regressions: queue advancement after a failed warm and a second import batch arriving while a warm was already in flight. Both were added in `src/main.rs`, and a second Codex coverage verification confirmed those queue regressions are now covered. Remaining Codex note: `warm_persisted_decoded_cache()` still lacks a direct unit test for a decode failure path in `src/decode.rs`.
- Gemini / Test coverage: no Gemini coverage verdict was completed for this follow-up.
- Codex / Correctness: not run for this follow-up.
- Gemini / Correctness: not run for this follow-up.
- Codex / Clean code / typing / efficiency / memory leaks: not run for this follow-up.
- Gemini / Clean code / typing / efficiency / memory leaks: not run for this follow-up.
- Codex / Documentation: not run for this follow-up.
- Gemini / Documentation: not run for this follow-up.
**Result:** Success - importing RAW and SVG files now starts warming the repo-local persisted decoded cache immediately in the background, so the first Detail open no longer has to be the moment that creates those cache files.
**Files changed:** `src/main.rs`, `src/decode.rs`, `docs/devlog/summary.md`, `docs/learning/lessons.md`, `docs/devlog/detailed/2026-04-21_2026-04-21.md`
**Reasoning:** The user’s complaint was about *when* the expensive work happened, not whether the cache existed at all. The right fix was to shift that work to import time while keeping the UI responsive, which meant adding a narrow background queue in the app layer and reusing the existing persisted-cache logic in the decode layer instead of duplicating it.
**Notes:** Validation passed with `cargo test` (236 tests), `cargo clippy -- -D warnings`, and `cargo build --release`. External review status for this follow-up is partial rather than complete: Gemini returned a positive design verdict before capacity errors, and Codex completed only the coverage pass plus a verification rerun after the queue-regression follow-up.

## [2026-04-21 09:44 PDT] - Keep a small hot set of recently viewed full Detail images in memory
**Action:** Added regressions in `src/main.rs` for the user flow where two detail-viewed images are loaded in the same session and the earlier image is reopened without falling back to a fresh load, plus a focused cache-level regression for keeping two recent entries hot even when their combined size exceeds the byte budget. Updated `SessionFullImageCache` to retain a two-entry recent-history floor beyond the byte cap while still enforcing the entry cap, preserving the existing stale-file validation path and LRU eviction for older entries. Updated the summary and lessons to record the new hot-history rule.
**Code reviewer comments:**
- Codex / Design: not run in this desktop session.
- Gemini / Design: not run in this desktop session.
- Codex / Test coverage: not run in this desktop session.
- Gemini / Test coverage: not run in this desktop session.
- Codex / Correctness: not run in this desktop session.
- Gemini / Correctness: not run in this desktop session.
- Codex / Clean code / typing / efficiency / memory leaks: not run in this desktop session.
- Gemini / Clean code / typing / efficiency / memory leaks: not run in this desktop session.
- Codex / Documentation: not run in this desktop session.
- Gemini / Documentation: not run in this desktop session.
**Result:** Success - same-session reopening now keeps at least the two most recently viewed full Detail images in the in-memory cache even when large images would otherwise overflow the byte budget, so hopping back to a recently viewed image does not immediately lose the memory-cache benefit.
**Files changed:** `src/main.rs`, `docs/devlog/summary.md`, `docs/learning/lessons.md`, `docs/devlog/detailed/2026-04-21_2026-04-21.md`
**Reasoning:** The existing in-memory cache already avoided re-decoding, but its byte cap could still evict the previous large RAW as soon as the user viewed a second one. A small recent-history floor matches the user-facing expectation better: keep the last couple of detail-viewed images hot, then let the ordinary LRU budget handle everything older than that.
**Notes:** `cargo test` first failed in the new regressions, confirming the old eviction behavior. A subsequent full `cargo test` pass after the cache change completed cleanly with 238 passing tests. One intermediate full-suite rerun also hit an unrelated transient failure in `decode::tests::write_decoded_cache_triggers_periodic_pruning`, but the next full rerun passed without code changes.

## [2026-04-21 09:57 PDT] - Persist non-destructive per-image edits to the local app copy
**Action:** Added local per-image edit persistence so committed `EditState` values now round-trip through `%LOCALAPPDATA%/photo/edits.json` instead of living only in memory for the current run. `src/edit.rs` now serializes `QuarterTurns`, `CropRect`, and `EditState`, restores saved state as a committed `UndoHistory` baseline, and normalizes saved crop/rotation data on load. `src/main.rs` now loads saved edit histories during app startup, persists committed edit changes after slider commits, reset/toggle/rotate actions, crop commits, and undo/redo, and prunes default or missing-file entries when writing the local edits file. Added regressions for rotation/crop round-tripping, pruning default and missing-file entries, and normalizing malformed saved crop bounds. Updated the architecture doc and drift log because the app now has a durable local edit-state store alongside the existing library and collection files, and refreshed the summary and lessons.
**Code reviewer comments:**
- Codex / Design: not run in this desktop session.
- Gemini / Design: not run in this desktop session.
- Codex / Test coverage: not run in this desktop session.
- Gemini / Test coverage: not run in this desktop session.
- Codex / Correctness: not run in this desktop session.
- Gemini / Correctness: not run in this desktop session.
- Codex / Clean code / typing / efficiency / memory leaks: not run in this desktop session.
- Gemini / Clean code / typing / efficiency / memory leaks: not run in this desktop session.
- Codex / Documentation: not run in this desktop session.
- Gemini / Documentation: not run in this desktop session.
**Result:** Success - non-destructive Detail edits now survive reopen and restart through the local app copy, including rotation and crop, while save-as-copy remains a separate export path for baked image files.
**Files changed:** `src/edit.rs`, `src/main.rs`, `docs/architecture/ARCHITECTURE.md`, `docs/architecture/drift-log.md`, `docs/devlog/summary.md`, `docs/learning/lessons.md`, `docs/devlog/detailed/2026-04-21_2026-04-21.md`
**Reasoning:** The user asked for the local copy to remember image edits, not just for the app to export a new baked file on demand. The narrowest architecture-safe change was to persist only committed per-image edit state in the app layer, restore it as the baseline when an image is reopened, and keep `edit.rs` responsible for the edit model plus normalization rather than pushing this logic into decode or viewer code.
**Notes:** Validation passed with `cargo test` (241 tests), `cargo clippy -- -D warnings`, and `cargo build --release`. One intermediate parallel validation run hit the existing intermittent `decode::tests::write_decoded_cache_triggers_periodic_pruning` failure again, but a clean standalone `cargo test` rerun passed without code changes. Existing unrelated local edits in `AGENTS.md`, `docs/devlog/detailed/2026-04-20_2026-04-20.md`, and `src/viewer.rs` were left untouched.

## [2026-04-21 18:51 PDT] - Move persisted local edit state into the repo
**Action:** Moved the per-image local edit-state file from `%LOCALAPPDATA%/photo/edits.json` to the visible repo-local `local-edits.json` file. `src/main.rs` now resolves the edit-state path from the discovered photo repo root instead of profile-scoped app storage, exposes a focused `local_edits_file_path_for_repo_root()` helper for the contract, and keeps the rest of the load/save round-trip behavior unchanged. Added focused regressions for repo-local path selection and updated `.gitignore` to ignore `/local-edits.json`. Refreshed the summary, lessons, architecture doc, drift log, and this detailed devlog entry to match the new storage location.
**Code reviewer comments:**
- Codex / Design: not run in this desktop session.
- Gemini / Design: not run in this desktop session.
- Codex / Test coverage: not run in this desktop session.
- Gemini / Test coverage: not run in this desktop session.
- Codex / Correctness: not run in this desktop session.
- Gemini / Correctness: not run in this desktop session.
- Codex / Clean code / typing / efficiency / memory leaks: not run in this desktop session.
- Gemini / Clean code / typing / efficiency / memory leaks: not run in this desktop session.
- Codex / Documentation: not run in this desktop session.
- Gemini / Documentation: not run in this desktop session.
**Result:** Success - persisted per-image edit state still round-trips and restores rotation/crop as before, but the file now lives in the repo root as `local-edits.json` so it is inspectable alongside `decoded-cache`.
**Files changed:** `.gitignore`, `src/main.rs`, `docs/architecture/ARCHITECTURE.md`, `docs/architecture/drift-log.md`, `docs/devlog/summary.md`, `docs/learning/lessons.md`, `docs/devlog/detailed/2026-04-21_2026-04-21.md`
**Reasoning:** The previous path satisfied persistence but not inspectability. Since the user explicitly wanted that metadata in the repo, the smallest safe fix was to change only the path-resolution contract, keep the storage format and semantics the same, and add a focused regression so future refactors do not quietly drift back to profile-local storage.
**Notes:** Validation passed with `cargo test` (243 tests), `cargo clippy -- -D warnings`, and `cargo build --release`. Existing unrelated local edits in `AGENTS.md`, `docs/devlog/detailed/2026-04-20_2026-04-20.md`, and `src/viewer.rs` were left untouched.

## [2026-04-21 20:36 PDT] - Persist baked local edited images instead of edit-operation history
**Action:** Replaced the repo-local `local-edits.json` edit-operation store with a repo-local `local-edits/` cache of baked local image copies. `src/edit.rs` now exposes `render_edited_image()` so both save/export and local persistence share the same pixel math. `src/main.rs` now keeps undo/redo strictly session-scoped in memory, writes committed edits as baked full-size and thumbnail-size local copies, prefers those baked local copies when reopening images after restart, and refreshes Library thumbnails immediately after committed rotation, exposure, crop, and other visible edits so Library matches the Detail image the user just left. Added regressions for repo-local cache path selection, baked-image round-tripping without restored history, persisted thumbnail preference, and immediate Library-thumbnail refresh after committed rotation and exposure changes. Updated `.gitignore`, the summary, lessons, architecture doc, and drift log to match the new contract.
**Code reviewer comments:**
- Codex / Design: pending.
- Gemini / Design: pending.
- Codex / Test coverage: pending.
- Gemini / Test coverage: pending.
- Codex / Correctness: pending.
- Gemini / Correctness: pending.
- Codex / Clean code / typing / efficiency / memory leaks: pending.
- Gemini / Clean code / typing / efficiency / memory leaks: pending.
- Codex / Documentation: pending.
- Gemini / Documentation: pending.
**Result:** Success - the app now persists the edited image data the user expects to see later, not the edit-operation history. Reopening after restart uses the baked repo-local local copy, Library thumbnails track committed edits immediately, and undo/redo is intentionally limited to the current session.
**Files changed:** `.gitignore`, `docs/architecture/ARCHITECTURE.md`, `docs/architecture/drift-log.md`, `docs/devlog/summary.md`, `docs/learning/lessons.md`, `docs/devlog/detailed/2026-04-21_2026-04-21.md`, `src/edit.rs`, `src/main.rs`
**Reasoning:** The user clarified that persistence is about reopening the edited photo, not carrying an undo stack across launches. That makes baked local image data the simpler and more correct contract: it preserves what the user saw, avoids restoring stale history semantics, and gives Library a natural source for showing the edited version on reopen.
**Notes:** Code changes and regressions are in place. The repo also still has an unrelated intermittent flake in `decode::tests::write_decoded_cache_triggers_periodic_pruning`; clean reruns have passed on this tree.

## [2026-04-22 00:01 PDT] - Harden baked local-edit persistence after design review
**Action:** Addressed design-review follow-ups in the baked local-edit persistence path. `src/main.rs` now refreshes the Library thumbnail from the same captured render snapshot that gets persisted, instead of recomputing that thumbnail later from mutable live state. The persisted full-size and thumbnail-size local copies now share a generation id so Library only trusts a thumbnail that matches its full sibling; if the thumbnail is stale or missing, it derives a fresh thumbnail from the persisted full copy instead. The baked local-edit path is also now covered by a regression that proves a source-file rewrite invalidates the repo-local local copy. While hardening the tests around this flow, I also fixed the test helper image factories so they allocate pixel buffers that actually match their declared dimensions. Updated the summary, lessons, and architecture doc to reflect the final persistence contract.
**Code reviewer comments:**
- Codex / Design: first read-only design review flagged two real issues: non-atomic full/thumbnail baked copies and auto-lens baking before EXIF settled. Both were fixed in code. A second Codex design pass then flagged a mismatch between the persisted snapshot and the immediate Library refresh path; that was addressed by driving the Library refresh from the same captured `LocalEditPersistRequest` snapshot. The same second pass also raised a source-validation concern; the code already invalidated baked local edits against stored source metadata, and I added a focused regression to lock that contract down explicitly.
- Gemini / Design: not completed in this desktop session.
- Codex / Test coverage: not completed in this desktop session.
- Gemini / Test coverage: not completed in this desktop session.
- Codex / Correctness: not completed in this desktop session.
- Gemini / Correctness: not completed in this desktop session.
- Codex / Clean code / typing / efficiency / memory leaks: not completed in this desktop session.
- Gemini / Clean code / typing / efficiency / memory leaks: not completed in this desktop session.
- Codex / Documentation: not completed in this desktop session.
- Gemini / Documentation: not completed in this desktop session.
**Result:** Success - repo-local baked local-edit persistence now behaves like the user-visible contract: the Library thumbnail and the persisted local copy come from the same committed snapshot, partial full/thumbnail writes fail closed through generation matching, auto-lens-corrected renders wait for EXIF before they are baked, and source rewrites invalidate stale local copies.
**Files changed:** `.gitignore`, `docs/architecture/ARCHITECTURE.md`, `docs/architecture/drift-log.md`, `docs/devlog/summary.md`, `docs/learning/lessons.md`, `docs/devlog/detailed/2026-04-21_2026-04-21.md`, `src/edit.rs`, `src/main.rs`
**Reasoning:** The original user request was about persisting the edited photo itself, not the edit history, so the remaining design work was all about making that baked-photo contract trustworthy. That meant removing opportunities for the Library preview and the persisted file to drift apart, failing closed on partial writes, and proving that stale local copies do not survive a source rewrite.
**Notes:** Validation passed with a clean rerun of `cargo test` (248 tests), `cargo clippy -- -D warnings`, and `cargo build --release`. One earlier full-suite run still hit the existing intermittent `decode::tests::write_decoded_cache_triggers_periodic_pruning` flake before the clean rerun. Existing unrelated local edits in `AGENTS.md`, `docs/devlog/detailed/2026-04-20_2026-04-20.md`, `src/decode.rs`, and `src/viewer.rs` were left untouched.

## [2026-04-22 09:27 PDT] - Fix impossible Detail dimensions by separating logical size from decoded buffer size
**Action:** Added a `decode::source_dimensions()` entrypoint for raster, SVG, and RAW files so the app can query logical image size separately from the decoded upload buffer. `src/main.rs` now carries `current_image_source_dimensions` through Detail loads, uses those logical dimensions for status-bar rotation/crop math, refreshes missing values after successful original-source loads, and clears them when the current image goes away. Same-session full-image cache entries now record both their base source and logical dimensions so repeat opens can stay fast without re-reading source metadata, and base-source flips invalidate old cache hits instead of mixing original-source dimensions with baked local-edit pixels. Added regressions for source-dimension reporting across RAW/PNG/SVG, recovering missing logical dimensions after a successful original load, reusing cached logical dimensions on same-session hits, invalidating cache hits when the preferred base source changes, and clearing logical dimensions when the current image is dropped. Also added a debugging note and updated the summary and lessons.
**Code reviewer comments:**
- Codex / Design: pending final rerun; initial desktop CLI invocation needed command-shape correction before a review result could be captured.
- Gemini / Design: pending final rerun; initial desktop CLI invocation needed command-shape correction before a review result could be captured.
- Claude / Design: first pass flagged that logical dimensions were being loaded too early for same-session fast paths and that cached logical dimensions could drift when the preferred base source flipped. The fix now defers fresh reads until needed and stores the base source in session-cache entries so mismatched hits are invalidated.
- Codex / Test coverage: pending final rerun.
- Gemini / Test coverage: pending final rerun.
- Claude / Test coverage: first pass asked for broader source-dimension coverage and cache-hit regressions. Added PNG/SVG coverage for `source_dimensions()`, a successful-original-load recovery regression, a cache-hit regression, and a base-source invalidation regression.
- Codex / Correctness: pending final rerun.
- Gemini / Correctness: pending final rerun.
- Claude / Correctness: first pass highlighted that a transient source-dimension read failure could stick and that cached base-source drift could mix dimensions with the wrong pixels. Successful original loads now refresh missing logical dimensions, and cache hits are rejected when their base source no longer matches the preferred source.
- Codex / Clean code / typing / efficiency / memory leaks: pending final rerun.
- Gemini / Clean code / typing / efficiency / memory leaks: pending final rerun.
- Claude / Clean code / typing / efficiency / memory leaks: first pass asked for clearer `start_load()` dimension plumbing and warned about repeated metadata reads on same-session fast paths. The session cache now carries logical dimensions, and the `start_load()` fast paths reuse those cached values instead of re-reading source metadata.
- Codex / Documentation: pending final rerun.
- Gemini / Documentation: pending final rerun.
- Claude / Documentation: first pass flagged the missing detailed devlog entry for this task and the stale `END_DATE` in the active detailed-devlog filename. Renamed the active file to `2026-04-21_2026-04-22.md`, added this entry, and kept the debugging note plus summary/lessons in sync.
**Result:** Success - the Detail status bar now reports logical image dimensions instead of impossible decoded-buffer dimensions, repeat opens reuse cached logical dimensions in the same session, and cache hits are invalidated when the app would otherwise mix original-source and baked-local-edit states.
**Files changed:** `src/decode.rs`, `src/main.rs`, `docs/debugging/2026-04-22-detail-dimensions.md`, `docs/devlog/summary.md`, `docs/learning/lessons.md`, `docs/devlog/detailed/2026-04-21_2026-04-22.md`
**Reasoning:** The UI bug was not that the source image metadata was wrong; it was that the status bar was reading the wrong layer of truth. Separating logical base-image size from the decoded GPU/upload buffer fixes the visible bug directly, and threading that logical size through the same-session cache keeps the fix from regressing perceived performance on repeat opens.
**Notes:** An earlier inspection found persisted RAW decoded-cache entries at oversized dimensions such as `16384 x 10923`, so the decoded-cache contract version was bumped to force a one-time rebuild of old persisted entries rather than trusting suspect cache content across sessions. `cargo test` now passes with 255 tests after one earlier clean rerun around the repo's existing intermittent `decode::tests::write_decoded_cache_triggers_periodic_pruning` flake.

## [2026-04-22 11:01 PDT] - Move logical-dimension recovery off the UI path and harden the fast path
**Action:** Followed up on the Detail-dimension fix to remove the remaining synchronous original-source dimension probe from `start_load()`. `src/main.rs` now threads logical dimensions back on `LoadedFullImage`, restores them from the same-session cache without re-probing, and rejects the already-displayed-image fast path when its cached base source no longer matches the preferred source. `src/decode.rs` now uses poison-safe reads for the test override mutexes, and the periodic-pruning regression once again drives `write_decoded_cache()` end-to-end instead of only the extracted helper. Added a new regression proving the displayed-image fast path does not reuse stale pixels when the preferred base source flips. Updated the debugging note, summary, and lessons to reflect the worker-thread dimension recovery and the remaining cache-schema tradeoff.
**Code reviewer comments:**
- Codex / Design: a first follow-up review reported one real issue and one performance tradeoff. The real issue was that the already-displayed-image fast path only checked the session cache fingerprint, not whether the cached entry still matched the preferred base source; that is now fixed and covered by a regression. The remaining design note is that persisted decoded-cache hits still reopen the original RAW/SVG source off the UI thread to recover logical dimensions because the on-disk cache schema does not yet store those logical dimensions. A later Codex verification rerun timed out in this desktop session.
- Gemini / Design: the follow-up review agreed that the UI-thread regression was gone, and it left one remaining design note: persisted decoded-cache hits still pay a background source-dimension read because the on-disk cache schema only stores decoded-buffer dimensions today. A later Gemini verification rerun crashed with an out-of-memory error before it returned a final answer.
- Claude / Design: review attempts in this desktop session timed out before returning findings.
- Codex / Test coverage: the first follow-up review also asked for coverage around the displayed-image fast path after a base-source flip; added `displayed_full_image_fast_path_does_not_reuse_a_stale_base_source`. A later Codex verification rerun timed out.
- Gemini / Test coverage: flagged that the pruning regression had become helper-specific. The test now drives `write_decoded_cache()` end-to-end again and still passes in the full suite. A later Gemini verification rerun crashed before confirming the updated diff.
- Claude / Test coverage: review attempts in this desktop session timed out before returning findings.
- Codex / Correctness: flagged the stale displayed-image fast path described above. The fast path now refuses a cached/displayed image whose base source does not match the preferred one. A later Codex verification rerun timed out.
- Gemini / Correctness: no new correctness bug beyond the test/lock issues below was returned in the successful follow-up pass.
- Claude / Correctness: review attempts in this desktop session timed out before returning findings.
- Codex / Clean code / typing / efficiency / memory leaks: noted that original RAW/SVG loads still inspect source dimensions separately from pixel decode on the worker thread, which is a performance tradeoff rather than a UI-thread correctness issue. The code now keeps that work off the UI path, and the remaining tradeoff is documented in the debugging note.
- Gemini / Clean code / typing / efficiency / memory leaks: flagged that the test override reads in `decode.rs` still used poison-panicking `.unwrap()` calls. Those reads now recover from poisoned mutexes the same way the override writers already did.
- Claude / Clean code / typing / efficiency / memory leaks: review attempts in this desktop session timed out before returning findings.
- Codex / Documentation: no documentation issue was reported in the successful Codex follow-up pass, and the summary/lessons/debugging note were refreshed to match the final behavior. The later Codex verification rerun timed out.
- Gemini / Documentation: no documentation issue was reported in the successful Gemini follow-up pass; the later rerun crashed before a final confirmation.
- Claude / Documentation: review attempts in this desktop session timed out before returning findings.
**Result:** Success - Detail status text still reports the real logical image dimensions, RAW opens no longer synchronously probe source dimensions on the UI thread, repeat opens restore cached logical dimensions immediately in-session, and the already-displayed-image shortcut no longer risks reusing stale pixels after a base-source flip.
**Files changed:** `src/decode.rs`, `src/main.rs`, `docs/debugging/2026-04-22-detail-dimensions.md`, `docs/devlog/summary.md`, `docs/learning/lessons.md`, `docs/devlog/detailed/2026-04-21_2026-04-22.md`
**Reasoning:** The first pass fixed the user-visible lie, but it still paid for that truth on the UI path for original-source loads. The right follow-up was to keep the status bar honest while threading logical dimensions back from the background load that already owns the expensive file work, then close the fast-path edge cases the reviewers found so the cache shortcuts stay correct.
**Notes:** Validation passed with `cargo test` (259 tests), `cargo clippy -- -D warnings`, and `cargo build --release`. Existing unrelated local edits in `docs/devlog/detailed/2026-04-20_2026-04-20.md` and `src/viewer.rs` were left untouched. The only review note left open is a documented performance tradeoff: persisted decoded-cache hits still reopen the original RAW/SVG source off the UI thread to recover logical dimensions because the on-disk cache schema does not yet store them.

## [2026-04-22 11:57 PDT] - Preserve thumbnail aspect ratio in shared square slots
**Action:** Reworked the thumbnail aspect-ratio fix around `iced`'s built-in containment instead of manual size math. `src/main.rs` now routes Library, collection, and drag-preview thumbnails through a shared square-slot helper that calls `Image::content_fit(iced::ContentFit::Contain)` explicitly, keeps the production call sites non-generic through a thin wrapper, and backs the behavior with renderer-based regression tests that assert the rendered bounds for wide, tall, and square thumbnails. Updated the summary and lessons to match the final implementation.
**Code reviewer comments:**
- Codex / Design: final review found no design issues in the completed code; the only late Codex finding on the full diff was the missing detailed devlog entry, which this entry resolves.
- Codex / Test coverage: no final coverage finding on the completed code; the renderer-backed wide/tall/square thumbnail-slot regressions were accepted as sufficient for this change.
- Codex / Correctness: no final correctness findings.
- Codex / Clean code / typing / efficiency / memory leaks: no final clean-code or efficiency findings after the production surface was trimmed back to a single shared thumbnail-slot helper.
- Codex / Documentation: the last full-diff pass flagged only the missing detailed devlog entry for this task; summary and lessons were otherwise in sync.
- Gemini / Design: final review returned no findings. An earlier pass on the manual-sizing version raised optional concerns about latent fallback behavior; the final implementation avoids that path by leaning on explicit `ContentFit::Contain`.
- Gemini / Test coverage: final review returned no findings.
- Gemini / Correctness: final review returned no findings.
- Gemini / Clean code / typing / efficiency / memory leaks: final review returned no findings.
- Gemini / Documentation: final review returned no findings, although the CLI also emitted model-capacity retry noise after the response.
- Claude / Design: the first review on the manual-sizing version flagged that a debug-only fallback could silently reintroduce stretching in release and suggested using the built-in contain behavior instead. The final code now uses explicit `ContentFit::Contain` at the shared thumbnail-slot seam.
- Claude / Test coverage: the first review flagged that helper-only tests were too detached from the widget contract, and a later review on the simplified slot-helper version asked for tighter x/y assertions plus a square-image case. The final tests now assert centered bounds for wide and tall slots and cover the square case too.
- Claude / Correctness: the first review flagged the release-mode fallback risk described above. That manual fallback path was removed when the code switched to the explicit contain-based slot helper.
- Claude / Clean code / typing / efficiency / memory leaks: a later review on the simplified version asked to hide generic renderer plumbing from production call sites. The final code now keeps the generic helper for tests and a thin non-generic wrapper for app use. Final Claude verification attempts on the latest diff timed out in this desktop session.
- Claude / Documentation: full-diff verification attempts timed out before Claude returned a documentation result. Summary, lessons, and this detailed entry were updated in-tree.
**Result:** Success - thumbnails now explicitly preserve aspect ratio inside the shared square slot used by Library, collection, and drag-preview surfaces, and the rendered bounds are locked down for wide, tall, and square images.
**Files changed:** `src/main.rs`, `docs/devlog/summary.md`, `docs/learning/lessons.md`, `docs/devlog/detailed/2026-04-21_2026-04-22.md`
**Reasoning:** The user-visible contract lives at the UI slot where `iced` draws thumbnails, not only in the decoded thumbnail pixels. Making containment explicit at that shared slot keeps the intent local to the real rendering seam, while avoiding the extra production generics and release-only fallback behavior the reviewers pushed back on.
**Notes:** Validation passed with `cargo test` (262 tests), `cargo clippy -- -D warnings`, and `cargo build --release` after clean reruns. Two earlier full-suite runs hit the repo's existing intermittent `decode::tests::write_decoded_cache_triggers_periodic_pruning` flake before the clean rerun. Existing unrelated local edits in `docs/devlog/detailed/2026-04-20_2026-04-20.md` and `src/viewer.rs` were left untouched.
