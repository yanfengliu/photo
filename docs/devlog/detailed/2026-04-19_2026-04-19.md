# Detailed Devlog

## [2026-04-19 21:02 PDT] - Docs restructure to match AGENTS.md
**Action:** Restructured the docs tree to match the repository instructions. Created canonical directories for architecture, detailed devlogs, learning, debugging, and reviews; split the architecture notes into `docs/architecture/ARCHITECTURE.md`, `decisions.md`, and `drift-log.md`; moved the long-form devlog into `docs/devlog/detailed/2026-03-29_2026-04-04.md`; compacted the devlog summary into `docs/devlog/summary.md`; added `docs/README.md` as the canonical docs index; and left compatibility shims at the legacy flat paths with explicit edit boundaries.
**Code reviewer comments:**
- Codex / Design: asked for clearer temporary-shim language on the legacy flat files and for the README to stop hard-coding an exact test count. Addressed and verified.
- Gemini / Design: asked for stronger canonical-path clarity and a docs index inside `docs/`. Addressed with `docs/README.md`, explicit shim language, and verified with a follow-up pass.
- Codex / Test coverage: flagged missing validation and reviewer evidence in this devlog entry plus a missing docs-maintenance lesson. Addressed and verified.
- Gemini / Test coverage: flagged the same validation gap and requested an explicit record of the docs-layout migration in the architecture drift log. Addressed and verified.
- Codex / Correctness: flagged stale architecture metadata and an outdated README module list. Addressed and verified.
- Gemini / Correctness: no findings.
- Codex / Clean code: asked for stronger ownership boundaries between canonical docs and the legacy shims. Addressed with explicit canonical-edit guidance in `docs/README.md` and the shim files, then verified.
- Gemini / Clean code: no findings.
- Codex / Documentation: required the final reviewer breakdown in this entry and a thumbnail-strategy decision that matches the current RAW preview fast path. Addressed and verified.
- Gemini / Documentation: echoed the need to finalize the review and validation record in this detailed devlog entry. Addressed and verified.
**Result:** Success - the docs tree now matches AGENTS.md, the canonical edit targets are explicit, and the supporting docs cover architecture, devlogs, learning notes, debugging, and review handoffs.
**Files changed:** `README.md`, `docs/README.md`, `docs/ARCHITECTURE.md`, `docs/architecture/ARCHITECTURE.md`, `docs/architecture/decisions.md`, `docs/architecture/drift-log.md`, `docs/devlog/summary.md`, `docs/devlog/detailed/2026-03-29_2026-04-04.md`, `docs/devlog/detailed/2026-04-19_2026-04-19.md`, `docs/learning/lessons.md`, `docs/debugging/template.md`, `docs/reviews/README.md`
**Reasoning:** The repo instructions now rely on a directory-based docs layout, dated detailed logs, maintained lessons, and a debugging template. Keeping legacy flat files as explicit shims preserves older references while making the canonical edit locations unambiguous.
**Notes:** Validation passed: `cargo test` (119 tests), `cargo clippy -- -D warnings`, and `cargo build --release`. The flat files at the root of `docs/` remain temporary compatibility shims until older references are updated. Unrelated worktree changes in `Cargo.toml`, `src/decode.rs`, `src/main.rs`, and `src/nav.rs` were left untouched during this docs-restructure task.

## [2026-04-19 22:12 PDT] - Add RAW viewing support and RAW-safe save/export behavior
**Action:** Added RAW file discovery and dialog coverage in `src/nav.rs` and `src/main.rs`; implemented RAW decoding in `src/decode.rs` using `rawler` with library thumbnail-first embedded-image loading and raw-pixel-first detail decoding; changed `src/edit.rs` so edited RAW files save as PNG copies instead of unsupported RAW extensions; expanded README and architecture/devlog docs; and added synthetic DNG coverage for malformed RAWs plus thumbnail, preview, thumbnail-only, and full-image fallback behavior.
**Code reviewer comments:**
- Codex / Design: no blocking RAW-specific design issues remained after the final pass.
- Gemini / Design: raised existing large-module concerns in `src/main.rs` outside the RAW change; no blocking RAW-specific action was required.
- Codex / Test coverage: requested explicit coverage for malformed RAWs, extension-list drift, thumbnail-vs-preview selection, thumbnail-only detail fallback, and a dedicated full-image-only fixture. Addressed with new synthetic DNG helpers and focused decode tests.
- Gemini / Test coverage: asked for explicit success and failure coverage around RAW decode flows. Addressed with end-to-end synthetic DNG decode tests plus malformed RAW error coverage.
- Codex / Correctness: found two user-visible issues: edited RAW saves kept unsupported RAW extensions, and detail loads could settle for embedded images too early. Addressed by saving RAW edits as PNG copies and preferring raw-pixel development before embedded-image fallbacks.
- Gemini / Correctness: no reliable additional RAW-specific findings after the fixes.
- Codex / Clean code: flagged a `clippy` issue in `status_bar`; addressed by replacing `and_then(...Some(...))` with `map(...)` and re-running `cargo clippy -- -D warnings`.
- Gemini / Clean code: follow-up CLI reviews were intermittently limited by plan-mode/tooling behavior; no additional actionable RAW-specific finding was surfaced.
- Codex / Documentation: asked for README decode-module wording, canonical docs wording around thumbnail-first behavior, and a task-level devlog entry for the RAW work. Addressed.
- Gemini / Documentation: CLI review was intermittently blocked by plan-mode/SSL issues; no additional actionable RAW-specific documentation issue was surfaced beyond the centralized-extension lesson already recorded in `docs/learning/lessons.md`.
**Result:** Success - the app now recognizes and opens common camera RAW formats, library thumbnails use embedded RAW images when available, detail loads prefer developed RAW pixels before falling back to embedded images, and edited RAW files save successfully as PNG copies.
**Files changed:** `Cargo.toml`, `README.md`, `src/decode.rs`, `src/edit.rs`, `src/main.rs`, `src/nav.rs`, `docs/architecture/ARCHITECTURE.md`, `docs/architecture/decisions.md`, `docs/architecture/drift-log.md`, `docs/devlog/summary.md`, `docs/devlog/detailed/2026-04-19_2026-04-19.md`, `docs/learning/lessons.md`, `docs/superpowers/plans/2026-03-30-image-editing.md`
**Reasoning:** RAW support needs more than extension whitelisting: the viewer, library scan, file dialogs, decode pipeline, export path, and docs all need to agree on what “supported” means. The final shape keeps library browsing fast via embedded images while steering detail and export flows toward higher-quality RAW data whenever the decoder can provide it.
**Notes:** Validation passed: `cargo clippy -- -D warnings`, `cargo test` (126 tests), and `cargo build`. Final Codex verification still noted one medium residual test-design risk: synthetic DNG fixtures cannot perfectly prove every `rawler` embedded-image branch behaves the same way as every real camera RAW, even though the suite now distinguishes thumbnail, preview, thumbnail-only, and full-image-only fixtures. Gemini follow-up verification was attempted multiple times but was intermittently limited by plan-mode behavior and SSL/API errors.

## [2026-04-19 23:59 PDT] - Add clockwise and counterclockwise rotation in Detail view
**Action:** Added clockwise and counterclockwise image rotation to the Detail view. Extended `src/edit.rs` with a constrained `QuarterTurns` rotation type, wired rotate actions into `src/main.rs`, updated the GPU preview path and status-bar dimensions for rotated images, preserved actual-size zoom across rotate/undo/redo/reset flows, and rotated saved output with coverage for clockwise, counterclockwise, half-turn, and wraparound cases. Also tightened test coverage for app-level rotation wiring, preview UV direction math, rotated dimensions in status text, and actual-size behavior.
**Code reviewer comments:**
- Codex / Design: flagged split rotation semantics across save, layout, and shader paths. Addressed by centralizing rotated-dimension helpers and consolidating viewport-to-texture rotation mapping in the shader; verification found no remaining design findings.
- Gemini / Design: noted only a low-severity WGSL uniform-typing clarity concern and otherwise found the quarter-turn design lean and coherent. No code change was required, and the final design remained clear and bounded.
- Codex / Test coverage: flagged missing app-level rotation wiring tests, preview-direction coverage, save coverage beyond clockwise, and rotated status-text coverage. Addressed with new `src/main.rs`, `src/edit.rs`, and `src/viewer.rs` tests; verification found no remaining contract-coverage gaps beyond a non-blocking shader-execution residual risk.
- Gemini / Test coverage: initially surfaced status-text and preview/shader coverage concerns and the CLI briefly attempted unintended test-scaffolding edits, which were cleaned up immediately. Addressed with direct status-text, save-branch, app-flow, and preview-math tests; later review still noted UI-level integration as a residual risk but did not surface a new actionable contract bug.
- Codex / Correctness: in sequence found actual-size zoom mismatches for rotated previews, the panned actual-size case, stale canvas-size usage, and undo/redo/reset paths that could leave rotated previews at the wrong 1:1 scale. Addressed by making actual-size math rotation-aware across those state transitions and by caching live viewer bounds; final verification found no remaining correctness issues in the reviewed rotation paths.
- Gemini / Correctness: initially raised an unrelated lens-correction aspect-ratio note that predated this rotation work. Follow-up review found no actionable rotation correctness issues after the actual-size fixes.
- Codex / Clean code: flagged raw `u8` rotation typing, an unnecessary no-op save clone, and the viewer-to-app canvas-size backchannel. Addressed the first two with `edit::QuarterTurns` and a `Cow<[u8]>` fast path for unrotated saves; a low-severity residual concern remains about the render-path canvas-size cache as a maintainability tradeoff in the current iced shader architecture.
- Gemini / Clean code: no findings after the final pass beyond an acceptable residual note that full-resolution rotated saves still allocate an owned buffer when a real quarter-turn is requested.
- Codex / Documentation: no findings in the final pass; summary, detailed devlog, lessons, and touched shader comments matched the final rotation implementation and the `144`-test validation record.
- Gemini / Documentation: no findings in the final pass; docs and touched comments were accurate and current, with only low-severity residual notes about accepted UI/performance/maintainability tradeoffs already captured elsewhere.
**Result:** Success - users can now rotate Detail-view images clockwise or counterclockwise, previews and saved output stay aligned, status text reflects rotated dimensions, and actual-size zoom stays correct through rotate/undo/redo/reset workflows.
**Files changed:** `src/edit.rs`, `src/main.rs`, `src/viewer.rs`, `assets/shaders/image.wgsl`, `docs/devlog/summary.md`, `docs/devlog/detailed/2026-04-19_2026-04-19.md`, `docs/learning/lessons.md`
**Reasoning:** Rotation touches more than a transform flag. The viewer layout, shader sampling, save/export path, undoable edit model, actual-size zoom behavior, and user-visible metadata all need to agree on the same quarter-turn model or the app drifts into preview/output mismatches.
**Notes:** Working-tree cleanup was needed after one Gemini coverage review attempted to create an unintended lib/bin split and test file; those changes were removed before continuing. Code review still leaves one non-blocking clean-code residual: the live canvas-size cache is shared between the viewer render path and app state because the current iced shader integration does not provide a cleaner bounds-update message on every draw. Final validation passed with `cargo test` (144 tests), `cargo clippy -- -D warnings`, and `cargo build`.
