# Detailed Devlog

## [2026-04-20 00:56 PDT] - Stop fit-to-window Detail images from drifting on click
**Action:** Fixed the confusing Detail-view click-and-drift behavior by centralizing pannability in `src/viewer.rs`. Fit-to-window images now ignore click/move drag attempts, while zoomed or off-center images still emit pan deltas so users can drag them back into place. While tightening that interaction path, also made `zoom_at_cursor()` use the effective clamped zoom factor for offset math and cleared stale drag positions when the pointer leaves the canvas to avoid re-entry jumps. Added viewer-contract tests for pannability selection, cursor affordance, real `ImageCanvas::update()` drag paths, drag re-entry, small visible offsets, and clamp-aware zoom behavior. Updated the debugging note, lessons, and summary.
**Code reviewer comments:**
- Codex / Design: initially flagged duplicated pannability policy between `viewer.rs` and `main.rs`. Addressed by restoring single ownership to `viewer.rs`; verification found no remaining design findings beyond the pre-existing canvas-size-cache coupling outside this change.
- Gemini / Design: first called out a new off-center lock-out regression after the initial fix. Addressed by keeping off-center fit images pannable until they are visually back in place. Follow-up Gemini verification was attempted but later blocked by repeated Gemini API capacity failures.
- Codex / Test coverage: requested direct event-path coverage for drag start and move behavior, then requested the off-center move path to be asserted end-to-end too. Addressed with new `ImageCanvas::update()` tests; verification found no remaining coverage findings.
- Gemini / Test coverage: no findings after the event-path tests were added. Residual note only: `ButtonReleased` state-reset coverage would be optional extra confidence, not a blocking gap.
- Codex / Correctness: no findings after the pannability helper was moved to pixel-space tolerance and small-offset cases were covered.
- Gemini / Correctness: flagged two adjacent viewer issues during the review loop: clamp-blind zoom-at-cursor offset math and drag re-entry jumps after leaving the widget bounds. Addressed with effective-factor zoom math plus `last_pos` reset on bounds exit; Codex verification found no remaining correctness findings. Gemini follow-up verification timed out after the fixes.
- Codex / Clean code: no findings after the final helper simplification.
- Gemini / Clean code: suggested simplifying `image_can_pan()` to use direct offset/scale checks instead of rect-to-UV-to-pixel comparisons. Addressed with the simplified helper; remaining Gemini residuals were broader pre-existing interaction questions outside this task's scope.
- Codex / Documentation: no findings after the debugging note wording was updated to mark the unconditional drag-start behavior as pre-fix history instead of current behavior.
- Gemini / Documentation: no findings in the documentation pass. Residual note only: runtime drag feel and lost-pointer-event edge cases still benefit from manual smoke testing, which the docs already record as a remaining risk.
**Result:** Success - fit-to-window Detail images no longer drift on click, zoomed and off-center images still drag correctly, adjacent zoom/drag edge cases are covered, and the docs/debugging record now match the final behavior.
**Files changed:** `src/viewer.rs`, `docs/debugging/2026-04-20-detail-view-click-pan.md`, `docs/devlog/summary.md`, `docs/devlog/detailed/2026-04-20_2026-04-20.md`, `docs/learning/lessons.md`
**Reasoning:** The confusing visual effect came from the viewer treating every left press as a drag start, regardless of whether the image could actually move. The fix needed one pannability rule to back both the grab affordance and the event path, while still preserving recovery for slightly off-center views and nearby zoom/drag edge cases that otherwise turn into new motion bugs.
**Notes:** Final validation passed with `cargo test` (154 tests), `cargo clippy -- -D warnings`, and `cargo build`. Gemini follow-up verifications were intermittently blocked by timeouts and `429` model-capacity errors during this task, but the completed Gemini passes that did return were folded into the final fix and doc updates.

## [2026-04-20 02:03 PDT] - Add Detail-view crop support with freeform and fixed-aspect modes
**Action:** Added a Detail-view crop workflow that supports freeform and square aspect selection, stores crop state in `EditState`, renders crop preview/overlay in the viewer shader path, and applies crop after rotation during save/export. Follow-up review fixes tightened pixel-snapped preview/save parity, made status and actual-size zoom follow the crop that is currently visible, preserved actual-size zoom across crop commits, clears, reset, undo, and redo, and avoided building a full-frame post-processed buffer before trimming cropped exports. Added regression coverage for pixel-snapped crop parity, rotated crop commit-to-save behavior, rotated/cropped preview framing, crop-aware mouse actual-size behavior, and crop-change actual-size preservation.
**Code reviewer comments:**
- Codex / Design: flagged fragmented crop-session ownership, duplicated crop/rotation transform logic, and the pre-existing shared canvas-size cache coupling between `main.rs` and `viewer.rs`. Addressed the user-visible state drift by unifying app helpers around the visible crop and by snapping committed preview crops to the same pixel bounds as save/export. Remaining residual: the render-path canvas-size cache and split crop-session ownership are still acknowledged maintainability tradeoffs under the current `iced` shader setup.
- Gemini / Design: no findings in the available pass; accepted the existing canvas-size cache as a documented tradeoff rather than a new design regression.
- Codex / Test coverage: requested end-to-end rotated crop commit/save coverage, direct rotated/cropped preview framing coverage, and mouse actual-size coverage while crop mode shows the uncropped image. Addressed with new integration and viewer tests; Codex verification found no remaining coverage issues.
- Gemini / Test coverage: no findings after the crop, rotation, status, viewer, and save/export regression coverage was expanded.
- Codex / Correctness: flagged preview/export pixel-rounding drift, crop-aware actual-size preservation regressions on crop-changing transitions, and actual-size commands using hidden crop state while crop mode showed the full image. Addressed by snapping crop previews to pixel bounds, routing status/actual-size through the visible crop, and preserving actual-size zoom across crop commits, clears, reset, undo, and redo; Codex verification found no remaining correctness issues.
- Gemini / Correctness: no findings in the available pass.
- Codex / Clean code / typing / efficiency / memory leaks: flagged the full-frame cropped-save allocation and the mismatch between viewer crop display and app-level zoom/status helpers. Addressed by iterating only the cropped save bounds and unifying app helpers around visible crop. Remaining residual: the viewer still updates canvas size through the shared cache during draw because the current `iced` shader integration does not surface that size cleanly elsewhere.
- Gemini / Clean code / typing / efficiency / memory leaks: no findings in the available pass.
- Codex / Documentation: flagged stale summary, detailed devlog, architecture overview, and lessons docs. Addressed by updating those files for the crop workflow; follow-up verification only requested a final note refresh once validation completed.
- Gemini / Documentation: flagged the missing crop devlog/summary/lessons updates and requested clearer rationale around crop preview vs. overlay in the shader path. Addressed by updating the docs and adding a brief shader comment describing the preview/overlay split.
**Result:** Success - Detail view now supports freeform and square crop selection, rotated crop exports match the selection users see, and crop-aware status/actual-size behavior stays in sync across crop mode, save/export, and history changes.
**Files changed:** `assets/shaders/image.wgsl`, `docs/architecture/ARCHITECTURE.md`, `docs/devlog/summary.md`, `docs/devlog/detailed/2026-04-20_2026-04-20.md`, `docs/learning/lessons.md`, `src/edit.rs`, `src/main.rs`, `src/viewer.rs`
**Reasoning:** Crop spans the viewer, shader, edit state, and save pipeline, so the main risk was silent drift between what the preview showed and what save/export produced, especially once rotation and actual-size zoom entered the path. The fix therefore prioritized one pixel-snapped crop contract across preview, status text, zoom math, history transitions, and export, then added tests that lock down the rotated and crop-mode edge cases reviewers called out.
**Notes:** Final validation after the review fixes and documentation updates passed with `cargo test` (167 tests), `cargo clippy -- -D warnings`, and `cargo build`. Gemini CLI access was intermittently constrained during the review loop, so the Gemini passes used `gemini-2.5-flash` plus a saved uncommitted diff when `gemini-3.x` capacity was unavailable.

## [2026-04-20 02:57 PDT] - Reflow library thumbnails after resizing while Detail view is open
**Action:** Fixed a responsive layout bug where opening the app small, switching to Detail, resizing larger, and returning to Library left thumbnail grids arranged for the old width. Added regression tests for library reflow after returning from Detail, narrow-window minimum-column behavior, and collection-grid reflow under the same resize path. Updated `App` to store the latest `window::Event::Resized` size, introduced shared thumbnail-grid geometry constants plus layout helpers, and made both library and collection grids derive their columns from the current window width instead of a fixed count. Follow-up cleanup removed duplicated thumbnail-card markup, avoided temporary chunk vectors during grid rendering, and replaced collection-grid linear scans with a persistent `library_indices_by_path` lookup rebuilt through `rebuild_library_indices()`.
**Code reviewer comments:**
- Codex / Design: initially flagged duplicated sidebar width knowledge and duplicated thumbnail-grid geometry between breakpoint math and rendering. Addressed by sharing the same constants across layout and render helpers; verification found no remaining design findings.
- Gemini / Design: no findings. Residual note only: keep responsive layout values centralized so future tweaks cannot drift between calculation and rendering.
- Codex / Test coverage: no blocking findings. Residual note only: the regression tests validate layout contracts rather than the rendered widget tree, which is acceptable for this app-level behavior.
- Gemini / Test coverage: no findings after the library, narrow-window, and collection resize regressions were added. Residual note only: there is still no manual visual regression harness, so the logic tests remain the main protection here.
- Codex / Correctness: initially called out the lack of a dedicated collection-grid resize regression. Addressed by adding `collection_grid_uses_latest_window_width_after_returning_from_detail`; verification found no remaining correctness findings.
- Gemini / Correctness: no findings.
- Codex / Clean code / typing / efficiency / memory leaks: initially flagged the collection-grid linear scan, temporary render allocations, duplicated thumbnail-card markup, and an outdated lesson entry. Addressed with the persistent path index, shared grid builder/card helper, and lessons update; verification found no remaining clean-code findings.
- Gemini / Clean code / typing / efficiency / memory leaks: surfaced only broader pre-existing concerns outside this task's scope, including the large `App::update` surface and eager collection saves. No change-specific findings remained.
- Codex / Documentation: initially flagged the missing detailed devlog entry and stale debugging-note description of the final lookup path. Addressed by appending this entry and correcting the debugging note; verification found no remaining documentation findings.
- Gemini / Documentation: no findings after the debugging note, summary, detailed devlog, and lessons documents were aligned with the final implementation.
**Result:** Success - library and collection thumbnail grids now reflow from the latest window width after resizing in Detail view, including when returning from a small-to-large resize path that previously kept the old layout.
**Files changed:** `docs/debugging/2026-04-20-library-grid-resize.md`, `docs/devlog/summary.md`, `docs/devlog/detailed/2026-04-20_2026-04-20.md`, `docs/learning/lessons.md`, `src/main.rs`
**Reasoning:** The stale layout came from the library path relying on fixed column assumptions instead of the latest resize state. The safest fix was to establish one window-width-driven grid contract shared by library and collection views, then lock down the user-visible resize path with tests before trimming the render-path duplication reviewers found.
**Notes:** Validation for the final code changes passed with `cargo test` (170 tests), `cargo clippy -- -D warnings`, and `cargo build`. Manual in-app smoke testing of live resize feel was not performed. Gemini CLI access was intermittently unreliable earlier in the session, but the available documentation pass completed successfully after the docs were updated.

## [2026-04-20 11:35 PDT] - Finish crash-hardening follow-up for collection actions and loading-time save
**Action:** Finished the crash-fix follow-up work in `src/main.rs` by hardening the remaining fail-open paths around collection actions and save requests. Added collection-existence guards for add/toggle actions, introduced `SaveRequest`, `visible_edit_state()`, `current_lens_vignetting()`, and `current_save_request()` so save uses the visible crop state, and made `SaveEdited` a safe no-op while a new image is still loading. Expanded regression coverage for removed collections, visible-crop save behavior, loading-time save attempts, save status/no-image behavior, positive collection toggles after reflow, and lens-vignetting save state. Also removed temporary review artifacts and cleaned up duplicated test-helper and vignetting logic found during review.
**Code reviewer comments:**
- Codex / Design: initially flagged that the new test setup was manually rebuilding library lookup invariants in multiple places. Addressed with focused test-only library mutation helpers; verification found no remaining design findings.
- Gemini / Design: initially flagged save/export still following hidden crop state and collection actions still assuming their stored destination remained valid. Addressed with `visible_edit_state()` plus collection-existence guards; verification found no remaining design findings.
- Codex / Test coverage: initially requested stale menu-action regressions plus visible-crop save coverage, then later asked for removed-collection and save no-op coverage. Addressed with the new regression set; verification found no remaining coverage findings.
- Gemini / Test coverage: requested explicit no-image save behavior, save-status assertions, lens-vignetting save coverage, and a positive toggle-after-reflow path. Addressed with targeted tests; verification found no remaining coverage findings.
- Codex / Correctness: found that `SaveEdited` could write the previous image to the newly selected path while a new image was still loading. Addressed by making `current_save_request()` return `None` during loading; verification found no remaining correctness findings.
- Gemini / Correctness: no findings in the correctness review pass.
- Codex / Clean code / typing / efficiency / memory leaks: initially flagged the temporary review artifacts left in the repo root. Addressed by removing the `tmp_codex_*.txt` files; verification found no remaining clean-code findings.
- Gemini / Clean code / typing / efficiency / memory leaks: first flagged duplicated test-helper reset logic, then duplicated lens-vignetting extraction between save and viewer uniforms. Addressed with `reset_library_navigation_state()` plus `current_lens_vignetting()`; focused verification found no remaining clean-code findings.
- Codex / Documentation: initially flagged the missing detailed devlog entry, wording that said save was disabled instead of a safe no-op, and docs that overstated earlier landed hardening beyond the current diff. Addressed by adding this entry and tightening the summary/debugging/lessons wording; final verification found no remaining documentation findings.
- Gemini / Documentation: no findings in the documentation pass.
**Result:** Success - collection actions now fail closed if their destination collection disappears, save/export uses the visible crop state, and loading-time save attempts no-op safely instead of acting on partially updated state.
**Files changed:** `docs/debugging/2026-04-20-stale-detail-state-crash.md`, `docs/devlog/summary.md`, `docs/devlog/detailed/2026-04-20_2026-04-20.md`, `docs/learning/lessons.md`, `src/main.rs`
**Reasoning:** The remaining crash-prone behavior was in fail-open follow-up paths rather than the primary viewer/render stack: collection actions trusted stale destination collections, and save trusted partially updated async-load state plus hidden crop state. The safest fix was to make those actions build from currently visible state, return early when state is incomplete, and lock the behavior down with app-level tests before trimming the review-driven duplication.
**Notes:** Final validation after the code-review follow-ups passed with `cargo test` (189 tests), `cargo clippy -- -D warnings`, and `cargo build`. Manual in-app reproduction of the original crash was not performed. Gemini CLI returned several noisy `AttachConsole failed` messages and intermittent `429` capacity errors during review, but the completed passes that did return were incorporated before the final verification round.

## [2026-04-20 13:04 PDT] - Replace Detail-view rotation words with icon buttons
**Action:** Swapped the Detail-view rotation controls in `src/main.rs` from worded buttons to icon-first buttons, keeping compact `-90°` and `+90°` cues inside each control so clockwise and counterclockwise actions stay legible without the heavier "Rotate ..." labels. Added a focused regression test that builds the shared rotation button helper, verifies the button-column-text composition for both directions, and asserts that the edit panel still contains the expected rotation-control section structure after future UI refactors. Updated the summary and lessons to record the final affordance and the testing limitation around `iced` widget trees.
**Code reviewer comments:**
- Codex / Design: earlier review rounds pushed the UI away from tooltip-only glyph buttons and toward a clearer in-button cue. After the final `-90°` and `+90°` cues were moved into the icon buttons, Codex verification found no remaining design issues in the reviewed scope.
- Gemini / Design: consistently favored keeping the action cue attached to the button instead of separated into surrounding labels. The latest Gemini design pass still noted style-level concerns about vertical rhythm and whether an icon button with a compact step label is "pure" enough, but did not identify a behavioral regression.
- Codex / Test coverage: requested stronger protection against edit-panel wiring drift than the initial isolated helper test provided. Addressed by extending the regression to assert the presence and shape of the `ROTATE` section in `edit_panel()`; the remaining residual is the framework-level limitation that `iced` widget-tree tests do not prove runtime glyph rendering with the live font stack.
- Gemini / Test coverage: highlighted the same general `iced` limits around literal rendered text, styling, and event binding. Addressed the section-placement gap in the available test coverage; remaining residuals are those runtime rendering limits rather than a missing app-level contract.
- Codex / Correctness: no correctness findings after the final control wiring and regression updates.
- Gemini / Correctness: a final Gemini correctness verification attempt was blocked by CLI quota exhaustion, so there is no additional Gemini correctness finding beyond the completed earlier review rounds.
- Codex / Clean code / typing / efficiency / memory leaks: no findings after the rotation-button helper and test cleanup.
- Gemini / Clean code / typing / efficiency / memory leaks: the latest available pass surfaced only minor style concerns around layout properties, not a correctness or boundary issue.
- Codex / Documentation: required the summary, detailed devlog, and lessons files to reflect the icon-button affordance and the real limits of the UI regression test. Addressed in this entry and the paired summary/lesson updates.
- Gemini / Documentation: a final Gemini documentation verification was not available in this pass because Gemini CLI access was already quota constrained; the documentation updates therefore rely on the completed Codex verification plus local consistency checks.
**Result:** Success - Detail-view rotation now uses icon buttons instead of word buttons, with compact in-button direction cues and regression coverage that locks down the edit-panel section structure around those controls.
**Files changed:** `docs/devlog/summary.md`, `docs/devlog/detailed/2026-04-20_2026-04-20.md`, `docs/learning/lessons.md`, `src/main.rs`
**Reasoning:** The worded rotate buttons added unnecessary visual weight to a compact edit toolbar. Icon-first controls solve that, but only if the direction cue stays attached to the button so the action remains obvious at a glance. The test update therefore focused on the user-facing contract we can honestly assert in `iced`: the presence, grouping, and composition of the rotation controls in the `ROTATE` section, while the docs record the remaining runtime-rendering limitation.
**Notes:** Final validation passed with `cargo test` (190 tests), `cargo clippy -- -D warnings`, and `cargo build`. `iced` widget-tree tests still cannot prove exact runtime glyph rendering or spacing, and a final Gemini correctness/documentation verification was blocked by quota exhaustion during this task.

## [2026-04-20 13:59 PDT] - Make the release click-to-open blur path lazy
**Action:** Investigated the user-reported release-build crash that happened after clicking a Library image into Detail view, where rendering delayed or appeared stuck before the app died. Traced the click-to-open path through `src/main.rs` and the GPU `prepare()` path in `src/viewer.rs`, then changed the viewer to skip blur-texture generation on image open unless clarity or dehaze is actually enabled. Added `AdjustmentUniforms::needs_blur()`, a small `BlurUpdatePlan` helper to make the lazy-blur state transition explicit, placeholder blur-view fallback/reset on image changes, and focused regression tests for both the blur predicate and the lazy-blur state machine. Recorded the debugging session in a new note and updated the summary and lessons with the remaining manual release-verification caveat.
**Code reviewer comments:**
- Codex / Design: first flagged that the lazy path reset `blur_image_id` without resetting the bound blur view, plus documentation that spoke with more certainty than the evidence supported. Addressed by storing a dedicated placeholder blur view, clearing `blur_texture_view` on image swap/clear, and downgrading the debugging note from confirmed root-cause language to likely-cause wording. Follow-up design review then narrowed the remaining concern to documentation precision around test scope and observed-vs-reported behavior; addressed by tightening the debugging note wording.
- Gemini / Design: the available Gemini pass found the lazy-blur state changes in `src/viewer.rs` well-named and coherent, with no blocking design issue in the viewer fix itself. Gemini CLI then emitted repeated `AttachConsole failed` noise during the same review session, so no stronger final Gemini design verification was available beyond that completed pass.
- Codex / Test coverage: first noted that a pure `needs_blur()` test did not protect the actual lazy `prepare()` state transition. Addressed by extracting `BlurUpdatePlan` and adding state-transition tests for image-change reset, first-enable blur generation, and the no-work steady state. Residual limitation: coverage still does not execute the full WGPU `prepare()` path in a live renderer.
- Gemini / Test coverage: the completed Gemini pass agreed that the boolean guard alone was not enough and recommended broader integration coverage around enabling/disabling blur-based adjustments across image loads. Addressed partially with the new `BlurUpdatePlan` tests; remaining residual is the lack of a GPU-backed integration harness for `prepare()`.
- Codex / Correctness: broad review flagged that `needs_blur()` used `abs() > f32::EPSILON` while WGSL blur branches still trigger on any non-zero value. Addressed by switching the CPU gate to the same exact non-zero rule as the shader.
- Gemini / Correctness: no concrete correctness bug was reported in the completed Gemini pass for `src/viewer.rs`; its feedback stayed focused on coverage depth rather than a failing code path.
- Codex / Clean code / typing / efficiency / memory leaks: broad review pushed toward a clearer state machine instead of ad hoc booleans in the lazy-blur path. Addressed with the small `BlurUpdatePlan` helper and explicit placeholder-view ownership. No remaining clean-code finding was raised after the state cleanup; residual note only: the WGPU path is still not directly integration-tested.
- Gemini / Clean code / typing / efficiency / memory leaks: the completed Gemini pass described the resource-management changes as necessary and robust, with its only follow-up request being stronger functional coverage for the state transitions.
- Codex / Documentation: broad review flagged the stale test count, missing summary/detailed devlog updates, and debugging-note wording that overstated both the regression coverage and the certainty of the diagnosis. Addressed by updating the summary to 194 tests, appending this detailed entry, and tightening the debugging note to call the crash behavior user-reported and the blur-path diagnosis likely rather than proven.
- Gemini / Documentation: no usable final Gemini documentation pass completed because the CLI degraded into `AttachConsole failed` noise during the review loop, so documentation verification for this task ultimately relied on the completed Codex passes plus local consistency checks.
**Result:** Success - the default Detail click-to-open path no longer builds blur resources unless the current edit state actually needs them, image changes now reset blur bindings back to the placeholder safely, and the viewer has focused regression coverage for the lazy-blur decision logic.
**Files changed:** `docs/debugging/2026-04-20-release-click-crash.md`, `docs/devlog/summary.md`, `docs/devlog/detailed/2026-04-20_2026-04-20.md`, `docs/learning/lessons.md`, `src/viewer.rs`
**Reasoning:** The updated user symptom pointed away from the earlier stale-state save/menu crash and toward the first Detail render after clicking a Library image. The viewer was still doing work on every image open that only clarity/dehaze actually needs, so the safest fix was to make that pre-pass lazy, keep the placeholder binding invariant explicit across image swaps, and cover the decision/state-transition logic with pure tests that do not require a GPU test harness.
**Notes:** Final validation passed with `cargo test` (194 tests), `cargo clippy -- -D warnings`, and `cargo build --release`. Manual in-app verification of the packaged release crash was not run, so the lazy-blur path should be treated as a high-confidence fix for a likely cause rather than a conclusively reproduced-and-verified root cause. Gemini review coverage for this task was partial because the CLI produced `AttachConsole failed` noise during follow-up attempts.

## [2026-04-20 14:34 PDT] - Fix the confirmed first-render uniform-layout panic on Detail open
**Action:** Reproduced the reported "Library click opens Detail, image never loads, then app crashes" failure locally and captured the actual panic output with `cargo run -- C:\Users\38909\Documents\github\photo\test.jpg`. The debug output showed a fatal `wgpu` validation error: the bound uniform buffer was 220 bytes while the shader expected 240. Traced that mismatch to `src/viewer.rs`, where recent crop-related fields had made the Rust `Uniforms` struct diverge from `assets/shaders/image.wgsl`. Added the failing regression test `uniforms_layout_matches_wgsl_uniform_buffer`, then fixed the layout with explicit padding around the crop fields so the Rust buffer once again matches the WGSL contract. Wrote a new debugging note with the confirmed root cause, updated the summary and lessons, and manually simulated direct Detail opens for both `test.jpg` and the persisted library RAW file `C:\Users\38909\Documents\images\DSC01154.ARW` in debug and release builds to confirm the process now stays alive.
**Code reviewer comments:**
- Codex / Design: no findings. Residual note only: the host-side layout regression is strong, but there is still no GPU-backed CI smoke test that executes the live renderer.
- Gemini / Design: no findings in the available `gemini-2.5-flash` pass. The review agreed that explicit padding plus the dedicated layout test is a reasonable way to preserve the Rust/WGSL contract.
- Codex / Test coverage: no findings. The new size/offset regression was considered the right permanent guard for this specific crash; residual limitation remains the lack of a GPU-backed renderer test.
- Gemini / Test coverage: no findings. The review called out the same already-documented limitation that CI still does not execute a full WGPU render pass.
- Codex / Correctness: no findings. The review agreed that the 220-byte vs. 240-byte mismatch explained the crash and that the explicit padding restores the expected offsets for the crop fields.
- Gemini / Correctness: no findings. The available review agreed that the debugging note, layout test, and `Uniforms` fix all point to the same confirmed root cause.
- Codex / Clean code / typing / efficiency / memory leaks: no findings. The explicit padding was accepted as the clearest low-risk fix for a GPU layout contract issue.
- Gemini / Clean code / typing / efficiency / memory leaks: no findings. The review considered the comments and focused test appropriate for this low-level renderer contract.
- Codex / Documentation: no findings. The debugging note, summary, and lessons were all considered current and aligned with the confirmed diagnosis and validation steps.
- Gemini / Documentation: no findings in the available flash pass. A `gemini-2.5-pro` review attempt was blocked by `429` quota exhaustion before the fallback flash pass completed successfully.
**Result:** Success - Detail-view opens no longer hit the fatal crop-uniform `wgpu` validation panic, and the repo now has a permanent regression test that locks the Rust uniform layout to the WGSL shader contract.
**Files changed:** `docs/debugging/2026-04-20-detail-open-uniform-layout-crash.md`, `docs/devlog/summary.md`, `docs/devlog/detailed/2026-04-20_2026-04-20.md`, `docs/learning/lessons.md`, `src/viewer.rs`
**Reasoning:** The earlier crash theory about excess blur work did not survive a direct debug repro. Once the live panic named a concrete uniform-buffer size mismatch, the safest path was to lock down the low-level GPU contract with a failing test first, then make the smallest explicit-layout change that restored the expected shader offsets without widening the viewer behavior change.
**Notes:** Validation passed with `cargo test` (195 tests), `cargo clippy -- -D warnings`, and `cargo build --release`. Manual simulation also confirmed that `target\debug\photo.exe` stayed alive after launching into `test.jpg` for 5 seconds and into `DSC01154.ARW` for 8 seconds, and that `target\release\photo.exe` stayed alive after launching into `DSC01154.ARW` for 8 seconds. The remaining risk is unchanged from review: this task now has a strong host-side layout regression, but not a GPU-backed automated render test.

## [2026-04-20 16:08 PDT] - Speed up RAW Detail loading with staged preview-first decode
**Action:** Reduced Detail-view image load latency by splitting RAW opening into a staged flow. Added `decode_embedded_preview()` plus shared RAW embedded-image helpers in `src/decode.rs`, then changed `src/main.rs` so non-RAW files still go straight to full decode while RAW files first attempt a fast embedded preview and only launch the heavier full-resolution decode plus EXIF follow-up for the still-current request. Centralized the async lifecycle in `DetailLoadState`, tagged preview/full/EXIF completions with request ids so stale results are ignored, moved EXIF reads off the UI completion path, blocked save while only a preview or required auto-lens metadata is available, and kept the preview-to-full handoff on the user's existing zoom/pan after correcting an initially wrong resolution-based zoom rewrite. Recorded the debugging session, updated the architecture docs/decision log for the staged flow, refreshed the summary and lessons, and added app/decode regressions for preview-first loading, no-preview fallback, preview-only failure fallback, stale preview/full/EXIF suppression, and auto-lens save gating.
**Code reviewer comments:**
- Codex / Design: initial review flagged stale async work accumulation, loosely coupled preview/loading flags, and preview-planning logic leaking into UI-time decisions. Addressed by choosing the RAW vs. non-RAW load plan in `start_load()`, centralizing state in `DetailLoadState`, and making follow-up work request-id aware. A later local re-check found the first preview-to-full framing fix was over-correcting by rewriting zoom from source resolution; addressed by removing that correction and keeping the existing zoom/pan.
- Gemini / Design: automated review attempt was blocked by repeated `429` model-capacity failures, so no Gemini design verdict was returned for this task.
- Codex / Test coverage: initial review requested explicit coverage for RAW files without embedded previews, preview-only fallback when the final decode fails, and stale EXIF completion after a newer load starts. Addressed with targeted regressions in `src/main.rs` plus embedded-preview decode tests in `src/decode.rs`.
- Gemini / Test coverage: automated review attempt was blocked by repeated `429` model-capacity failures, so no Gemini coverage verdict was returned for this task.
- Codex / Correctness: initial review flagged that the preview-to-full handoff could jump and that EXIF-loading state was not reflected in save affordances or lens info. Addressed by keeping the swap on the current zoom/pan, disabling save until `current_save_request()` is valid, and showing `Loading lens metadata…` while auto-lens data is still in flight.
- Gemini / Correctness: automated review attempt was blocked by repeated `429` model-capacity failures, so no Gemini correctness verdict was returned for this task.
- Codex / Clean code / typing / efficiency / memory leaks: no final automated Codex verdict completed because `codex exec review uncommitted` was interrupted by high-demand reconnect errors before returning findings. Local consistency review kept the shared RAW helper path, `DetailLoadState`, and request-id filtering in place to avoid duplicated staging logic.
- Gemini / Clean code / typing / efficiency / memory leaks: automated review attempt was blocked by repeated `429` model-capacity failures, so no Gemini clean-code verdict was returned for this task.
- Codex / Documentation: no final automated Codex verdict completed because `codex exec review uncommitted` was interrupted by high-demand reconnect errors before returning findings. Locally updated the debugging note, architecture docs, summary, this detailed entry, and lessons so they match the staged-load behavior and the final handoff semantics.
- Gemini / Documentation: automated review attempt was blocked by repeated `429` model-capacity failures, so no Gemini documentation verdict was returned for this task.
**Result:** Success - RAW Detail view now shows a usable embedded preview much sooner when one exists, upgrades in place to the full-resolution decode without resetting the current zoom/pan, keeps EXIF/lens autodetection off the first-image path, and prevents preview-only or metadata-incomplete saves from running.
**Files changed:** `docs/architecture/ARCHITECTURE.md`, `docs/architecture/decisions.md`, `docs/architecture/drift-log.md`, `docs/debugging/2026-04-20-detail-load-latency.md`, `docs/devlog/summary.md`, `docs/devlog/detailed/2026-04-20_2026-04-20.md`, `docs/learning/lessons.md`, `src/decode.rs`, `src/main.rs`
**Reasoning:** The visible wait was coming from the RAW detail path insisting on full RAW development before showing anything, plus EXIF/lens lookup sharing that completion path. The safest user-facing speedup was therefore to show an embedded RAW image first when available, keep the async lifecycle explicit so stale work cannot win races, and protect save/export until the final image and any required auto-lens metadata are ready. The later zoom rewrite was removed because the viewer's fit math already normalizes source resolution, so preserving the user's existing zoom/pan produced the stable handoff we actually wanted.
**Notes:** Final validation passed with `cargo test` (204 tests), `cargo clippy -- -D warnings`, and `cargo build --release`. `codex exec review uncommitted` did not complete because of high-demand reconnect failures, and Gemini review attempts repeatedly returned `429` capacity exhaustion, so the final reviewer status for this task is partial and blocked by external tooling rather than by a known code issue. Manual stopwatch benchmarking was not added, so the performance improvement is user-visible pipeline work rather than a quantified benchmark result.

## [2026-04-20 19:02 PDT] - Restore missing rotation glyph rendering in Detail view
**Action:** Fixed the missing clockwise/counterclockwise rotation glyphs in `src/main.rs` by routing the icon label through `Segoe UI Symbol` with `iced::widget::text::Shaping::Advanced` while keeping the existing `-90°` and `+90°` step cues. Refactored the button construction through a small generic `rotation_button_widget()` helper so the regression can inspect the actual icon text emitted by the button path instead of only nearby constants, then updated the summary and lessons with the Windows `iced` glyph-rendering constraint.
**Code reviewer comments:**
- Codex / Design: no findings in the final design pass.
- Gemini / Design: review attempts were blocked. An initial `gemini-3-pro` call failed with `ModelNotFoundError`, and subsequent Gemini CLI calls failed with `429` quota exhaustion before a design verdict was produced.
- Codex / Test coverage: initially found that `rotation_controls_use_icon_buttons` only asserted constants and widget structure, not the actual icon label used by the button. Addressed by introducing the testable `rotation_button_widget()` path and extending the regression to inspect the emitted icon text state for both font and shaping; Codex verification then found no remaining coverage gaps.
- Gemini / Test coverage: review attempt blocked by Gemini CLI `429` quota exhaustion.
- Codex / Correctness: no findings. The review agreed that the explicit symbol font plus advanced shaping is the right fix for the missing glyph-render path.
- Gemini / Correctness: review attempt blocked by Gemini CLI `429` quota exhaustion.
- Codex / Clean code / typing / efficiency / memory leaks: no findings. The review accepted the small generic helper as a lean way to make the actual button path testable without widening the UI behavior change.
- Gemini / Clean code / typing / efficiency / memory leaks: review attempt blocked by Gemini CLI `429` quota exhaustion.
- Codex / Documentation: initially flagged the missing detailed devlog entry for this follow-up fix. Addressed by appending this entry after updating the summary and lessons; follow-up Codex documentation verification found no remaining documentation issues.
- Gemini / Documentation: review attempt blocked by Gemini CLI `429` quota exhaustion.
**Result:** Success - the clockwise and counterclockwise rotation icons render again in Detail view, the compact `-90°` and `+90°` cues remain intact, and the regression now verifies the actual icon-label render settings instead of only nearby constants.
**Files changed:** `docs/devlog/summary.md`, `docs/devlog/detailed/2026-04-20_2026-04-20.md`, `docs/learning/lessons.md`, `src/main.rs`
**Reasoning:** The buttons were still wired correctly, so the safest fix was to keep the existing affordance and treat the problem as a glyph-rendering contract instead of redesigning the control. In this Windows `iced` UI, the durable constraint is that the open-circle rotation arrows are not reliable on the default text path; binding them to a font with known coverage and locking that down with a real button-path regression solves the bug without changing behavior.
**Notes:** Final validation after the review follow-up passed with `cargo test` (204 tests) and `cargo build --release`. Existing unrelated local edits in `AGENTS.md` and `src/viewer.rs` were left untouched. Gemini review coverage for this task was attempted for every theme but remained blocked by external model availability/quota limits rather than by a known code issue.

## [2026-04-20 20:09 PDT] - Add a persisted decoded-image cache for RAW and SVG Detail loads
**Action:** Added a persisted full-image decode cache in `src/decode.rs` for the expensive Detail-load paths that benefit most from reuse: RAW and SVG. The cache now stores decoded RGBA output on disk, validates entries against both the source-file fingerprint and a focused decode-contract hash, writes through collision-safe unique temp files, and bounds disk growth with periodic oldest-first pruning. Added regression coverage for scope selection, fallback cache-path selection, cache creation/reuse, source-change invalidation, decode-contract invalidation, corrupt-cache fallback, temp-path uniqueness, and retention pruning. Also added the repo-local fallback cache directory to `.gitignore` and updated the summary and lessons.
**Code reviewer comments:**
- Codex / Design: initially flagged two design issues: unbounded persisted cache growth and cache freshness tied only to source metadata plus a manually maintained version. Addressed by adding a bounded size budget with pruning and by replacing the manual-only invalidation with a decode-contract hash; final design verification found no remaining design findings.
- Gemini / Design: review attempt blocked by Gemini CLI `429` quota exhaustion.
- Codex / Test coverage: first requested a contract-level cache reuse test closer to `decode_image()` plus corrupt-cache recovery coverage; a second follow-up noted that the first corrupt-cache test only exercised a clean cache miss instead of the read-error fallback. Addressed by adding temp-scoped cache-dir regression coverage for cache creation/reuse, fallback-path selection, and the true truncated-cache error path; final verification found no remaining coverage issues.
- Gemini / Test coverage: review attempt blocked by Gemini CLI `429` quota exhaustion.
- Codex / Correctness: initially flagged the risk of stale cache entries surviving decode changes and the shared-temp-file race for concurrent same-source writes. Addressed by storing a decode-contract hash in the cache header and by switching to unique temp filenames per write attempt; final correctness verification found no remaining issues.
- Gemini / Correctness: review attempt blocked by Gemini CLI `429` quota exhaustion.
- Codex / Clean code / typing / efficiency / memory leaks: initially flagged repeated hot-path hashing of the whole source file for cache invalidation and synchronous full-cache pruning on every write, then later flagged that one test still touched the real persisted cache directory. Addressed by caching a focused contract hash with `OnceLock`, amortizing pruning behind a write interval, and moving the contract-level cache test to a temp-scoped cache directory; final verification found no remaining clean-code issues.
- Gemini / Clean code / typing / efficiency / memory leaks: review attempt blocked by Gemini CLI `429` quota exhaustion.
- Codex / Documentation: initially flagged the missing detailed devlog entry for this task. Addressed by appending this entry after the summary and lessons updates; follow-up documentation verification found no remaining issues.
- Gemini / Documentation: review attempt blocked by Gemini CLI `429` quota exhaustion.
**Result:** Success - repeated RAW and SVG Detail loads can now reuse a persisted decoded-image cache instead of paying the full decode cost every time, while stale, corrupt, or oversized cache state fails safely and is covered by regression tests.
**Files changed:** `.gitignore`, `docs/devlog/summary.md`, `docs/devlog/detailed/2026-04-20_2026-04-20.md`, `docs/learning/lessons.md`, `src/decode.rs`
**Reasoning:** A decoded cache helps most where the current Detail path is doing materially expensive work before the GPU ever sees pixels, especially RAW development and SVG rasterization. The safest way to ship that win was to keep the cache entirely inside `decode.rs`, scope it to those expensive formats, and add the operational guardrails reviewers called out so reuse never silently outruns correctness or disk hygiene.
**Notes:** Final validation after the review follow-ups passed with `cargo test` (213 tests), `cargo build --release`, and `cargo clippy -- -D warnings`. Existing unrelated local edits in `AGENTS.md`, `src/main.rs`, and `src/viewer.rs` were left untouched. Gemini review was attempted for every theme but remained blocked by external quota limits rather than by a known code issue.

## [2026-04-20 21:34 PDT] - Add a same-session full-image cache for repeat Detail opens
**Action:** Added a bounded same-session full-image cache in `src/main.rs` so repeat Detail opens can reuse an already loaded full-resolution image immediately instead of blanking the viewer and repeating the RAW preview/full async path. The new cache keeps preview-only RAW states out, resets stale UI state on synchronous hits, bounds retained images by both entry count and byte budget, and validates hits against current source state before reuse. To keep cached hits safe under concurrent file changes, the cache now validates through a write-denying read handle, and the full-image worker only returns a cacheable fingerprint captured while that same handle stays alive through the decode. Added regressions for repeat RAW reuse, preview exclusion, stale-state reset, same-size rewrite invalidation, stale pre-rewrite fingerprints, and both byte-budget plus entry-cap eviction behavior. Updated the summary and lessons with the new same-session cache behavior and validation constraint.
**Code reviewer comments:**
- Codex / Design: no findings in the scoped same-session cache change.
- Gemini / Design: review attempt blocked by Gemini CLI `429` quota exhaustion.
- Codex / Test coverage: initially flagged missing coverage for stale-state reset on synchronous cache hits and for the separate entry-cap eviction path. Addressed with regressions for both; follow-up verification found no remaining coverage issues.
- Gemini / Test coverage: review attempt blocked by Gemini CLI `429` quota exhaustion.
- Codex / Correctness: iterative review flagged several stale-cache hazards: metadata-only invalidation, fingerprint capture after decode, sampled-signature blind spots, and cache-hit TOCTOU gaps. Addressed by moving cache insertion onto a fingerprint carried out of the blocking full-image task, switching the content signature to a streamed full-file hash, validating cache hits under a write-denying read handle, and keeping that handle alive through decode so cacheable fingerprints come from the same guarded file state; final verification found no remaining correctness issues.
- Gemini / Correctness: review attempt blocked by Gemini CLI `429` quota exhaustion.
- Codex / Clean code / typing / efficiency / memory leaks: initially flagged that cache lookup always hashed the whole source on the UI thread and that the worker was redundantly fingerprinting before and after decode. Addressed by short-circuiting cache misses before any file hashing, adding a cheap metadata precheck before the full hit validation, and collapsing the worker to a single guarded fingerprint pass while the read lock stays alive through decode; follow-up verification found no remaining clean-code issues.
- Gemini / Clean code / typing / efficiency / memory leaks: review attempt blocked by Gemini CLI `429` quota exhaustion.
- Codex / Documentation: no findings after updating the summary, lessons, and this detailed entry for the shipped same-session cache behavior.
- Gemini / Documentation: review attempt blocked by Gemini CLI `429` quota exhaustion.
**Result:** Success - repeat same-session Detail opens can now reuse a previously loaded full image immediately, RAW preview-only states do not poison that cache, stale UI state clears correctly on cache hits, and changed source files fail closed instead of silently reusing stale pixels.
**Files changed:** `docs/devlog/summary.md`, `docs/devlog/detailed/2026-04-20_2026-04-20.md`, `docs/learning/lessons.md`, `src/main.rs`
**Reasoning:** The persisted decode cache improved expensive full decodes but still left noticeable latency from rereading cached pixels and waiting for the async load path to come back after `start_load()` blanked the image. The most direct way to make repeat opens feel immediate was therefore a same-session full-image cache checked synchronously in `start_load()`, but that only works if the cache is bounded, preview-only states stay out, and the source-validation story is strong enough that fast hits never silently outrun concurrent file changes.
**Notes:** Final validation after the implementation and review follow-ups passed with `cargo test` (219 tests), `cargo build --release`, and `cargo clippy -- -D warnings`. Existing unrelated local edits in `AGENTS.md`, `src/decode.rs`, and `src/viewer.rs` were left untouched. Gemini review was attempted for every completed theme but remained blocked by external quota limits rather than by a known code issue.
