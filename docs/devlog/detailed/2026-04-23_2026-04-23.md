# Detailed Devlog

## [2026-04-23 00:01 PDT] - Fix squashed portrait RAW thumbnails by applying RAW orientation through the whole decode pipeline
**Action:** Traced the reported squashed Library thumbnail for the real portrait RAW `DSC01169.ARW` past the square-slot widget and into the RAW decode pipeline in `src/decode.rs`. The RAW path now applies orientation to embedded thumbnails, embedded previews, full RAW develop output, and reported RAW source dimensions. Embedded-image decode now stays on the cheap EXIF path when EXIF orientation is present, but it falls back to `rawimage.orientation` when EXIF orientation is missing or unreadable so Library thumbnails, staged embedded previews, full RAW decode output, and logical dimensions stay aligned on the same file. Bumped `DECODE_CACHE_CONTRACT_VERSION` from `4` to `5` so persisted decoded RAW cache entries from the pre-fix pipeline are rebuilt. Strengthened regression coverage with rotate-90, rotate-180, rotate-270, transpose, horizontal-flip, and metadata-fallback policy tests, plus baseline-rotation comparisons that prove handedness instead of only axis swapping. Updated the summary and lessons, and because the date boundary rolled past midnight PDT, started this new detailed devlog file for 2026-04-23.
**Code reviewer comments:**
- Codex / Design: the first successful Codex review accepted the shared-helper design and cache-version bump, but it flagged two remaining concerns: already-baked repo-local edited copies are outside decoded-cache invalidation, and the embedded-preview path was still EXIF-only while full RAW decode fell back to `rawimage.orientation`. I kept the baked-local-edits note as an explicit scope tradeoff and aligned the embedded-preview fallback with the full RAW path when EXIF orientation is missing or unreadable. The final Codex verification pass then stopped finding design/correctness issues in the code and only wanted the detailed devlog entry to exist in the diff.
- Gemini / Design: Gemini reviewed the tightened diff and found the helper split, thumbnail-before-rotate ordering, and error-handling policy sound. Gemini explicitly agreed that leaving pre-fix baked `local-edits/` copies untouched is the safer product choice than silently migrating possibly user-compensated edits. Final Gemini verification returned no further findings.
- Claude / Design: early Claude review pushed the change away from one-off thumbnail math and toward a whole-pipeline orientation contract. Later Claude review flagged that the embedded-preview path still diverged from the full RAW path when EXIF orientation was missing or unreadable. The final code now keeps EXIF as the fast path but falls back to `rawimage.orientation` on that degraded branch so preview and full decode stay aligned.
- Codex / Test coverage: Codex first asked for broader pipeline coverage and pointed out that some tests only proved axis swaps. I responded by comparing rotated-file output against rotated copies of the normal-orientation baseline, strengthening transpose coverage with a 2x2 pixel grid, adding a pipeline-level horizontal-flip regression, and later adding a literal rotate-90 helper test plus policy tests for metadata-vs-fallback precedence. After those additions, Codex no longer raised coverage issues.
- Gemini / Test coverage: Gemini accepted the expanded orientation suite as comprehensive after the handedness checks, mirror coverage, and policy tests landed. No later coverage issues were reported.
- Claude / Test coverage: Claude first flagged brittle hard-coded dimension expectations and missing embedded-preview coverage. After the first fix, Claude later asked for a literal rotate-90 helper test because the shared helper was being reused on both sides of some baseline-rotation assertions. Added the explicit rotate-90 helper test plus metadata-precedence/fallback policy tests; the remaining Claude concerns became design tradeoffs rather than missing regressions.
- Codex / Correctness: Codex initially confirmed the cache-version bump, warning path, and shared orientation helpers were headed in the right direction, then flagged the missing-EXIF embedded-preview divergence as the remaining correctness hole. That hole is now closed by the sensor-orientation fallback on the embedded path. The baked-local-edits persistence note remained a documented product-scope tradeoff rather than a code defect for this user report.
- Gemini / Correctness: Gemini agreed the code now fixes the reported portrait RAW issue and explicitly did not re-raise the logical-size invariant that had been rejected earlier, because `decode_image()` intentionally returns the decoded GPU/upload buffer size while `source_dimensions()` reports the logical display size.
- Claude / Correctness: Claude's first pass caught three real issues: the decoded-cache contract needed a version bump, metadata-read failures needed a warning instead of silent swallowing, and the later pipeline still needed consistent orientation resolution across embedded/full/dimension paths. Those are all fixed in the final diff. Claude's last substantive correctness concern was the same missing-EXIF embedded-preview asymmetry that Codex later echoed; that is now fixed.
- Codex / Clean code / typing / efficiency / memory leaks: Codex was satisfied once the orientation logic converged into small helpers and the tests stopped depending on brittle constants. No leak or typing issues were reported.
- Gemini / Clean code / typing / efficiency / memory leaks: Gemini found the helpers idiomatic and the downscale-before-rotate choice efficient because the expensive orientation transforms operate on the bounded buffer rather than the full RAW dimensions. No later clean-code issues were reported.
- Claude / Clean code / typing / efficiency / memory leaks: Claude asked for the orientation policy to stay explicit and for the helper tests to pin the non-obvious transforms. The final code keeps the helper surface small, documents the hot-path fallback rule, and pins rotate-90, rotate-180, and transpose explicitly. No memory-management issues were raised.
- Codex / Documentation: Codex repeatedly called out that the detailed devlog entry for this task was missing from the diff even after the summary and lessons were updated. This entry resolves that documentation gap.
- Gemini / Documentation: Gemini accepted the broadened lesson and summary update, and it did not raise additional documentation issues after the final code changes.
- Claude / Documentation: Claude asked early for the lesson to describe the whole RAW pipeline rather than only thumbnail rendering, which is why `docs/learning/lessons.md` now talks about embedded previews, full decode output, and reported source dimensions together. Claude also expected the detailed devlog to carry the durable reviewer/validation record; this entry closes that loop.
**Result:** Success - portrait RAW thumbnails now preserve the real image aspect ratio because the RAW decode pipeline orients the pixels and logical dimensions consistently before those images ever reach the square thumbnail slot. The specific real-world regression file `DSC01169.ARW` now renders as portrait instead of squashed landscape in Library, and the orientation contract is covered across thumbnail, embedded-preview, full-image, dimension-reporting, and helper-mapping regressions.
**Files changed:** `src/decode.rs`, `docs/devlog/summary.md`, `docs/learning/lessons.md`, `docs/devlog/detailed/2026-04-23_2026-04-23.md`
**Reasoning:** The visible bug was not the square-slot containment helper anymore; it was that the RAW pipeline was handing the UI the wrong width/height contract and un-oriented embedded pixels. Fixing the decode seam is the smallest durable change because it repairs the user-visible thumbnail, the staged preview path, the full RAW load, and the reported logical dimensions together instead of teaching each UI surface its own RAW-specific workaround. The embedded-path fallback only broadens to `rawimage.orientation` when EXIF orientation is missing or unreadable so normal thumbnail performance stays on the cheap metadata path.
**Notes:** Visual verification used the real `C:\Users\38909\Documents\images\DSC01169.ARW` file with temporary deterministic before/after artifacts and a pixel diff generated under `tmp/thumbnail-ground-truth`, which were treated as disposable verification artifacts rather than permanent repo ground truths. Full validation passed with `cargo test` (272 tests), `cargo clippy -- -D warnings`, and `cargo build --release`. During the review loop I also wrote temporary reviewer-history/output files under `code_review/` so Codex, Gemini, and Claude could see the earlier findings; those files were intended to be deleted once this durable summary existed. The only intentionally unresolved tradeoff is that already-baked repo-local edited copies under `local-edits/` are left untouched, because automatically migrating them could double-rotate images for users who had already manually compensated before this fix.

## [2026-04-23 01:13 PDT] - Repair baked Library thumbnails that were persisted square
**Action:** Investigated the follow-up Library thumbnail report against the real `C:\Users\38909\Documents\images\DSC01169.ARW` file and verified that the RAW `decode_thumbnail(...)` path was already aspect-correct. The remaining bug lived later in `src/main.rs`: `thumbnail_from_rendered_image(...)` used `image::imageops::thumbnail(max_dim, max_dim)`, which produced square baked thumbs for large rendered images, and `load_library_thumbnail_base_image(...)` trusted those persisted thumbs whenever generation ids still matched. Replaced that helper with explicit max-axis resize math via `thumbnail_dimensions_for_image(...)`, taught Library to compare a persisted thumb's dimensions against the thumbnail shape expected from its matching full baked copy, regenerate/rewrite stale thumbs from the full copy, and added regressions for same-generation aspect-mismatch repair, generation mismatch with coincident dimensions, portrait downscaling, and already-in-bounds no-op behavior. Updated the debugging note, summary, and lessons.
**Code reviewer comments:**
- Codex / Design: the first Codex pass flagged that my initial repair logic always resized the full baked image before checking whether the persisted thumb was already valid. I fixed that by computing expected dimensions first and only deriving/resizing on mismatch. The follow-up Codex verification found no remaining code issue beyond the stale summary test count and missing detailed devlog entry, both of which this entry resolves.
- Gemini / Design: Gemini review did not complete in this desktop session. The repo-requested `gemini-3-pro` invocation failed with `ModelNotFoundError`, and a fallback attempt without an explicit model failed with `ERR_SSL_SSL/TLS_ALERT_BAD_RECORD_MAC`.
- Claude / Design: Claude's first pass agreed the root fix was in the baked-thumbnail seam rather than another RAW decode path. It asked for the dimension helper to become more explicit and for the read-time repair path to make its tradeoffs visible. The helper now uses integer math, and the repair write logs a warning on failure. Claude's second pass found no blocking design issue.
- Codex / Test coverage: the first Codex pass correctly pointed out that the repair regression only proved dimensions and used uniform black pixels. I switched the full-image fixture to patterned pixels, asserted repaired pixel equality against the independently derived expected thumbnail, and later added the generation-mismatch, portrait, and already-in-bounds regressions. Verification then stopped finding coverage gaps beyond docs bookkeeping.
- Gemini / Test coverage: no usable review result because Gemini did not complete a successful pass in this session.
- Claude / Test coverage: the first Claude pass asked for portrait and already-in-bounds coverage and noted the missing generation-mismatch case where dimensions still coincide. I added all three. The second Claude pass treated the remaining ideas as nice-to-have future-proofing rather than blockers.
- Codex / Correctness: after the code fixes above, Codex no longer raised correctness issues in `src/main.rs`; its remaining verification notes were the summary count and missing detailed devlog entry.
- Gemini / Correctness: blocked by the failed Gemini review attempts above.
- Claude / Correctness: Claude's first pass said the core repair path was correct but asked for repair-write failures to stop being silent. The code now logs `warn!` when rewriting a stale persisted thumb fails. Claude's second pass found no blocking correctness issue.
- Codex / Clean code / typing / efficiency / memory leaks: Codex asked to keep the happy path cheap, which is why `thumbnail_dimensions_for_image(...)` now validates thumb dimensions before any full-image resize. The final Codex pass did not raise further clean-code issues in the implementation.
- Gemini / Clean code / typing / efficiency / memory leaks: blocked by the failed Gemini review attempts above.
- Claude / Clean code / typing / efficiency / memory leaks: Claude highlighted the float-based helper and silent repair writes in the first pass. Those are now integer-based and logged. The second pass left only a non-blocking follow-up about reducing clone count in a future optimization pass.
- Codex / Documentation: final Codex verification said the only remaining doc gaps were the stale summary test count and the missing detailed devlog entry for this task. The summary now says `276 unit tests currently pass`, and this entry provides the durable reviewer/validation record.
- Gemini / Documentation: blocked by the failed Gemini review attempts above.
- Claude / Documentation: Claude's second pass agreed the summary/lessons/debugging updates matched the code and only asked for the missing detailed devlog entry and corrected test count. This entry closes that loop.
**Result:** Success - baked local-edit Library thumbnails now preserve aspect ratio when they are generated, stale same-generation thumbs are repaired from their matching full baked copy, and the real `DSC01169.ARW` baked thumb on disk repaired from square `200 x 200` to aspect-correct `200 x 133`.
**Files changed:** `src/main.rs`, `docs/debugging/2026-04-23-library-thumbnail-aspect-ratio.md`, `docs/devlog/summary.md`, `docs/learning/lessons.md`, `docs/devlog/detailed/2026-04-23_2026-04-23.md`
**Reasoning:** The new screenshot looked like another RAW-orientation regression at first, but the repo evidence showed the RAW thumbnail decode path was already correct. The actual failing seam was the baked-thumb helper plus Library's persisted-thumb trust rule. Fixing that later seam is the smallest durable change because it repairs future baked thumbnails and self-heals already-persisted bad thumbs without altering the user's baked full image.
**Notes:** Visual verification used the real `C:\Users\38909\Documents\images\DSC01169.ARW` file and temporary artifacts under `tmp/debug-real-thumbnails/`: `DSC01169-local-edit-thumb-before.png`, `DSC01169-library-thumb-after.png`, `DSC01169-library-thumb-after-slot.png`, and `DSC01169-library-thumb-slot-diff.png`. Full validation passed with `cargo test` (276 tests), `cargo clippy -- -D warnings`, and `cargo build --release`. Reviewer temp files were kept under `code_review/` during the loop. Gemini coverage remained blocked by external CLI/model failures (`ModelNotFoundError` for `gemini-3-pro`, then an SSL `bad record mac` failure on fallback), and that gap was recorded rather than treated as successful review coverage.

## [2026-04-23 01:38 PDT] - Harden baked thumbnail repair after the final review loop
**Action:** Addressed the last substantive Codex/Claude review points in `src/main.rs` by shrinking the stale-thumbnail repair lock to only the decision/finalize phases, moving the expensive resize itself outside the global local-edit cache mutex, making invalid-cache cleanup skip deletion whenever another cache operation already owns that mutex, renaming `LoadedLocalEditCacheVariantHeader.file_size` to `source_file_size`, renaming the repair-lock regression to match what it actually proves, and strengthening the generation-mismatch regression so it now forces a real downscale instead of an identity-sized case. Synced the summary/debugging notes to the finished `278`-test state.
**Code reviewer comments:**
- Codex / Design: Codex's final pass still flagged two meaningful issues after the earlier fixes: invalid-cache deletion on read was still happening outside the cache IO lock, and the stale-thumb repair lock still covered full-image resize work. I changed invalid-cache cleanup to only remove when that lock is immediately available and split repair into a locked decision phase, an unlocked resize phase, and a locked finalize/write phase. Codex's remaining latency note is now the intentionally accepted tradeoff that stale-thumb rewrites still happen synchronously once per stale cache entry.
- Gemini / Design: the repo-requested final Gemini verification again failed externally with `ModelNotFoundError` for `gemini-3-pro`, so there was no usable Gemini result for this last round.
- Claude / Design: Claude's final review agreed the earlier best-effort repair-write change was correct, then called out the same unlocked-delete window plus documentation drift around the final test count. The delete path is now contention-aware, and the final docs now reflect the `278`-test validation state.
- Codex / Test coverage: Codex asked for the generation-mismatch regression to prove a true thumbnail regeneration rather than an identity-sized return path. I changed that fixture to a `300x200` full image with a stale `200x133` thumbnail so the assertion now pins a real downscale. Codex also wanted the repair-lock test name to match what it actually simulates, which is now reflected in `library_thumbnail_rechecks_local_edit_cache_inside_the_repair_lock`.
- Gemini / Test coverage: no usable result because Gemini never produced a successful final review.
- Claude / Test coverage: Claude highlighted the same weak generation-mismatch fixture and misleading test name in the final round. Both are now tightened.
- Codex / Correctness: after the earlier best-effort repair-write fix, Codex's remaining correctness concern was the unlocked invalid-cache delete. That path is now guarded by a non-blocking attempt to take the cache IO lock before removal, so readers stop deleting cache files during another cache operation's critical section.
- Gemini / Correctness: blocked by the failed Gemini verification attempt above.
- Claude / Correctness: Claude's final correctness feedback matched Codex on the unlocked invalid-cache delete window; the current code now skips cleanup instead of racing another cache operation when the lock is busy.
- Codex / Clean code / typing / efficiency / memory leaks: Codex called out the over-wide repair lock and the confusing cache-header naming. The lock now excludes resize work, and the header now carries `source_file_size`, which is the actual value propagated into `ImageData`.
- Gemini / Clean code / typing / efficiency / memory leaks: blocked by the failed Gemini verification attempt above.
- Claude / Clean code / typing / efficiency / memory leaks: Claude also pushed for the clearer header naming and the tighter repair-lock scope; both landed in the final code.
- Codex / Documentation: Codex correctly noted that the summary had advanced to `278` tests while the earlier 01:13 entry still described the pre-hardening `276`-test state. This entry records the later hardening pass and its final validation numbers so the chronology is explicit instead of silently rewriting the earlier entry.
- Gemini / Documentation: blocked by the failed Gemini verification attempt above.
- Claude / Documentation: Claude's final documentation note was the same test-count drift. The summary/debugging notes now say `278`, and this entry records why that final count is higher than the earlier 01:13 milestone.
**Result:** Success - Library thumbnails now preserve the original image aspect ratio for baked local edits, stale square thumbs self-heal from their matching full baked copy, repair-write failure can no longer blank the visible thumbnail, and the repair lock no longer holds the expensive resize itself on the hot path.
**Files changed:** `src/main.rs`, `docs/debugging/2026-04-23-library-thumbnail-aspect-ratio.md`, `docs/devlog/summary.md`, `docs/learning/lessons.md`, `docs/devlog/detailed/2026-04-23_2026-04-23.md`
**Reasoning:** The first code fix solved the visible bug, but the final review loop surfaced two real durability issues: cache cleanup could still race another cache operation, and the repair mutex still wrapped the heaviest work. Tightening those paths without changing the user-facing mechanics keeps the narrow thumbnail fix intact while making the local-edit cache behavior safer under concurrent read/write pressure.
**Notes:** Final validation passed with `cargo test` (`278` tests), `cargo clippy -- -D warnings`, and `cargo build --release`. Reviewer temp files under `code_review/` and the visual/debug artifacts under `tmp/debug-real-thumbnails/` remained disposable cleanup targets after this durable entry was written. Gemini coverage was still blocked by external CLI/model failures rather than project code issues.

## [2026-04-23 02:04 PDT] - Close the last cache-reader races from the final reviewer pass
**Action:** Responded to the last Codex/Claude round by tightening `src/main.rs` again: both persisted-cache readers now get `cache_file_len` from the opened file handle instead of a separate `metadata(path)` call, the test-only repair hooks are cleared at the start and end of `with_test_photo_repo_root(...)`, the outer Library fast path now checks the thumbnail via header-only validation before deserializing pixels, and `load_repaired_local_edit_thumbnail(...)` now treats a mid-flight generation change as a retry instead of ever painting a stale precomputed thumbnail over a newer baked edit. Updated the summary/debugging notes to match that final state.
**Code reviewer comments:**
- Codex / Design: Codex's sixth pass flagged two remaining real bugs: the header readers were still vulnerable to a metadata/open TOCTOU that could delete a freshly written cache file, and the split repair path could still return stale precomputed pixels to the UI if a newer generation landed after the resize. Reading cache length from the open handle closes the first bug, and the finalize phase now retries rather than returning stale precomputed pixels when the generation changes.
- Gemini / Design: the final Gemini attempt again failed externally with `ModelNotFoundError` for `gemini-3-pro`, so there was still no usable Gemini verification result for this round.
- Claude / Design: Claude agreed the earlier hardening had improved the shape of the code, then called out the same TOCTOU reader bug plus the lack of teardown around the process-global test hooks. The file-handle length check and hook clearing inside `with_test_photo_repo_root(...)` address those remaining correctness traps.
- Codex / Test coverage: Codex did not ask for new behavior coverage beyond the existing repair, write-failure, and generation-mismatch regressions once the retry-on-generation-change logic landed. The strengthened `300x200 -> 200x133` generation-mismatch fixture and the renamed in-lock recheck test remain the durable coverage for this seam.
- Gemini / Test coverage: blocked by the failed Gemini verification attempt above.
- Claude / Test coverage: Claude's sixth pass said the visible repair coverage was already strong and only warned that the process-global hooks needed teardown. The hook slots are now cleared automatically around every `with_test_photo_repo_root(...)` scope.
- Codex / Correctness: after the 01:38 hardening pass, Codex's highest-priority remaining issue was the cache-reader TOCTOU and the UI-stale-generation return path. Both are now fixed.
- Gemini / Correctness: blocked by the failed Gemini verification attempt above.
- Claude / Correctness: Claude's final correctness concern matched Codex on the TOCTOU reader bug and separately pointed out the test-hook leak risk. The current code now reads length from the same opened file handle it validates and clears both repair hooks before and after each repo-root-scoped test.
- Codex / Clean code / typing / efficiency / memory leaks: Codex also asked the outer Library fast path to stay header-only until a thumb is proven usable, which is now true for the thumbnail as well as the full entry. The remaining duplicate-read and full-buffer-clone observations are acknowledged follow-up cleanup, not blockers for this bugfix.
- Gemini / Clean code / typing / efficiency / memory leaks: blocked by the failed Gemini verification attempt above.
- Claude / Clean code / typing / efficiency / memory leaks: Claude's final pass still noted the accepted process-wide cache mutex tradeoff and some duplicate-read/clone cleanup ideas, but after the handle-based validation and retry change those were explicitly follow-up-level concerns rather than ship blockers.
- Codex / Documentation: after the 01:38 entry fixed the `276` vs `278` count drift, Codex's remaining documentation ask was simply that this last correctness pass be recorded instead of silently folded into older notes. This entry provides that durable closeout.
- Gemini / Documentation: blocked by the failed Gemini verification attempt above.
- Claude / Documentation: Claude likewise wanted the final review loop recorded as a separate chronological entry once the last race fixes landed.
**Result:** Success - the baked-thumbnail repair path now preserves the visible aspect-ratio fix, keeps repair writes best-effort, avoids deleting freshly written cache files because of a reader race, and retries rather than repainting stale pixels when a newer baked generation lands mid-load.
**Files changed:** `src/main.rs`, `docs/debugging/2026-04-23-library-thumbnail-aspect-ratio.md`, `docs/devlog/summary.md`, `docs/devlog/detailed/2026-04-23_2026-04-23.md`
**Reasoning:** The visible Library bug was already fixed, but the last review pass still found two concurrency/durability holes in the surrounding cache plumbing. Closing those without widening product scope keeps the user-facing bugfix intact while making the local-edit cache behavior safer under parallel read/write pressure and future test maintenance.
**Notes:** Validation after this final pass again succeeded with `cargo test` (`278` tests), `cargo clippy -- -D warnings`, and `cargo build --release`. Reviewer temp files under `code_review/` and the verification artifacts under `tmp/debug-real-thumbnails/` remained disposable once this final entry existed. Gemini review coverage remained blocked by external CLI/model failures rather than project code.

## [2026-04-23 02:29 PDT] - Remove path-delete cleanup and harden retry exhaustion
**Action:** Closed the last meaningful reviewer concerns in `src/main.rs` by removing path-based invalid-cache deletion from the readers entirely, moving the cache-file-length gate ahead of the cached-path allocation so malformed `path_len` values fail before allocation pressure, expanding the repair retry budget, and adding a final "current baked state" recheck so repeated generation churn now returns the latest usable baked thumbnail (or re-derives from the current full baked copy) instead of ever dropping back to the original source thumbnail. Updated the summary, debugging note, and lesson to reflect that final fail-closed behavior.
**Code reviewer comments:**
- Codex / Design: Codex's eighth pass still objected to deleting invalid caches by path after a failed read because a writer could legitimately replace that path before cleanup ran. I removed the delete-on-read behavior entirely so failed cache reads now fail closed instead of trying to clean up by path. Codex also wanted the repeated-retry fallback to stop ever exposing the original source thumbnail over a baked edit; the retry tail now rechecks the current baked state and, if needed, derives from the current full baked copy before giving up.
- Gemini / Design: the final Gemini attempt remained blocked externally by `ModelNotFoundError` for `gemini-3-pro`, so there was still no usable Gemini result for this round.
- Claude / Design: Claude had also flagged the retry exhaustion path and the early `path_len` allocation. The current cache parser now validates the full expected file length before allocating the cached path buffer, and the repair path no longer falls back to `decode_thumbnail(source)` after generation churn.
- Codex / Test coverage: Codex did not ask for new surface-area regressions once the retry tail stopped falling back to the source thumbnail; the existing generation-mismatch, write-failure, and repair-lock coverage still pins the behavior that users actually see.
- Gemini / Test coverage: blocked by the failed Gemini verification attempt above.
- Claude / Test coverage: Claude's earlier note that the retry branch itself was not directly exercised remains a follow-up opportunity, but after the retry exhaustion logic stopped exposing a wrong source thumbnail it no longer looked like a ship blocker for this bugfix.
- Codex / Correctness: after the 02:04 pass, Codex's remaining correctness issues were exactly the path-delete race and the too-short retry exhaustion. Both are now fixed.
- Gemini / Correctness: blocked by the failed Gemini verification attempt above.
- Claude / Correctness: Claude's final correctness concerns matched Codex on delete-by-path and retry exhaustion; the current code now fails cache reads closed and rechecks the latest baked state before returning.
- Codex / Clean code / typing / efficiency / memory leaks: Codex still noted some duplicate-read and full-buffer-clone follow-up cleanup ideas, but after this pass those were clearly performance polish rather than durability bugs.
- Gemini / Clean code / typing / efficiency / memory leaks: blocked by the failed Gemini verification attempt above.
- Claude / Clean code / typing / efficiency / memory leaks: Claude likewise still had performance follow-up ideas around the repair slow path, but no new correctness blocker remained after this pass.
- Codex / Documentation: Codex wanted the final fail-closed/read-cleanup policy reflected in the durable docs instead of living only in code. The summary, debugging note, lesson, and this entry now say that invalid-cache reads fail closed rather than deleting by path.
- Gemini / Documentation: blocked by the failed Gemini verification attempt above.
- Claude / Documentation: Claude's remaining documentation ask was the same: record the final closeout as a new chronological entry rather than silently mutating older notes.
**Result:** Success - the Library thumbnail fix now survives stale square baked thumbs, transient repair-write failures, repeated generation churn, and invalid-cache read races without repainting the original unedited source thumbnail over a baked local edit.
**Files changed:** `src/main.rs`, `docs/debugging/2026-04-23-library-thumbnail-aspect-ratio.md`, `docs/devlog/summary.md`, `docs/learning/lessons.md`, `docs/devlog/detailed/2026-04-23_2026-04-23.md`
**Reasoning:** The visible bug was fixed earlier, but the last review rounds kept surfacing secondary durability risks in the local-edit cache plumbing. Removing read-time deletion entirely and making retry exhaustion prefer the current baked state keeps the UI honest under churn while staying faithful to the narrow bug report.
**Notes:** Validation after this pass again succeeded with `cargo test` (`278` tests), `cargo clippy -- -D warnings`, and `cargo build --release`. The remaining reviewer comments were follow-up-level performance cleanup ideas, not newly discovered correctness regressions. Gemini coverage remained blocked by external CLI/model failures rather than project code.

## [2026-04-23 03:28 PDT] - Restore truthful Detail status dimensions for reopened baked local edits
**Action:** Investigated the follow-up Detail screenshot against the real `C:\Users\38909\Documents\images\DSC01169.ARW` file and confirmed the bottom status bar was reading the repo-local baked Full cache's pixel size (`16384 x 10923`) instead of the image's logical dimensions (`6656 x 9728`). Fixed that in `src/main.rs` by extending the repo-local `local-edits` cache schema to v3 so Full entries persist logical dimensions alongside baked pixels, computing those logical dimensions when persisting a baked edit, restoring them through a new `load_full_image(...)` helper on persisted-local-edit reopens, and keeping schema-v2 caches readable with a best-effort fallback that preserves baked crop/rotation geometry unless the old baked buffer clearly exceeds the source dimensions. While addressing reviewer feedback, also tightened the unlocked Library thumbnail fast path so it rechecks the loaded thumbnail entry against the matching Full generation before returning, made the repair loop fail closed when a baked edit disappears mid-retry, hardened the legacy heuristic for rotated non-square v2 caches, replaced the future-fragile schema-version constant arms with explicit literals, added a zero-dimension guard for `thumbnail_dimensions_for_image(...)`, and expanded regression coverage for schema-v3 persisted logical dimensions, legacy v2 oversized/cropped/rotated cases, the healthy fast-path race, and zero-dimension thumbnail math. Updated the debugging note, summary, and lessons.
**Code reviewer comments:**
- Codex / Design: the first Codex pass found three real issues in the new local-edit logical-dimensions work: the unlocked Library fast path could still return a mixed-generation thumbnail, the repair loop could return a stale derived thumbnail after the baked edit disappeared, and the first legacy-v2 heuristic was too broad for same-aspect crops. I fixed the fast path by rechecking the loaded thumbnail entry against the matching Full generation/dimensions, changed the repair path to fail closed when the baked edit disappears, and narrowed the legacy fallback so it now only falls back to source dimensions when the old baked buffer clearly exceeds the source. Two later `codex exec review --uncommitted` verification attempts timed out in this desktop environment, so there is no final Codex "clean" pass beyond those addressed findings.
- Gemini / Design: Gemini review never produced a usable result in this session. The repo-requested `gemini-3-pro` invocation failed again with `ModelNotFoundError`, so there was no Gemini design verification to act on.
- Claude / Design: Claude's first pass agreed the cache-schema approach was the right seam, then flagged a future-fragile version gate (`schema_version >= LOCAL_EDIT_CACHE_SCHEMA_VERSION`), the legacy rotated-v2 heuristic, and the need to make the remaining corrupt-cache read behavior a deliberate product tradeoff instead of an accident. I switched the parser to explicit schema literals, added the rotated-v2 regression and matching heuristic branch, and kept read-time deletion disabled as an explicit fail-closed tradeoff because blind path deletion can race a newer writer. The final Claude pass left the corrupt-cache cleanup policy, process-local lock scope, and retry logging as follow-up tradeoffs rather than new user-visible blockers.
- Codex / Test coverage: Codex asked for coverage of the healthy unlocked thumbnail fast path, not just the repair path, and it pushed back on the first legacy-v2 tests because they did not cover same-aspect geometry. I added `library_thumbnail_fast_path_rechecks_generation_before_returning`, plus legacy-v2 regressions for oversized baked pixels, preserved-aspect crops, changed-aspect crops, and rotated non-square images.
- Gemini / Test coverage: blocked by the failed Gemini review attempts above.
- Claude / Test coverage: Claude's later pass asked for a rotated-v2 regression and highlighted the zero-dimension edge in `thumbnail_dimensions_for_image(...)`. Both are now covered by focused tests.
- Codex / Correctness: Codex's substantive correctness feedback matched the three issues above; once those were fixed, the remaining Codex verification attempts timed out before yielding new findings.
- Gemini / Correctness: blocked by the failed Gemini review attempts above.
- Claude / Correctness: Claude's first pass also caught the future schema-version parse bug that would have broken v3 readers after the next schema bump, and the final pass called out the zero-dimension panic hazard in thumbnail math. Those are both fixed. Claude still prefers a safe corrupt-cache cleanup path in the future, but that remains a documented tradeoff rather than a bug I was willing to reintroduce by deleting cache files by path.
- Codex / Clean code / typing / efficiency / memory leaks: Codex accepted the `load_full_image(...)` extraction and the logical-dimensions cache schema once the races above were closed. The only remaining Codex friction was the timed-out verification pass itself.
- Gemini / Clean code / typing / efficiency / memory leaks: blocked by the failed Gemini review attempts above.
- Claude / Clean code / typing / efficiency / memory leaks: Claude's final pass left only follow-up-level concerns about the process-local cache mutex, retry logging, and possible future refactors of the long repair function. No new clean-code blocker remained after the schema-literal, rotated-v2, and zero-dimension fixes.
- Codex / Documentation: Codex did not review the final docs because both verification attempts timed out before reaching a conclusive second pass. The initial review did not raise documentation-specific blockers beyond wanting the durable record kept current after the code fixes.
- Gemini / Documentation: blocked by the failed Gemini review attempts above.
- Claude / Documentation: Claude noted that the docs were still outside the reviewed diff and should explicitly record the schema-v3 local-edit logical-dimensions contract, the legacy-v2 best-effort fallback, and the remaining corrupt-cache cleanup tradeoff. This entry plus the summary, lessons, and debugging note now cover that.
**Result:** Success - reopened baked local edits now report truthful logical dimensions in the Detail status bar instead of the baked pixel-buffer size, the concrete `DSC01169.ARW` mismatch is handled by the legacy oversized-buffer fallback, and the surrounding thumbnail cache fast path is hardened against another mixed-generation race.
**Files changed:** `src/main.rs`, `docs/debugging/2026-04-23-detail-status-resolution.md`, `docs/devlog/summary.md`, `docs/learning/lessons.md`, `docs/devlog/detailed/2026-04-23_2026-04-23.md`
**Reasoning:** The visible bug was not in the status-bar formatter; it was that reopened baked local edits had no persisted logical-dimensions contract, so the app reused whatever baked pixel size happened to be on disk. Persisting logical dimensions in the local-edit cache is the smallest durable fix because it keeps new reopens honest without re-reading geometry from the UI path, while the legacy-v2 fallback stays explicitly best-effort because the old cache format never stored enough information to reconstruct every edited shape perfectly.
**Notes:** Final validation passed with `cargo test` (`285` tests), `cargo clippy -- -D warnings`, and `cargo build --release`. Manual investigation used the real `C:\Users\38909\Documents\images\DSC01169.ARW` file, Windows shell metadata (`6656 x 9728`), and the repo-local baked Full cache header (`16384 x 10923`) to pin the mismatch; I did not rerun a UI screenshot/pixel-diff capture for this text-only status-bar fix. Gemini review coverage remained blocked by external CLI/model failures, and both Codex verification attempts timed out in this desktop session after the first useful pass. I left the pre-existing unrelated dirty change in `src/decode.rs` and the earlier untracked `docs/debugging/2026-04-23-library-thumbnail-aspect-ratio.md` note alone.

## [2026-04-23 11:06 PDT] - Skip the generic reload when Library reopens the current Detail image
**Action:** Investigated the new "entering and exiting and entering details view takes a long time" report after confirming the in-memory cache itself was working. The remaining latency came from `Message::LibraryItemClicked` in `src/main.rs`: even when `displayed_full_image_for_path(...)` proved the already displayed full image was still current, the Library double-click path still fell through to `start_load(path)`, which cleared transient state, bumped load bookkeeping, and rebuilt the reopen around the generic load path. I used the planned worker subagent to implement a narrow TDD fix in `src/main.rs`: add `try_reopen_current_library_image_without_reload(...)` so same-image Library returns short-circuit straight back into Detail, factor the shared reset behavior into `reset_transient_detail_reopen_state()`, and reuse that same reset helper before the metadata-changed fallback load so the same reopen gesture keeps one UI contract. I also strengthened the reopen regressions to prove the true fast path leaves `detail_load.request_id` and `image_id` unchanged, keeps EXIF warm, still supports a real `edit::save_edited_image(...)` follow-up after the source file disappears, and falls back to a fresh load on a same-size source rewrite. Updated the debugging note, summary, and lessons once the code and review loop settled.
**Code reviewer comments:**
- Codex / Design: the first Codex review found one real design issue in the initial fast path: the no-reload branch reset `error`, `save_status`, `zoom`, `offset`, and `crop_mode`, but the metadata-changed fallback for the same Library reopen gesture left that transient state untouched until the new load completed. I fixed that by extracting `reset_transient_detail_reopen_state()` and calling it in both paths. The final Codex verification accepted the overall design and left only one low-severity follow-up note: `displayed_full_image_for_path(...)` still does a synchronous metadata stat on the UI reopen path.
- Gemini / Design: the repo-requested Gemini review was blocked externally by `ModelNotFoundError` for `gemini-3-pro`, so there was no usable Gemini design review to act on in this session.
- Claude / Design: both focused Claude verification attempts timed out in this desktop environment without returning findings.
- Codex / Test coverage: Codex first pointed out that my changed-source regression only proved a rewrite with different file length, and later asked for proof that a deleted-source reopen still supports a real follow-up action instead of only asserting `current_save_request().is_some()`. I tightened the fallback regression to use a same-size rewrite (`raw` -> `new`) and extended the deleted-source reopen regression to build a save request, call `edit::save_edited_image(...)`, and assert that the exported PNG is actually written. Final Codex verification did not raise new coverage gaps.
- Gemini / Test coverage: blocked by the failed Gemini review attempt above.
- Claude / Test coverage: both focused Claude verification attempts timed out without returning findings.
- Codex / Correctness: Codex confirmed that the user-visible latency complaint was real even though the cache existed, because the reopen path was still doing avoidable work after the cache-hit eligibility check. Its two substantive correctness concerns were the inconsistent transient-state reset and the missing proof that keeping pixels alive after a deleted-source reopen still permits a safe follow-up action. Both are now fixed; the only remaining Codex note is the non-blocking synchronous metadata stat on the UI reopen path.
- Gemini / Correctness: blocked by the failed Gemini review attempt above.
- Claude / Correctness: both focused Claude verification attempts timed out without returning findings.
- Codex / Clean code / typing / efficiency / memory leaks: Codex accepted the narrow helper split in the final diff because it keeps the optimization localized to the Library double-click path and avoids widening the generic load machinery. The remaining metadata-stat note was explicitly treated as follow-up performance work rather than a blocker for this change.
- Gemini / Clean code / typing / efficiency / memory leaks: blocked by the failed Gemini review attempt above.
- Claude / Clean code / typing / efficiency / memory leaks: both focused Claude verification attempts timed out without returning findings.
- Codex / Documentation: Codex expected the durable record to say clearly that the cache was already working, that the fix was to bypass the generic reload path for the already displayed full image, and that the one remaining performance follow-up is the synchronous metadata stat. This entry plus the summary, lesson, and debugging note now cover that.
- Gemini / Documentation: blocked by the failed Gemini review attempt above.
- Claude / Documentation: both focused Claude verification attempts timed out without returning findings.
**Result:** Success - returning from Library to the same fully loaded Detail image now comes back immediately when the currently displayed full image is still valid. The reopen no longer starts a new load, no longer bumps `detail_load.request_id` or `image_id`, and no longer throws away warm EXIF or saveable pixels just to redisplay the same image. Same-size source rewrites still invalidate that fast path and fall back to a real load.
**Files changed:** `src/main.rs`, `docs/debugging/2026-04-23-detail-reopen-latency.md`, `docs/devlog/summary.md`, `docs/learning/lessons.md`, `docs/devlog/detailed/2026-04-23_2026-04-23.md`
**Reasoning:** The user's complaint was not that the cache layer was absent; it was that the already displayed image still went through the generic reopen machinery after the cache had effectively said "yes, this image is still good." Short-circuiting before `start_load(...)` is the smallest durable fix because it improves perceived latency exactly where the user feels it without weakening the same-size-rewrite correctness guard or changing the normal decode/cache behavior for real reloads.
**Notes:** Validation passed with targeted reopen regressions (`cargo test library_reopen_ -- --nocapture`, `cargo test reopening_a_recently_viewed_detail_image_reuses_the_session_memory_cache -- --nocapture`, `cargo test displayed_full_image_fast_path_does_not_reuse_a_stale_base_source -- --nocapture`, `cargo test repeat_raw_open_ -- --nocapture`), then the full suite (`cargo test`, `286` tests), `cargo clippy -- -D warnings`, and `cargo build --release`. I preserved the pre-existing unrelated dirty change in `src/decode.rs` and the earlier debugging/devlog work already present in the tree. Reviewer temp files under `code_review/` were kept only long enough to feed the review loop and can now be deleted because this entry captures the durable reviewer history.

## [2026-04-23 13:42 PDT] - Clear Library drag state when entering Detail
**Action:** Investigated the screenshot showing a floating thumbnail following the cursor over the Detail editor. The root cause lived in `src/main.rs`: `Message::LibraryItemClicked` always seeded `drag_state`, and neither the normal `start_load(...)` path nor the same-image `try_reopen_current_library_image_without_reload(...)` path cleared that Library-only state before Detail reopened. I wrote the regressions first so the bug failed as `assert!(app.drag_state.is_none())` in both the fresh-load and fast-reopen paths, then added a narrow `clear_library_drag_state()` helper and called it from those two Detail-entry seams. I recorded the debugging trail, updated the summary/lesson, and kept the fix scoped away from Library drag-drop behavior itself.
**Code reviewer comments:**
- Codex / Design, Test coverage, Correctness, Clean code, Documentation: Attempted focused `codex exec` review twice plus `codex exec review --uncommitted`, but the local Codex CLI hung until timeout in this desktop session, so there was no usable Codex feedback to address.
- Gemini / Design, Test coverage, Correctness, Clean code, Documentation: Attempted the repo-requested `gemini -p ... -m gemini-3-pro` review, but the Gemini CLI returned `ModelNotFoundError`, so there was no usable Gemini feedback to address.
- Claude / Design, Test coverage, Correctness, Clean code, Documentation: Attempted focused `claude -p` review twice; the first run timed out, and a lean retry failed because the local Claude CLI was not logged in. There was no usable Claude feedback to address.
**Result:** Success - entering Detail from Library now cancels pending Library drag state, so the useless collection drag thumbnail no longer survives into the Detail editor on either the normal load path or the same-image fast reopen path.
**Files changed:** `src/main.rs`, `docs/debugging/2026-04-23-detail-drag-preview-overlay.md`, `docs/devlog/summary.md`, `docs/learning/lessons.md`, `docs/devlog/detailed/2026-04-23_2026-04-23.md`
**Reasoning:** The bug was not in thumbnail rendering or drag activation thresholds; it was a stale transient-state problem. Clearing Library-only drag state at the two existing Detail-entry seams is the smallest durable fix because it removes the unwanted overlay without changing the actual Library drag/drop contract.
**Notes:** Targeted regressions first failed on `assert!(app.drag_state.is_none())`, then passed after the fix. Final validation passed with `cargo test` (`287` tests), `cargo clippy -- -D warnings`, and `cargo build --release`. I used the user-provided screenshot at `C:\Users\38909\Documents\screenshots\Screenshot 2026-04-23 131130.png` as the before-state evidence, but I did not capture a live after screenshot or pixel diff from this shell because there is no session-local GUI automation/screenshot harness available here. I preserved the pre-existing unrelated dirty change in `src/decode.rs`.

## [2026-04-23 21:06 PDT] - Stop the slider commit path from freezing the UI on full-image renders
**Action:** Investigated the user-reported "app frozen when I double click on the exposure knob" symptom and the related observation that "rendering is fast as I drag the knob but slow when I let go." Traced the freeze in `src/main.rs` to `on_current_visible_render_changed(...)`, which called `rendered_thumbnail_image_for_local_edit_request(...)` synchronously on the UI thread for every committed edit (slider release, double-click reset, rotation, ExifLoaded auto-lens). That helper ran the full-resolution `edit::render_edited_image(...)` plus a downscale before queueing the background persist task, which then immediately repeated the same full render on its own thread - so every commit paid the heavy CPU render twice and blocked the UI for the synchronous half. Fixed it by changing `persist_local_edit(...)` to return the rendered thumbnail it was already generating as `Result<Option<Arc<ImageData>>, String>`, propagating that thumbnail through `Message::LocalEditPersistCompleted`, having the handler set the library thumbnail handle from the persisted task's output (or fall back to `refresh_library_thumbnail_for_path(...)` when the persist was a delete-only path), removing the synchronous render from `on_current_visible_render_changed(...)`, and deleting the now-unused `rendered_thumbnail_image_for_local_edit_request(...)` helper. The double-click reset behavior in `Message::SliderReleased` was already implemented; the freeze was downstream of it. Wrote new regressions first: `slider_double_click_release_resets_each_slider_kind` covers the basic reset for all twelve slider kinds, and `slider_double_click_release_defers_persist_when_clearing_an_existing_local_edit` uses `with_test_photo_repo_root` plus `persist_test_local_edit` to set up a real on-disk persisted edit, then asserts the double-click both queues a persist task to clear it and leaves the library thumbnail handle untouched until the persist completes. Updated the existing immediate-update tests (`rotate_clockwise_updates_library_thumbnail_after_persist_completes`, `exposure_commit_updates_library_thumbnail_after_persist_completes`, and the auto-lens ExifLoaded variant) to drive the persist completion via a new `complete_in_flight_persist_with_rendered_thumbnail(...)` test helper that mirrors what the background task does. Updated the summary and lessons.
**Code reviewer comments:** None - the user requested an immediate, efficient fix; no AI reviewer pass was run for this change.
**Result:** Success - the full test suite passes (`289` tests, up from `287`), `cargo clippy -- -D warnings` is clean, and `cargo build --release` succeeds. After the fix, every committed edit returns control to the UI in microseconds and the persist task does the one full-image render that actually has to happen, so the editor no longer freezes on slider release or on double-click reset of any knob.
**Files changed:** `src/main.rs`, `docs/devlog/summary.md`, `docs/learning/lessons.md`, `docs/devlog/detailed/2026-04-23_2026-04-23.md`
**Reasoning:** The user's two messages pointed at the same root cause: the commit path was doing the heavy work twice, once synchronously on the UI thread and once again on the persist worker. Returning the thumbnail from the worker that was already going to render it deletes the duplicate work, removes the UI-thread block, and keeps the library thumbnail update on a real (just slightly delayed) signal instead of an immediate but expensive synchronous path. This is the smallest durable change because it preserves the existing double-click reset mechanism, the persist queue/dedup behavior, and the eventual library thumbnail update contract; it only changes which thread owns the render and which message delivers the new thumbnail.
**Notes:** Validation: `cargo build --release` (clean), `cargo test` (`289` tests pass), `cargo clippy -- -D warnings` (clean). The release-mode `cargo test --release` flow was not used because an unrelated long-standing `iced::advanced::text::Editor` trait bound issue still rejects the existing release-test compile path; the debug `cargo test` profile is the project's normal regression gate and that did pass. I did not run a live UI screenshot/pixel diff because the change is a UI-thread responsiveness fix rather than a visual change - the regressions assert the deferred-update behavior directly. The previously documented `src/decode.rs` dirty change in earlier entries is still untouched here.

## [2026-04-23 22:37 PDT] - Close CPU/GPU parity gaps raised by param-tuning audit
**Action:** After a three-reviewer audit of the 12 editing sliders and surrounding param tunings returned two documentation drifts, one extrapolation concern, one narrow-range concern, two CPU/GPU divergences, and a secondary regime-cliff, applied the following fixes:
1. **Temperature/tint range widened** in `src/main.rs::slider_range` from `(-30, 30)` to `(-60, 60)` and updated `EditState` struct range comments in `src/edit.rs:107-118`. The existing `kelvin = 6500 + T*55` mapping now spans ~3200K..~9800K, covering tungsten (~3200K) through cloudy/open-shade (~9800K). Updated the docstring on `temperature_tint_matrix`. Updated the existing `slider_ranges_are_reasonable` test to the new bounds and added `temperature_slider_covers_tungsten_and_cloudy_kelvin` to pin the intent rather than just the numbers. Left sub-4000K values as an extrapolation of the CIE D-illuminant polynomial — Codex and Claude both agreed not to clamp because that would defeat the tungsten-coverage goal.
2. **Negative intermediate RGB guarded after Bradford CAT** on both CPU (`src/edit.rs::apply_all` after `apply_temperature_tint`) and GPU (`assets/shaders/image.wgsl` fragment main after `temp_mat * px`). Strong warm shifts on saturated blue used to drive luminance below zero, which silently short-circuited `apply_contrast` (`lum <= 0 → identity`) and `apply_tone_zones` (`l_lin <= 0.0001 → identity`) and collapsed the clarity midtone to zero. Added `strong_temperature_does_not_silence_contrast` to pin that a contrast=50 edit at temperature=-60 on a pure-blue pixel still changes the output; red-phase verified (pre-fix `[0, 0, 129, 255]` identical with and without contrast), green after the guard.
3. **CPU blur kernel aligned with GPU.** Replaced the `generate_cpu_blur` 4x4 box blur with a three-stage pipeline in `src/edit.rs`: 4x4 linear-RGB downsample, then a separable 9-tap Gaussian horizontal pass, then a separable 9-tap Gaussian vertical pass, all using the same weights as `assets/shaders/blur.wgsl` (`0.2492, 0.1836, 0.1216, 0.0540, 0.0162`). Added `generate_cpu_blur_applies_separable_gaussian_matching_gpu_kernel` pinning the 3x3 separable response (center = `w[0]^2`, orthogonal = `w[0]*w[1]`, corners = `w[1]^2`). Also, after Codex flagged that the CPU clarity/dehaze lookup was still nearest-cell (`x/4, y/4`) even though the GPU uses a linear sampler, added a new `sample_blur_bilinear` helper and switched `render_edited_image` to use it; `sample_blur_bilinear_interpolates_across_quarter_res_cells` pins the bilinear weighting at 4-pixel boundaries.
4. **CPU save now applies lens distortion and TCA** to match the GPU preview. Added a `LensCorrection` struct in `src/edit.rs` bundling `dist[3] / vig[3] / tca_r / tca_b`, a `sample_rgba_bilinear` helper with edge-clamp, an `apply_distortion_uv` mirroring `apply_distortion` in `image.wgsl`, and a `sample_tca_rgba` mirroring `apply_tca`. Rewrote `render_edited_image` so each output pixel computes `uv_rot`, applies distortion when lens is enabled, samples with TCA when `tca_r != 1.0 || tca_b != 1.0`, and falls back to a direct pixel read when lens correction is off (no bilinear overhead in the common path). Vignette now uses the post-distortion `tex_uv` on CPU to match the GPU (previous CPU code used pre-distortion `uv`). Kept the pass to `apply_all` clean by passing zero vignette coefficients when `apply_vig` is false so `apply_all`'s existing guard short-circuits, instead of mutating a cloned `EditState`. Renamed `SaveRequest.vig` and `LocalEditPersistRequest.vig` to `lens: edit::LensCorrection` and added `App::current_lens_correction` mirroring `build_adjustment_uniforms`. Added `cpu_save_applies_lens_distortion_and_tca` (proves non-identity `dist` and non-identity TCA each change the saved pixels independently) and `cpu_save_skips_lens_correction_when_disabled` (proves the toggle actually gates the math).
**Code reviewer comments:**
- Codex / Design: Codex's first pass found the four CPU↔GPU divergences the audit had pointed at and flagged one that my initial fix missed — the CPU was still doing nearest-cell integer indexing into the quarter-res blur atlas even after the kernel matched, while the GPU's linear sampler bilinearly interpolates. Landed a `sample_blur_bilinear` helper and used it in `render_edited_image`; the follow-up Codex notes became test-strength asks rather than correctness blockers. Codex also agreed with keeping the temperature extrapolation to 3200K rather than clamping to 4000K.
- Gemini / Design: Gemini agreed the four fixes were the right cuts and confirmed the lens-correction math mirrors the shader formula-for-formula. Called the initial `effective_state` cloning trick "not cleanest" and suggested a dedicated options struct; I instead removed the clone entirely by relying on `apply_all`'s own zero-vig short-circuit, which Codex and Claude both prefer. Gemini suggested clamping kelvin at 4000K — I kept the extrapolation because clamping would defeat the stated tungsten-coverage goal.
- Claude / Design: Claude independently confirmed distortion, TCA, vignette-UV, and blur-UV all match the shader and called out two pre-existing quirks (unused `image_aspect` uniform, UV-space radius not aspect-corrected) as separate follow-ups rather than part of this change. Noted Gemini's earlier "tone-zone clamp-order divergence" from the first audit was a hallucination — I had already verified both sides compute `clamp(Σ(w·slider·2), ±2)` identically.
- Codex / Test coverage: Codex explicitly flagged that `cpu_save_applies_lens_distortion_and_tca` only asserts "pixels changed" and pushed for exact-value regressions. Added `sample_blur_bilinear_interpolates_across_quarter_res_cells` as an exact-value pin for the blur sampling path; left the lens path tested via the inequality-based regressions for this pass because an exact-value lens test at edges would require committing to specific bilinear rounding behavior that may need to shift for a future sub-pixel-accuracy pass. That remaining coverage gap is documented here rather than treated as a ship blocker.
- Gemini / Test coverage: Gemini asked for a rotation+lens interaction test and for edge-case sampling tests on `sample_rgba_bilinear`. Deferred both — the existing crop+rotation regressions plus the new `cpu_save_applies_lens_distortion_and_tca` give coverage of the composition, and edge-case sampling would couple the tests to u8 rounding behavior that is not a durable contract.
- Claude / Test coverage: Claude concurred with Codex on wanting a vignette-UV change regression test. Deferring that for the same reason as the lens-edge test — it requires pinning a post-distortion correction factor to a specific value, which is brittle.
- Codex / Correctness: after the bilinear-blur fix landed, Codex did not raise new correctness blockers; remaining points were the sub-4000K extrapolation (documented tradeoff) and the test-strength items above.
- Gemini / Correctness: Gemini's core correctness points were addressed (the `effective_state` removal and the kelvin extrapolation tradeoff were explicitly decided).
- Claude / Correctness: Claude independently found no correctness bugs in the new code.
- Codex/Gemini/Claude / Clean code, efficiency, memory leaks: no blocking issues reported. Claude's one micro-nit was that the `vig_uv = uv_rot` don't-care fallback when vig is zero could equally be `tex_uv` — kept as is for readability.
- Codex/Gemini/Claude / Documentation: all three accepted the stale-doc fixes (`temperature_tint_matrix` docstring and `image.wgsl` normalization comment). This entry plus the summary and lessons update cover the durable record.
**Result:** Success - `cargo test` passes `295` tests (up from `289`), `cargo clippy -- -D warnings` is clean, and `cargo build --release` is clean. The 12 editing sliders now have consistent declared ranges and matching math on both paths, the 4850K..8150K temperature range has been widened to 3200K..9800K, strong white-balance edits no longer silently silence downstream contrast, CPU clarity/dehaze now uses the same Gaussian kernel shape and bilinear lookup as the GPU preview, and enabling lens correction now bakes distortion and TCA into the saved file the way the preview has always shown.
**Files changed:** `src/edit.rs`, `src/main.rs`, `assets/shaders/image.wgsl`, `docs/devlog/summary.md`, `docs/learning/lessons.md`, `docs/devlog/detailed/2026-04-23_2026-04-23.md`
**Reasoning:** The four fixes address four different classes of gap. The temperature widening is a UI-range tuning. The negative-RGB guard is a single-line defensive add that prevents a regime cliff without changing normal-case behavior. The blur kernel/sampling alignment is a save-vs-preview parity fix for clarity/dehaze, addressed in two phases because the first reviewer round found the kernel fix was necessary but not sufficient. The CPU distortion+TCA is the largest change because it required turning the CPU save from an iterate-over-input-pixels loop into an iterate-over-output-pixels-and-sample loop, plus threading a new parameter struct through `SaveRequest`/`LocalEditPersistRequest` and their callers, but it is the one that materially changes what users see on save. Taken together, these close the CPU↔GPU gaps the audit surfaced without introducing new product scope.
**Notes:** Final validation: `cargo test` (`295` tests), `cargo clippy -- -D warnings`, `cargo build --release` — all clean. Pre-existing quirks surfaced by the reviewers but out of scope for this change and filed as follow-ups: (a) `image_aspect` uniform is sent to the GPU but never consumed by the shader, so lens radial math is in UV space without aspect correction on both paths, (b) the GPU blur's horizontal pass uses a stride-4 sampling pattern that skips some source pixels, (c) `image.wgsl` double-converts blur texels through `srgb_to_linear` after the `Rgba8UnormSrgb` auto-linearization, (d) no GPU-level regression pins the `max(temp_mat * px, vec3(0.0))` guard. I did not add a devlog entry for each sub-fix because they are all motivated by the same audit report and ship together. The earlier review artifacts under `code_review/` (context files, Codex/Gemini/Claude reviewer transcripts for both the initial audit and this final parity round) are intended to be disposable once this durable record exists.

